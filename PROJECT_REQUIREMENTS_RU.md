# Единый реестр требований Natro

Это основной и обязательный источник требований для Natro, iPhone Helper, модификации Яндекс
Навигатора, HUD Speed Bridge, HUD и панели приборов. Перед любой разработкой или выпуском нужно
прочитать этот файл целиком. Если сообщение пользователя меняет или добавляет требование, запись
добавляется сюда **до** реализации или одновременно с ней.

- Репозиторий: `AGKulikov/Status`.
- Рабочая ветка навигации: `feature/navigation-hud-v2`.
- Первично восстановленный период: **25.08.2026–01.09.2026**.
- Последнее обновление: **03.09.2026**.
- Владелец требований и единственный источник окончательной приёмки: пользователь.

В первичный реестр включены доступная история разговоров, исправления пользователя, переданные
скриншоты/видео, опубликованные исходники и история выпусков за указанный период. Если точный
визуальный эталон был передан только изображением или видео, словесное требование ниже не заменяет
эталон: при проверке необходимо снова сопоставить результат с исходным вложением.

## Как вести этот файл

1. Каждое новое требование получает постоянный идентификатор. Идентификаторы не переиспользуются.
2. Старое требование не удаляется. При изменении решения оно получает статус `ЗАМЕНЕНО` и ссылку
   на новое требование.
3. `В КОДЕ` не означает `ГОТОВО`. Для автомобильного интерфейса готовность наступает только после
   проверки на целевом KX11 и подтверждения пользователя.
4. Перед выпуском формируется матрица: требование → исходный файл → автоматическая проверка →
   результат на KX11. Непроверенный пункт нельзя описывать как исправленный без оговорки.
5. После каждого нового сообщения пользователя обновляются разделы требований, открытых дефектов
   и журнал изменений в конце файла.

Статусы:

- `ТРЕБОВАНИЕ` — принято, реализация не подтверждена.
- `В РАБОТЕ` — выполняется сейчас.
- `В КОДЕ / НУЖЕН KX11` — найдено в опубликованном коде и покрыто доступными статическими тестами,
  но не подтверждено на машине.
- `ПОДТВЕРЖДЕНО` — пользователь подтвердил правильную работу на целевом устройстве.
- `ОШИБКА` — пользователь сообщил, что реализация не соответствует требованию.
- `ЗАБЛОКИРОВАНО` — требуется внешняя возможность, оборудование или выбор пользователя.
- `ЗАМЕНЕНО` — действует более поздняя формулировка.

## 1. Неподлежащие изменению правила проекта и выпусков

| ID | Требование | Статус / критерий |
|---|---|---|
| REL-001 | Приложение называется **Natro**, а не Status Widget. | `ТРЕБОВАНИЕ`; проверять label и пользовательские тексты APK. |
| REL-002 | Сохранять прежний `applicationId` Natro и данные пользователя при обновлении. | `ТРЕБОВАНИЕ`; установка только поверх текущей сборки без очистки данных. |
| REL-003 | Каждый новый Natro имеет строго больший `versionCode`, обычно предыдущий +1. | `ТРЕБОВАНИЕ`; проверять через `aapt` и против предыдущего APK. |
| REL-004 | Номер версии содержит цифры патча только `0…9`: после `2.5.9` идёт `2.6.0`, а не `2.5.10`. | `ПОДТВЕРЖДЕНО` для `2.6.0`; правило действует для будущих выпусков. |
| REL-005 | Все устанавливаемые APK подписываются стабильной общей подписью Natro, чтобы обновляться поверх наших сборок. | `ТРЕБОВАНИЕ`; SHA-256 сертификата `6e9855aedc008bbdd8a7fbf3f490be07f964b7ac658a837a1592647a08365c75`. |
| REL-006 | Перед выдачей проверять подпись, схемы v2/v3, одного подписанта, `zipalign`, пакет, label, `versionName`, `versionCode` и установочную совместимость с предыдущим APK. | `ТРЕБОВАНИЕ`; отчёт должен прилагаться к выпуску. |
| REL-007 | Все написанные нами исходники всегда сохраняются в публичном `AGKulikov/Status`, включая Natro, Navigator mod, HUD Speed Bridge, Helper, тесты, патчеры и сценарии сборки. | `ТРЕБОВАНИЕ`; удалённое дерево сверяется с локальным. |
| REL-008 | Оригинальные APK, декомпилированные чужие исходники, базы камер, лицензии, токены и закрытые ключи в публичный Git не помещаются. | `ТРЕБОВАНИЕ`; baseline принимается только по закреплённому SHA-256. |
| REL-009 | Подписанные APK передаются отдельно; наличие APK без опубликованного соответствующего дерева исходников недопустимо. | `ТРЕБОВАНИЕ`. |
| REL-010 | Нельзя выдавать старую версию под видом новой или собирать от непроверенного/неопубликованного дерева. | `ТРЕБОВАНИЕ`; provenance содержит commit и tree SHA. |
| REL-011 | Успешная компиляция и статические тесты не доказывают работу интерфейса на KX11. Это нужно явно указывать до проверки пользователем. | `ТРЕБОВАНИЕ`. |
| REL-012 | При разработке дизайнов панели приборов APK не собирать до одобрения дизайна, если пользователь отдельно не дал прямую команду на сборку. | `ТРЕБОВАНИЕ`; текущая прямая команда на исправление/сборку является разрешением для данного выпуска. |
| REL-013 | Этот реестр является живым: новые требования добавлять при поступлении, статусы менять только с доказательством. | `В РАБОТЕ`; контроль через `AGENTS.md` и историю Git. |
| REL-014 | Все переданные для выпуска логи, видео и скриншоты должны быть просмотрены; необъяснённые падения, критические ошибки и расхождения с визуальным эталоном нельзя молча считать закрытыми. | `ТРЕБОВАНИЕ`; в отчёте указывать источник доказательства или отсутствие проверки. |
| REL-015 | Стабильный комплект подписи уже создан проектом и хранится в защищённом постоянном хранилище: `status-widget-ha-update.jks` и приватный резервный архив с файлом пароля. Перед выводом об отсутствии ключа обязательно проверять это хранилище и восстанавливать комплект самостоятельно; повторно запрашивать ключ у пользователя можно только после доказанной утраты или повреждения обеих копий. | `ПОДТВЕРЖДЕНО` 02.09.2026; восстановлены alias `status-widget-ha` и сертификат SHA-256 `6e9855aedc008bbdd8a7fbf3f490be07f964b7ac658a837a1592647a08365c75`. |

## 2. Целевая платформа и базовые компоненты

| ID | Требование | Статус / критерий |
|---|---|---|
| BASE-001 | Целевая магнитола — ECARX KX11, Android API 28, `arm64-v8a`. | `ТРЕБОВАНИЕ`. |
| BASE-002 | Основная рабочая область — `1760×720`. | `ТРЕБОВАНИЕ`. |
| BASE-003 | HUD: display ID `2`, поверхность `1920×1080`, активная область `x=0, y=720, 728×190`. | `ТРЕБОВАНИЕ`; любое изменение требует нового подтверждения пользователя. |
| BASE-004 | Базовый Яндекс Навигатор — `30.3.0`, пакет `ru.yandex.yandexnavi`, versionCode `739564630`. | `ТРЕБОВАНИЕ`. |
| BASE-005 | Базовый HUD Speed — `76.0-L13`, пакет `air.StrelkaHUDFREE`, versionCode `76000013`. | `ТРЕБОВАНИЕ`. |
| BASE-006 | Наш Navigator APK меняет только проверенные точки интеграции и новый dex; остальные baseline entries должны оставаться идентичными. | `ТРЕБОВАНИЕ`; проверяется boundary verifier. |
| BASE-007 | HUD Speed Bridge является read-only мостом уже загруженных камер и не должен извлекать/публиковать чужую базу. | `ТРЕБОВАНИЕ`. |
| BASE-008 | Оригинальная подпись HUD Speed недоступна. Первый переход с оригинального APK на NatroBridge требует резервной копии настроек и однократного удаления; последующие наши сборки обновляются поверх. | `ТРЕБОВАНИЕ`; миграция допускает root ADB с компьютера. |

## 3. Яндекс Навигатор, отдельные карты, HUD и карта панели приборов

### 3.1 Независимость и жизненный цикл карт

| ID | Требование | Статус / критерий |
|---|---|---|
| NAV-001 | Основной Навигатор, HUD и панель приборов должны иметь отдельные поверхности карты и независимые экземпляры/профили камеры. | `В КОДЕ / НУЖЕН KX11`. |
| NAV-002 | Масштабирование, перемещение, наклон и изменение основной карты не должны менять HUD или карту панели приборов. | `ОШИБКА` по прежним сборкам; текущую реализацию проверять на KX11. |
| NAV-003 | HUD и панель приборов используют общий источник маршрута/позиции, но рисуют карты самостоятельно, а не скриншот основной карты. | `В КОДЕ / НУЖЕН KX11`. |
| NAV-004 | HUD-карта должна запускаться автоматически при холодном старте, а не только после входа в её настройки. | `ОШИБКА` в прошлых сборках; нужен холодный старт на KX11. |
| NAV-005 | Элементы HUD не должны исчезать после обновления; карта, часы и настроенные слои должны восстанавливаться без ручного открытия редактора. | `ОШИБКА`; нужен тест обновления и холодного старта. |
| NAV-006 | После завершения или отмены маршрута на HUD не должен оставаться фантомный маршрут. | `ОШИБКА`; проверять остановку, новый маршрут и потерю Guidance. |
| NAV-007 | Полоса маршрута/пробок не должна мерцать ни с маршрутом, ни без него. | `ОШИБКА`; требуется длительный визуальный тест. |
| NAV-008 | Повороты карты, особенно около 90°, должны быть плавными и не перегружать систему. | `ТРЕБОВАНИЕ`; сравнивать с основной картой и измерять frame pacing. |
| NAV-009 | На основной карте не должно быть двух курсоров/стрелок; карта не должна прыгать из-за конкурирующих владельцев камеры или позиции. | `ОШИБКА` в прежних сборках; нужен маршрутный тест на KX11. |
| NAV-010 | При открытии Музыки, климата или другого приложения независимые карты продолжают движение не менее двух минут; возврат в Навигатор не создаёт второй курсор и не вызывает скачок основной карты. | `В КОДЕ / НУЖЕН KX11`. |
| NAV-011 | Обычный и оконный Навигатор не должны самопроизвольно завершаться после запуска. Automotive `NavigationLayer`/дополнительная `GuidanceCamera` запрещены на независимом `OffscreenMapWindow`, если они приводят к асинхронному падению MapKit. | `ОШИБКА` подтверждена журналом 01.09.2026: три завершения через 13, 10 и 9 с после подключения HUD-карты; безопасный hotfix в коде, нужен тест без маршрута и на маршруте по 30 минут. |
| NAV-012 | Вся независимая карта HUD и вся карта панели приборов не должны мерцать, кратко очищаться или показывать пустой кадр при обновлении позиции, маршрута, пробок, настроек либо surface. Последний полностью отрисованный кадр сохраняется до готовности следующего; запрещено периодически пересоздавать `OffscreenMapWindow`, renderer или bitmap только ради обновления данных. | `В КОДЕ / НУЖЕН KX11`; после ошибки 02.09.2026 устранены обновление геометрии маршрута до 10 раз/с, повторная публикация surface при resize и отзыв lease при временной невидимости окна. Пройденный маршрут скрывается одним обратимым `Subpolyline.hide`, lease сохраняет последний кадр. |
| NAV-013 | При изменении ширины или высоты элемента карты HUD карта перестраивает реальный viewport и композицию под новую геометрию, а не растягивает последний кадр старого размера. Во время resize сохраняется последний готовый кадр до готовности нового, поэтому исправление не должно возвращать мерцание или пустую surface из `NAV-012`. | `В КОДЕ / НУЖЕН KX11`; resize публикует новую неизменяемую generation и пересоздаёт `OffscreenMapWindow` после короткого объединения изменений, требуется `GATE-015`. |
| NAV-014 | Известные аварийные механизмы считаются постоянными запретами для каждого следующего выпуска: независимый `OffscreenMapWindow` не создаёт Automotive `NavigationLayer` и собственную `GuidanceCamera`; оконная identity типа 2038 не применяется до конца `onResumeFragments` и не переотправляется при обновлении несвязанных настроек; смена surface сначала отсоединяет старую поверхность, а все отложенные Natro-callback оконного режима изолируют свои исключения от главного looper Навигатора. | `В КОДЕ / НУЖЕН KX11`: повторный аудит 02.09.2026 не нашёл возвращённых нативных путей, DEX-verifier расширен, новые poll/inset callbacks получили аварийную границу; требуется `GATE-007` и `GATE-021`. |
| NAV-015 | Перед следующими правками оконного интерфейса должна существовать проверяемая карта фактической UI-иерархии Navigator 30.3.0 для `MapActivity`, свободного режима, активного Guidance, верхних уведомлений, штатных контролов и слоя Natro. Для каждого изменяемого узла фиксируются layout/владелец, способ появления, inset/padding/margin, динамическая замена, допустимое вмешательство и восстановление. Утверждение о владельце отступа или координаты без этой цепочки запрещено. | `СТАТИЧЕСКИ ЗАВЕРШЕНО`: точный baseline `663018fb…f27f` разобран в `docs/NAVIGATOR_30_3_UI_CONTAINER_AUDIT_RU.md`; аппаратные свойства vendor WindowManager/анимаций подтверждаются только соответствующими KX11-gates. |

### 3.2 Оконный режим Навигатора

| ID | Требование | Статус / критерий |
|---|---|---|
| WIN-001 | Навигатор должен стабильно запускаться в ограниченном окне из лаунчера и панели приборов, без промежуточного падения. | `ОШИБКА`; проверять холодный/тёплый запуск не менее 10 раз. |
| WIN-002 | Оконный режим не должен сам переходить в полный экран, скрывать HOME, системную строку или нижнее меню. | `ТРЕБОВАНИЕ`. |
| WIN-003 | Вокруг окна не должно быть чёрного непрозрачного фона; фон и углы соответствуют выбранным настройкам. | `ОШИБКА`; нужен визуальный тест. |
| WIN-004 | Должны настраиваться положение, размер, фиксация окна и радиус скругления. | `ТРЕБОВАНИЕ`. |
| WIN-005 | При тапе по карте слева должна появляться кнопка переключения полноэкранного/оконного режима. | `В КОДЕ / НУЖЕН KX11`: реализовано заново через фактический штатный контейнер по `WIN-018`, требуется `GATE-024`. |
| WIN-006 | Верхние элементы в оконном режиме должны располагаться выше; не допускается двойной внутренний отступ от системного inset. | `ОШИБКА В 2.6.8 НА KX11`: именованные контейнеры получили нулевой padding, но штатные inset-listener вложенных `PaddingtonView` снова добавляют root inset; исправление 2.6.9 ведётся по `WIN-014`. |
| WIN-007 | Для оконного режима нужны отдельные переключатели скрытия элементов интерфейса. | `ТРЕБОВАНИЕ`. |
| WIN-008 | Поведение и расположение кнопки сверять с предоставленным рабочим модом 29.4.2. | `ТРЕБОВАНИЕ`; копировать поведение, не чужой код. |
| WIN-009 | Кнопка режима не является постоянным оверлеем: появляется после касания карты третьей в левом столбце штатных контролов, переключает окно/полный экран и затем скрывается по штатному сценарию. | `В КОДЕ / НУЖЕН KX11`: отдельный таймер/overlay удалён, видимость наследуется от фактического rail по `WIN-018`; требуется `GATE-024`. |
| WIN-010 | Переключение в обе стороны выполняет один и тот же элемент: в оконном режиме он переводит в полный экран, в полном экране — обратно в окно. Раздельные кнопки и ветвь, отсутствующая в одном режиме, запрещены. | `В КОДЕ / НУЖЕН KX11`: один reparentable-экземпляр сохраняет двусторонний listener; требуется `GATE-016` и `GATE-024`. |
| WIN-011 | Кнопка режима должна быть реальным дочерним элементом штатного левого вертикального блока управления сразу после кнопок дорожного события и Алисы/голоса. Экранный оверлей с вычисляемой координатой, а также сохранённые положения справа/снизу запрещены. | `ЗАМЕНЕНО WIN-013`: аппаратная проверка 2.6.8 доказала, что внедрение дочернего элемента в перестраиваемое дерево Яндекса делает кнопку недоступной. |
| WIN-012 | В оконном режиме нулевой верхний системный inset применяется не только к карте, но и к отдельному штатному `PaddingtonView`/`controls_engine_container` и контейнеру верхних уведомлений; при возврате в полный экран исходные padding/inset восстанавливаются. | `ОШИБКА В 2.6.8 НА KX11`; перечисления именованных контейнеров недостаточно, расширено `WIN-014`. |
| WIN-013 | Единственная кнопка режима принадлежит стабильному слою Natro, после каждого тапа гарантированно выводится поверх интерфейса и визуально занимает третье место в левом штатном столбце сразу после дорожного события и Алисы/голоса. Яндекс не должен удалять, скрывать или переподключать её при перестройке своего дерева; справа и в постоянном виде кнопки нет. | `ЗАМЕНЕНО WIN-018`: пользователь потребовал использовать фактический штатный контейнер, его подложку и цикл появления; стабильный overlay остаётся только безопасным владельцем/fallback до готовности контейнера. |
| WIN-014 | В оконном режиме повторный верхний inset подавляется у каждого фактически созданного `PaddingtonView` в деревьях контролов и верхних уведомлений, включая поздно добавленные вложенные контейнеры. Нельзя ограничиваться разовой записью padding: штатный listener, повторно читающий root inset, должен быть переопределён на всё время жизни оконного Activity; полноэкранный Activity сохраняет штатные insets. | `ОШИБКА НА KX11 03.09.2026`: свободный режим поднялся, но отдельный поздний контейнер активного Guidance сохранил повторный inset; уточнено `WIN-015`. |
| WIN-015 | При активном ведении маршрута верхняя карточка манёвра, группа текущей скорости/ограничения и остальные верхние Guidance-элементы в оконном режиме используют тот же нулевой повторный системный inset, что и свободный режим. Отдельный `PaddingtonView` вокруг `navi_guidance_controls_touch_container` должен находиться заново после каждой поздней замены Guidance-контроллера и удерживаться собственным listener/guard; ссылка на уже отсоединённое дерево недопустима. | `ОШИБКА НА KX11 03.09.2026`: в подписанном Navigator 30.3.0 из пары Natro 2.7.0 реализация ограничивалась внутренним `PaddingtonView`, но по `IMG_8227.jpeg` карточка манёвра и вся группа скорости по-прежнему вместе смещены вниз; исправление продолжено на внешних уровнях в `WIN-017`. |
| WIN-016 | Кнопка оконного режима всегда находится слева непосредственно под штатной парой «дорожное событие + Алиса/голос» и не меняет координату между тапами, появлением/скрытием карточки манёвра, свободным режимом и активным ведением. Позицию нельзя вычислять по глобальному `alice_fab`, нулевым ghost-view или другим плавающим контролам. После первого валидного layout точный третий слот фиксируется на всё время жизни Activity; до layout используется один детерминированный левый fallback. | `ЧАСТИЧНО ЗАМЕНЕНО WIN-018`: запрет на глобальные/ghost-якоря сохраняется, но экранная координата больше не является основным механизмом — третий слот должен принадлежать фактическому левому контейнеру. |
| WIN-017 | Исправление верхнего отступа активного ведения применяется ко всему экрану Guidance, а не к одному составному знаку: в оконном режиме общий `activity_container_controller` и безымянный корень `CarGuidanceController`, содержащий одновременно `contextmaneuverview` и `speed_group`, не получают глобальный status-bar inset. Штатные внутренние отступы элементов (в baseline 30.3.0 — 12 dp сверху) сохраняются; фиксированный отрицательный `translationY` и индивидуальное смещение отдельных знаков запрещены. Поздняя замена контроллера должна переопределяться по фактической identity, полноэкранный режим восстанавливает штатное состояние. | `В КОДЕ / НУЖЕН KX11`: внешний Conductor-root и общий визуальный root активного ведения разрешаются отдельно, получают zero-top dispatch/guard, а динамическое дерево Conductor сканируется на новые `PaddingtonView`; требуется `GATE-016`, `GATE-021` и `GATE-023`. |
| WIN-018 | Кнопка оконного/полноэкранного режима должна быть третьим визуальным элементом фактического левого вертикального контейнера сразу после точной штатной пары `guidance_add_road_event` + `guidance_open_voice_search` либо `navi_service_add_road_event` + `navi_service_open_voice_search`. Валидируется ближайший общий вертикальный контейнер: для Guidance прямым ребёнком является штатная обёртка `MapControlsFrameLayoutRect`, внутри которой находится `guidance_add_road_event`, а голосовая кнопка является следующим прямым ребёнком; для свободного режима оба штатных контрола являются прямыми детьми. Поиск по глобальным Alice/ghost/координатам запрещён. Кнопка использует штатный прямоугольный фон, размер, внутренние отступы и нажатое состояние семейства `MapControlsFrameLayoutRect`, а появление, скрытие, перемещение, alpha/scale и перестроение наследует от того же штатного контейнера, без отдельного пятиисекундного fade/таймера Natro. После замены свободного режима/Guidance элемент заново подключается только к новой валидной паре; до её появления touch-прозрачный слой Natro может держать кнопку скрытой, но не рисовать отличающийся постоянный дубль. | `В КОДЕ / НУЖЕН KX11`: координатная кнопка и её fade удалены; один экземпляр в штатной оболочке с векторной финальной иконкой подключается третьим к exact rail и безопасно паркуется `GONE` между деревьями; требуется `GATE-024`. |

### 3.3 Навигационные слои и визуальные требования

| ID | Требование | Статус / критерий |
|---|---|---|
| MAP-001 | Светофоры с обратным отсчётом должны отображаться. Если приватный stock-view недоступен, резервный слой сохраняет сигнал и секунды. | `В КОДЕ / НУЖЕН KX11`; положение и хвост уточнены в `MAP-032`. |
| MAP-002 | Знаки движения по полосам должны располагаться сбоку от дороги, как в основном Навигаторе, и не перекрывать дорогу впереди. | `В КОДЕ / НУЖЕН KX11`; автоматическая сторона уточнена в `MAP-032`, штатный хвост — в `MAP-033`. |
| MAP-003 | Ограничения камер из MapKit переводятся из м/с в км/ч: около `16,67 → 60`, `25 → 90`. Нельзя рисовать `17` и `25` как готовые км/ч. | `В КОДЕ / НУЖЕН KX11`; `CameraSpeedNormalizer` и Java-регрессия прошли CI 01.09.2026. |
| MAP-004 | HUD Speed передаёт числовой лимит вместе с единицей; `KPH`, `MPH` и `MPS` нормализуются в целые км/ч. | `В КОДЕ / НУЖЕН KX11`; неизвестная/отсутствующая единица сохраняет KPH для обратной совместимости, Java-регрессия прошла CI 01.09.2026. |
| MAP-005 | Камера и её скорость отображаются одним цельным знаком. Отдельная камера плюс отдельный круг скорости рядом запрещены. | `В КОДЕ / НУЖЕН KX11`. |
| MAP-006 | Одна физическая камера не должна порождать несколько соседних знаков. Дубликаты между Yandex/HUD Speed и внутри одного источника объединяются, сохраняется запись с наиболее полной информацией. | `В КОДЕ / НУЖЕН KX11`; правило объединения типов и направлений уточнено в `MAP-031`. |
| MAP-007 | Направление камеры должно иметь отдельные настройки размера и прозрачности; изменения применяются сразу. | `ЗАМЕНЕНО MAP-026`; единый параметр размера больше не используется. |
| MAP-008 | Названия улиц рисуются непосредственно на полосе маршрута отдельным плоским слоем, ориентированным вдоль маршрута, а не отдельной карточкой рядом. | `ЗАМЕНЕНО MAP-024`; аппаратная проверка показала, что самостоятельно нарисованный bitmap не повторяет штатный вид и изгиб подписи Яндекса. |
| MAP-009 | На линии маршрута должны отображаться стрелки предстоящих поворотов, как в основном Навигаторе. | `В КОДЕ / НУЖЕН KX11`; после невидимого результата 2.6.2/2.6.3 найдено, что штатный `createDefaultManeuverStyle` возвращает `enabled=false`, а код повторно присваивал это значение стрелкам. Теперь полный `DrivingRoute.geometry` передаётся в `RouteHelper.addManeuvers`, стиль явно создаётся с `enabled=true`, стрелки явно видимы. |
| MAP-010 | Пользователь может отключить автоматическое изменение масштаба от скорости и задать фиксированный zoom. | `В КОДЕ / НУЖЕН KX11`. |
| MAP-011 | В режиме свободной камеры фиксируется географическая точка, но последующие настройки zoom/tilt продолжают работать. | `В КОДЕ / НУЖЕН KX11`. |
| MAP-012 | В режиме «Только дороги» всё между дорогами прозрачно, но Guidance, знаки, манёвры и обязательные маршрутные элементы не фильтруются. | `ОШИБКА` в прежней версии; исправление требует визуальной проверки. |
| MAP-013 | Фон HUD-панели может быть полностью прозрачным. | `ТРЕБОВАНИЕ`. |
| MAP-014 | Дорожные события имеют отдельное подменю и режимы `всегда`, `только на маршруте`, `скрыто`. | `В РАБОТЕ`: безопасный standalone-слой поддерживает `всегда`/`скрыто` и включает `только на маршруте` при свежем маршруте, но пока может захватить событие с соседней дороги; аварийный automotive-слой возвращать нельзя. |
| MAP-015 | При активном маршруте пробки отображаются на маршруте; без маршрута допускается показ всех пробок. Пройденный участок не должен снова выделяться как непройденный. | `В КОДЕ / НУЖЕН KX11`. |
| MAP-016 | Настройки карты HUD и панели приборов должны быть человеческими: переключатели и ползунки вместо обязательного ручного ввода чисел. | `ТРЕБОВАНИЕ`. |
| MAP-017 | Размер, видимость и приоритет каждого пользовательского слоя настраиваются отдельно, без влияния на основную карту. | `ТРЕБОВАНИЕ`. |
| MAP-018 | На HUD и панели приборов отображается конечная точка активного маршрута; после завершения маршрута маркер исчезает вместе с маршрутными элементами. | `В КОДЕ / НУЖЕН KX11`. |
| MAP-019 | Манёвр, расстояние, полосы, маршрутные пробки, светофоры и повороты показываются только при свежем активном маршруте; после завершения маршрута или потери bridge устаревшие элементы очищаются не позднее 3 секунд. | `В КОДЕ / НУЖЕН KX11`. |
| MAP-020 | По умолчанию используется feature-aware порядок MapKit 30.3.0; ручной порядок включается отдельным переключателем и имеет ползунок для каждого независимо переставляемого root-layer. Нативные стрелки не являются отдельным слоем и всегда следуют приоритету маршрутной полилинии. При отключении ручного режима восстанавливается проверенный автоматический порядок без перестановки Yandex-owned слоёв. | `В КОДЕ 2.6.9 / НУЖЕН KX11`; старый отдельный ползунок стрелок удалён как неработающий, JSON-ключ сохранён alias к маршруту. |
| MAP-021 | Для HUD и панели приборов независимо настраиваются размеры курсора, знаков полос, камер и их информации, светофоров и секунд, названий улиц, дорожных событий, конечной точки, а также толщина маршрута и обычных улиц. | `В КОДЕ / НУЖЕН KX11`. |
| MAP-022 | При активном маршруте курсор использует привязанную к маршруту позицию MapKit. Обрезка линии обратима: после ложного скачка GPS вперёд и возврата скрытый участок маршрута восстанавливается сразу. | `В КОДЕ / НУЖЕН KX11`. |
| MAP-023 | Повороты на линии нельзя выводить самостоятельно нарисованными `Canvas`-пиктограммами или угадывать по строке действия. Используются штатные `PolylineMapObject.addArrow` и `RouteHelper.createDefaultManeuverStyle/applyManeuverStyle`, привязанные к точной `PolylinePosition` активного маршрута. | `В КОДЕ / НУЖЕН KX11`; основной путь — штатный `RouteHelper.addManeuvers` на неизменённой полной геометрии маршрута, с клоном штатного стиля и принудительным `enabled=true`; `addArrow` оставлен только совместимым штатным fallback без самодельной графики. Требуется `GATE-011`. |
| MAP-024 | На независимых картах HUD и панели приборов названия улиц отрисовывает штатный дорожный слой MapKit: шрифт, контур, ориентацию по изгибу дороги, плотность и разрешение конфликтов выбирает Яндекс. Самодельные `Bitmap`/`Canvas`-надписи и отдельные placemark-карточки запрещены. Публичный MapKit не фильтрует штатные подписи по принадлежности активному маршруту, поэтому режим «только на маршруте» заменяется включением штатных названий дорог; прежняя сохранённая настройка мигрирует без исчезновения подписей. | `В КОДЕ / НУЖЕН KX11`; Natro 2.6.5 и Navigator собраны из commit `21956ddf1c5b98ad85c1cb901972a5746013e183`, эталон — `IMG_8189.jpeg`, требуется `GATE-012`. |
| MAP-025 | Для штатных стрелок поворотов на полилинии HUD и панели приборов независимо настраиваются цвет заливки, цвет обводки, толщина обводки и длина. Изменение длины пропорционально меняет общую длину и наконечник, но не ширину стрелки и не толщину обводки. Ширина тела стрелки всегда определяется фактической толщиной маршрутной полилинии; отдельный множитель ширины стрелки запрещён. Если пользователь не выбрал цвета, сохраняются штатные цвета `ManeuverStyle` Яндекса. | `ЧАСТИЧНО ЗАМЕНЕНО MAP-030`: цвета, обводка и привязка ширины тела сохраняются, но наконечник больше не обязан меняться вместе с длиной. |
| MAP-026 | Единая настройка размера треугольного сектора направления камеры удаляется. Для HUD и панели приборов независимо задаются длина сектора, ширина только его широкого основания, цвет и прозрачность. Изменение ширины не меняет длину, положение вершины и дальность; изменение длины не растягивает основание. Старое значение размера мигрирует одновременно в длину и ширину. | `В КОДЕ / НУЖЕН KX11`; независимая геометрия, настройки и миграция покрыты статическими регрессиями, требуется `GATE-014`. |
| MAP-027 | Единый знак камеры со скоростью должен быть компактным и максимально близким к штатному виду Яндекс Навигатора: белый круг ограничения с красной окантовкой, число скорости по центру и маленький значок камеры у края. Большая прямоугольная карточка, хвост, подпись источника и дополнительные условные символы внутри запрещены; прозрачное поле bitmap должно быть минимальным. | `ЗАМЕНЕНО MAP-029`; пользователь отдельно уточнил, что маленький значок камеры у круга скорости тоже нужно удалить. |
| MAP-028 | Диапазон настройки длины штатных стрелок поворотов на полилинии начинается с `10%`, а не с `50%`, отдельно для HUD и панели приборов. Остальная геометрия и правила `MAP-025` сохраняются. | `В КОДЕ / НУЖЕН KX11`; диапазон конфигурации, обеих настроек и слоя изменён на 10–250%, требуется обновлённый `GATE-013`. |
| MAP-029 | Если источник передал ограничение скорости камеры, единый знак состоит только из компактного белого круга с красной окантовкой и числом скорости. Дополнительный маленький значок камеры на краю или рядом запрещён. Значок камеры допускается только для камеры без известного ограничения скорости, чтобы такой объект оставался распознаваемым. | `УТОЧНЕНО MAP-031`: для обычной камеры скорости остаётся только круг; если одна физическая камера одновременно контролирует полосы/перекрёсток/остановку, рядом внутри одного составного знака сохраняется штатная пластина соответствующего контроля, но маленькая камера на круг скорости всё равно запрещена. |
| MAP-030 | Для HUD и панели приборов размер треугольного наконечника штатной стрелки поворота на маршрутной полилинии настраивается отдельно от длины тела в диапазоне 10–250%. Изменение наконечника не меняет длину и ширину тела; изменение длины не обязано менять наконечник. При невозможной комбинации MapKit может только ограничить наконечник длиной стрелки. Ширина тела остаётся привязана к толщине маршрутной линии. Старое единое значение длины мигрирует одновременно в длину и наконечник, чтобы сохранить прежний вид. | `В КОДЕ 2.6.9 / НУЖЕН KX11`; отдельное поле, два экрана настроек, миграция и статические контракты добавлены; требуется обновлённый `GATE-013`. |
| MAP-031 | Одна физическая камера может содержать несколько типов контроля и два направления. При объединении записей Yandex и HUD Speed нельзя выбрасывать второй тип или второе направление: метаданные объединяются в один маркер. Для контроля полос/разметки, перекрёстка/светофора или остановки используется точная компактная штатная пластина Яндекса, состыкованная с кругом скорости без большого пустого поля. Внутри круга скорости и у его края дополнительный маленький значок камеры запрещён. Разные последовательные физические камеры одного источника не объединяются только из-за близости дальше 12 м. | `В КОДЕ 2.6.9 / НУЖЕН KX11`; требуется обновлённый `GATE-014` по эталонам `IMG_8208.jpeg`/`IMG_8209.jpeg`. |
| MAP-032 | Табличка полос, светофоры и составные знаки камер на независимых картах HUD/панели участвуют в общем экранном размещении. На каждом движении/масштабировании карты выбирается свободное из восьми положений вокруг точной географической точки с учётом краёв viewport и уже занятых прямоугольников; карточки не должны складываться друг на друга или закрывать маршрут впереди. Светофор всегда находится сбоку от маршрута и имеет видимый штатный хвост к своей координате; региональный fallback также обязан рисовать хвост. | `ЧАСТИЧНО ЗАМЕНЕНО MAP-037`: восемь положений сохраняются только для карточек полос и светофоров; перенос любого знака камеры отклонён аппаратной проверкой 03.09.2026. |
| MAP-033 | Табличка движения по полосам должна передаваться штатной текстурой Яндекса целиком вместе с хвостиком. Хвост меняет сторону вместе с выбранным свободным положением и остаётся привязан к точной `RoutePosition`; ручной bitmap без хвостика запрещён. | `В КОДЕ 2.6.9 / НУЖЕН KX11`; слой использует `LaneSignBalloonTextureFactory.createTexture`, `BalloonAnchor` и штатный `BalloonGeometry.imageAnchor`, требуется `GATE-018`. |
| MAP-034 | После добавления или изменения любого слоя нужно повторно сверять полный порядок отрисовки HUD и панели приборов со штатным Яндекс Навигатором. Проверка охватывает дорожную подложку и подписи, пробки, маршрут, скрываемый пройденный участок, тело и наконечник стрелок манёвров на маршрутной полилинии, конечную точку, полосы, светофоры, камеры и их сектора/составные пластины, дорожные события, карточки Guidance и курсор. Нельзя считать корректным порядок, проверенный только между старыми слоями до появления новых; ручные приоритеты пользователя должны быть ограничены безопасным диапазоном, не позволяющим спрятать обязательные Guidance-элементы под дорогами или фоном. | `В КОДЕ 2.6.9 / НУЖЕН KX11`: отдельный `MapSublayerOrder` ищет каждый sublayer по `id + SublayerFeatureType`, перемещает только Natro-owned ID через `moveAfter/moveBefore`, делит камеру на `GROUND`-сектор и `PLACEMARKS_AND_LABELS`-знак и связывает стрелки с приоритетом маршрута. Автоматический порядок и безопасные feature-группы ручного режима описаны в `docs/NATRO_2_6_9_LAYER_ORDER_AUDIT_RU.md`; требуется `GATE-019`. |
| MAP-035 | Уменьшение единого/составного знака камеры не должно делать число ограничения или штатную пластину нечёткими. Поскольку MapKit принимает placemark как bitmap-текстуру, знак камеры нужно растрировать с запасом разрешения и уменьшать до выбранного экранного размера через `IconStyle`, а не рисовать текст непосредственно в минимальные 26–40 px. | `В КОДЕ 2.6.9 / НУЖЕН KX11`: минимальный диаметр исходной текстуры камеры 80 px, экранный размер и collision-рамка остаются равны выбранному масштабу; проверяется расширенным `GATE-014`. |
| MAP-036 | Правило чёткого масштабирования распространяется на все масштабируемые значки независимых карт: камеры и скорость, светофоры и секунды, таблички полос, дорожные события, конечную точку, курсор и будущие placemark/card-слои. Для каждого выбранного размера финальная текстура должна заново формироваться из исходного vector/drawable или штатного фабричного рендера Яндекса в достаточном разрешении; запрещены увеличение ранее уменьшенного bitmap, повторное bitmap→bitmap сжатие и рисование мелкого текста сразу в минимальном растре. Если публичный MapKit принимает только bitmap, допускается один качественный oversampling/downsampling до целевого физического размера; геометрические маршрут, сектора и штатные стрелки остаются нативными объектами MapKit. | `ТРЕБОВАНИЕ / НУЖЕН КОД И KX11`: камера частично соответствует через `MAP-035`, но светофоры, полосы, события, конечная точка и курсор ещё не прошли единый аудит исходной текстуры и `GATE-020`. |
| MAP-037 | Любой знак камеры — одиночный круг скорости, камера без скорости или составной знак с пластиной — остаётся визуальным центром на точной географической точке камеры и не переносится в одно из восьми положений. На активном маршруте знак не должен съезжать с линии из-за collision-layout; вершина сектора направления и центр placemark используют одну координату. Центрированная рамка камеры первой резервирует место в общем screen-space проходе, а карточки полос/светофоров обходят её. | `В КОДЕ 2.6.9 / НУЖЕН KX11`: все камерные знаки используют anchor `(0.5, 0.5)` через `reserveCentered`, порядок reservation изменён на камера → полосы → светофоры; требуется `GATE-018` и `GATE-022` по `IMG_8214.jpeg`/`IMG_8219.jpeg`. |

## 4. Карточка предупреждений HUD и левая таблица знаков

| ID | Требование | Статус / критерий |
|---|---|---|
| CARD-001 | Макет предупреждений состоит ровно из двух блоков: основное предупреждение и ниже меньший блок «Далее». | `ОШИБКА` в предыдущих вариантах; эталон — переданные изображения. |
| CARD-002 | Главный блок должен повторять согласованный визуальный эталон, а не произвольную большую карточку. | `ТРЕБОВАНИЕ`. |
| CARD-003 | Знаки в левой таблице передаются как есть из источника; нельзя заново перерисовывать или интерпретировать их пиктограммы. | `В КОДЕ / НУЖЕН KX11`; комбинированная карточка использует исходный bitmap и в live-режиме не подменяет отсутствующий знак догадочной стрелкой. |
| CARD-004 | Для левой таблицы нужны расширенные настройки каждого элемента: размер каждого шрифта, внутренние отступы, размеры строк/ячеек, интервалы и видимость. | `В КОДЕ / НУЖЕН KX11`; добавлены независимые размеры шрифтов, четыре группы внутренних отступов, размеры зон/строк, интервалы, фон, рамка и видимость. |
| CARD-005 | Кнопка «Отмена» в редакторе не сохраняет изменения и возвращает состояние, существовавшее до открытия. | `ОШИБКА`; нужен тест снимка настроек. |
| CARD-006 | Изменение масштаба не должно фиксировать обрезанное изображение или делать дальнейшую настройку невозможной. | `ОШИБКА`; границы рассчитывать от исходной геометрии. |
| CARD-007 | Масштаб меняется шагом 1%. Рядом с ползунками масштаба и положения есть `−`/`+`: положение меняется на 1 px, масштаб — на 1%. | `ТРЕБОВАНИЕ`. |
| CARD-008 | Перемещение/масштабирование должно происходить визуально в реальном времени и сохранять возможность последующей правки. | `ТРЕБОВАНИЕ`. |
| CARD-009 | У карточки ближайшего манёвра отдельно настраиваются три размера шрифта, доля и масштаб исходного знака, четыре отступа знака, четыре отступа текста, интервалы, фон, прозрачность, рамка и скругление; при отсутствии bitmap нельзя рисовать предполагаемую стрелку. | `В КОДЕ / НУЖЕН KX11`. |
| CARD-010 | Маршрутная карточка панели приборов отдельно показывает оставшееся расстояние, ETA и оставшееся время; настраивает их шрифты, отступы, знак, прогресс и маркер, использует реальные сегменты пробок и не содержит кнопку завершения маршрута. | `В КОДЕ / НУЖЕН KX11`. |

## 5. Панель приборов Natro

| ID | Требование | Статус / критерий |
|---|---|---|
| CLUSTER-001 | В Natro существует отдельный раздел «Панель приборов» для внешнего дисплея с более высоким разрешением, чем HUD. | `В КОДЕ / НУЖЕН KX11`. |
| CLUSTER-002 | Панель редактируется в реальном времени по принципу HUD: элементы можно перемещать, масштабировать, скрывать и настраивать. | `В КОДЕ / НУЖЕН KX11`. |
| CLUSTER-003 | Карта панели приборов является отдельной независимой картой со своим профилем. | `В КОДЕ / НУЖЕН KX11`. |
| CLUSTER-004 | Нужны как цифровые, так и аналоговые варианты спидометров/тахометров; целевой каталог — около 10 вариантов каждого типа. | `ТРЕБОВАНИЕ`. |
| CLUSTER-005 | Визуальные ориентиры: современные панели BMW, Mercedes-Benz и Rolls-Royce; копировать уровень качества и композицию, а не товарные знаки/закрытые ассеты. | `ТРЕБОВАНИЕ`. |
| CLUSTER-006 | Последнее решение: оставить пять новых вариантов панели и удалить прежние варианты из выбора. | `ТРЕБОВАНИЕ`; перечень пяти фиксируется согласованными макетами. |
| CLUSTER-007 | Все пять вариантов модульные. В любом варианте можно убрать отдельный элемент: например спидометр, цифру скорости внутри него, либо добавить слева информационный блок. | `ТРЕБОВАНИЕ`. |
| CLUSTER-008 | Штатные нижние «крылья» KX11 нужно визуально маскировать фоном, потому что перекрасить прошивочные элементы нельзя. | `ТРЕБОВАНИЕ`. |
| CLUSTER-009 | Фон панели настраивается отдельно: сверху чёрный, снизу выбираемый пользователем цвет, регулируется размер/глубина чёрной зоны и переход градиента. | `ТРЕБОВАНИЕ`. |
| CLUSTER-010 | Геометрия и безопасные зоны соответствуют фактической панели KX11; прошивочные участки не должны конфликтовать с нашими элементами. | `ТРЕБОВАНИЕ`. |
| CLUSTER-011 | Скорость, обороты и другие быстрые параметры должны иметь минимальную задержку, сравнимую с переданным референсным дашбордом. | `ТРЕБОВАНИЕ`; измерять задержку, не оценивать только визуально. |
| CLUSTER-012 | Архитектура телеметрии выбирается по принципу максимальной скорости без перегрузки: быстрые сигналы не ждут медленных, тяжёлые данные не должны блокировать UI. | `ТРЕБОВАНИЕ`; допускается один транспорт с разными частотами/очередями данных. |
| CLUSTER-013 | Пользователь должен сначала одобрить макеты панели; сборка APK без одобрения дизайна запрещена, кроме отдельной прямой команды. | `ТРЕБОВАНИЕ`. |
| CLUSTER-014 | Зафиксированные варианты 11–15: **Slate Horizon** — два аналоговых прибора и центральная карта; **Glacier Map** — цифровая скорость, горизонтальный RPM и карта; **Aerowave** — зеркальные дуги; **Steel Vector** — цифровая скорость и вертикальный RPM; **Continuum** — левая полукруглая шкала и отдельный правый навигационный блок. | `В КОДЕ / НУЖЕН KX11`; визуально сверить с одобренными эскизами. |
| CLUSTER-015 | Каждый из пяти вариантов хранит собственные положение, размер, слой, видимость, прозрачность, стиль и состав модулей; переключение и миграция не сбрасывают пользовательскую конфигурацию. | `В КОДЕ / НУЖЕН KX11`. |
| CLUSTER-016 | В аналоговом приборе независимо скрываются фон, шкала, цифры, стрелка, цифровое значение и единица; в цифровом — фон, единица и линия значения. Информационный блок имеет три независимо выбираемые строки. | `В КОДЕ / НУЖЕН KX11`. |
| CLUSTER-017 | Включённая карта панели приборов запускается на холодном старте самой панели без предварительного входа в настройки Natro. Панель должна создать Binder endpoint до публикации первой `TextureView` surface и повторно передать сохранённый lease после подключения Navigator. | `В КОДЕ 2.6.9 / НУЖЕН KX11`: живая панель повторяет пропущенную раннюю публикацию `TextureView` до успеха, не блокируя её недостоверным первым visibility; Navigator после внешнего запуска панели проверяет Binder, сбрасывает backoff и повторяет HELLO/lease. Требуется `GATE-009`. |

## 5.1 Панель штатной вкладки DIM

| ID | Требование | Статус / критерий |
|---|---|---|
| DIM-001 | Отдельная настраиваемая панель открывается на дисплее водителя в штатной вкладке «Навигация» и управляется прокруткой и подтверждением на руле. | `В КОДЕ / НУЖЕН KX11`. |
| DIM-002 | В панель добавляются сохранённые маршруты, приложения, функции автомобиля, сценарии умного дома, звонки и Android Intent; порядок действий редактируется. | `В КОДЕ / НУЖЕН KX11`. |
| DIM-003 | Настраиваются координаты, размер панели и строк, число видимых строк, отдельные шрифты, иконки, внутренние отступы, интервалы, цвета, рамка и прозрачность. | `В КОДЕ / НУЖЕН KX11`. |
| DIM-004 | Панель автоматически уступает дисплей mNavi, полной приборной панели Natro, штатному звонку, медиаменю, другой DIM-вкладке и выключенному экрану; при недоступности ECARX API мост восстанавливается в фоне без блокировки Natro. | `В КОДЕ / НУЖЕН KX11`. |

## 6. Natro: запуск, производительность и базовый интерфейс

| ID | Требование | Статус / критерий |
|---|---|---|
| NATRO-001 | Ускорить холодную загрузку Natro и убрать длительный период тормозов после старта. | `ОШИБКА`; измерять от подачи питания до готовности UI/телеметрии. |
| NATRO-002 | Не допускать чёрного экрана при первой загрузке и зависания на логотипе. | `ОШИБКА`. |
| NATRO-003 | Тяжёлые подсистемы запускаются параллельно там, где это безопасно; единственный владелец BLE и публикация UI-состояния сохраняют последовательность. | `ТРЕБОВАНИЕ`. |
| NATRO-004 | HUD/панель/медиа не должны перегружать hot path; частоты обновления и перерисовки ограничиваются фактическим изменением данных. | `ТРЕБОВАНИЕ`. |
| NATRO-005 | В статус-баре корректно определяются активная SIM и LTE/2G без обязательной перезагрузки. | `ОШИБКА`. |
| NATRO-006 | Иконка Bluetooth показывает состояние подключения и ANCS; иконка Wi‑Fi соответствует согласованному iOS-подобному стилю. | `ТРЕБОВАНИЕ`. |
| NATRO-007 | Заряд iPhone передаётся точным процентом, а не округлением по 5%. | `ТРЕБОВАНИЕ`. |
| NATRO-008 | Два независимо настраиваемых порога уведомления о низком заряде используют точный процент. | `ТРЕБОВАНИЕ`. |
| NATRO-009 | Последняя цветовая корректировка: при заряде ниже 20% батарея уже красная, а не жёлтая. | `ТРЕБОВАНИЕ`; заменяет прежнюю цветовую градацию для этой зоны. |
| NATRO-010 | Скрытие системных иконок, часов, BT и других выбранных элементов действительно применяется и восстанавливается. | `ОШИБКА`. |
| NATRO-011 | Для медиаплитки есть отдельное скрытие play/pause; прогресс поддерживает прямое перемещение. | `ТРЕБОВАНИЕ`. |
| NATRO-012 | Нужны любимые маршруты/кнопки маршрутов, триггер потери маршрута и геофильтр, где это технически возможно. | `ТРЕБОВАНИЕ`. |
| NATRO-013 | Иконки и элементы не должны показывать пользователю технические состояния «без лицензии», если функция доступна штатно. | `ТРЕБОВАНИЕ`. |
| NATRO-014 | Автоматическое восстановление специальных возможностей после загрузки выполняется без повторного Toast «Разрешения получены автоматически». Уже включённый компонент в полном или сокращённом Android-формате не переписывается и не считается новым разрешением. | `ОШИБКА` подтверждена фото 01.09.2026; исправление в коде, нужен холодный старт KX11 и два входа в настройки. |
| NATRO-015 | Повторное создание информационных плиток и других UI-потребителей не должно бесконтрольно расходовать лимит слушателей значений коннекторов. Плитки используют один мультиплексированный upstream-listener на экземпляр `WidgetService`, каждый прямой владелец отписывается при смене сервиса/остановке, а исчерпание защитного лимита отклоняет только лишнего подписчика и никогда не бросает исключение в главный поток Natro. | `В КОДЕ / НУЖЕН KX11`: hub и lifecycle-контракты сохранены; прежний `IllegalStateException: Too many connector value listeners` заменён безопасным отказом с диагностикой и unit-регрессией, требуется `GATE-021`. |

### 6.1 Пользовательская системная шторка

| ID | Требование | Статус / критерий |
|---|---|---|
| SHADE-001 | Вместо штатной верхней шторки KX11 нужна собственная шторка Natro, открываемая тем же жестом сверху вниз поверх лаунчера и других приложений. | `ОШИБКА НА KX11 02.09.2026`; в 2.6.3 стандартный жест не открывает панель. Исправление нельзя считать готовым до аппаратной проверки. |
| SHADE-002 | Состав и геометрия новой шторки редактируются по принципу лаунчера: пользователь добавляет нужные элементы, перемещает и масштабирует их, меняет порядок, видимость и оформление. | `РЕАЛИЗОВАНО В КОДЕ / НЕ ПРОВЕРЕНО НА KX11`; часы, дата, медиа, яркость, громкость и отдельный каталог действий имеют независимую геометрию/оформление и живой редактор. |
| SHADE-003 | Штатная шторка не должна показываться одновременно с Natro или кратко мигать перед ней. Отключается только доказанно относящийся к шторке компонент ECARX; запрещено вслепую отключать весь SystemUI, системную строку либо автомобильные защитные окна. | `В РАБОТЕ`; диагностика 01.09.2026 определила владельца окна как `ecarx.notificationcenterui`, но точный компонент ещё не установлен. |
| SHADE-004 | Отключение штатной шторки должно быть обратимым: перед изменением сохраняется исходное состояние пакета/компонента, предоставляется отдельная команда восстановления, а при неудачном запуске Natro штатная панель может быть возвращена без сброса магнитолы. | `ЧАСТИЧНО / НУЖЕН АУДИТ KX11`; добавлены read-only `kx11-shade-audit.sh` и строгий exact-component disable/restore, применение заблокировано до получения имени компонента. |
| SHADE-005 | Пользовательская шторка запускается после холодной загрузки, плавно открывается сверху вниз, закрывается обратным жестом/Back и не блокирует сенсор после закрытия. | `ОШИБКА НА KX11 02.09.2026`; после попытки открыть 2.6.3 невидимое окно блокирует все касания внутри Natro. Нужны 50 аппаратных циклов после исправления. |
| SHADE-006 | Новая шторка обязана уступать приоритет штатным окнам 360/парктроника, звонка, критическим автомобильным предупреждениям и выключению экрана; её показ не должен менять существующие правила паузы уведомлений. | `РЕАЛИЗОВАНО В КОДЕ / НЕ ПРОВЕРЕНО НА KX11`; используется единый ECARX external-overlay listener без замены логики паузы уведомлений. Нужен тест 360/звонка/критических окон. |
| SHADE-007 | Текущий APK Natro работает как обычное приложение UID `10081`, имеет `SYSTEM_ALERT_WINDOW`, но диагностика сообщает `root_available=false`; поэтому бесшовное отключение ECARX-компонента допускает одноразовую подготовку с root ADB либо установку Natro как привилегированного приложения. Имитация замены через мгновенное закрытие уже открывшейся штатной шторки не считается готовым решением. | `В РАБОТЕ / ИССЛЕДОВАНИЕ`; root ADB с компьютера ранее подтверждён в `DRIVE-003`. |
| SHADE-008 | Закрытая шторка никогда не оставляет полноэкранное touch-окно. Во время распознавания жеста окно остаётся узкой верхней полосой; расширение допускается только после принятого решения открыть. `CANCEL` без достигнутого порога, короткий тап, неудачный свайп, подавление автомобильным окном и закрытие всегда физически возвращают высоту окна к зоне жеста, даже если логическое состояние уже было `closed`. Если KX11 завершает настоящий стандартный свайп событием `CANCEL` после достаточного движения вниз, измеренный жест допускается принять как открытие. | `В РАБОТЕ`; добавлено после критического аппаратного дефекта 2.6.3. Критерий — 50 неудачных/отменённых жестов и 50 открытий/закрытий без единого заблокированного касания. |

## 7. Уведомления, камера 360 и парктроники

| ID | Требование | Статус / критерий |
|---|---|---|
| NOTIF-001 | Уведомления не теряются и не приходят с многоминутной задержкой из-за общей очереди или залипшей паузы. | `ОШИБКА`. |
| NOTIF-002 | Пауза уведомлений допускается только от двух настроенных источников: 360/парктроники и выбранные пользователем приложения. Общая пауза «при любом overlay» удаляется. | `ТРЕБОВАНИЕ`. |
| NOTIF-003 | При показе 360/парктроника новые уведомления принимаются в очередь, но не перекрывают автомобильное изображение. После закрытия показ возобновляется. | `ОШИБКА`; проверять перекрывающиеся удержания. |
| NOTIF-004 | Если 360 и парктроник создают два удержания, закрытие одного не возобновляет показ преждевременно, а закрытие последнего обязательно снимает паузу. | `ТРЕБОВАНИЕ`; нужен reference-count/token test. |
| NOTIF-005 | Уведомления аккумулятора и внутренние уведомления Natro проходят через надёжный диспетчер и не теряются даже без ANCS. | `ОШИБКА`. |
| NOTIF-006 | Одновременные уведомления показываются FIFO; позиция оверлея может быть зафиксирована. | `ТРЕБОВАНИЕ`; ранее зафиксированный интервал очереди — 1 с на элемент, пока пользователь не изменит. |
| NOTIF-007 | После отключения парктроника показ уведомлений должен продолжаться автоматически, без перезагрузки Natro. | `ОШИБКА`. |
| NOTIF-008 | Удержание 360/парктроника определяется штатными сигналами KX11: `SwtDispOnAndOffStsResp` 29021, `VisnImgDispModResp` 29043 и прежде всего `PrkgDstCtrlSts` 28995 (`2/3` — активно, `1` — закрыто). Медленный сигнал одного источника не должен преждевременно снять удержание другого. | `В КОДЕ / НУЖЕН KX11`. |

## 8. ANCS и iPhone Helper

| ID | Требование | Статус / критерий |
|---|---|---|
| ANCS-001 | После обновления Natro ANCS должен восстанавливаться без перезагрузки магнитолы и без повторного сопряжения. | `ОШИБКА`. |
| ANCS-002 | Сохраняется один владелец обратного ANCS GATT-канала; Helper не создаёт конкурирующее второе BLE-подключение после handoff. | `ТРЕБОВАНИЕ`. |
| ANCS-003 | Длительный «тихий» владелец не должен задерживать вход в scan примерно на четыре минуты; повторные ступени поиска должны быть короткими и ограниченными. | `ОШИБКА`; ориентир обсуждения — ступени около 10/20 с. |
| ANCS-004 | Ошибки `encryption key size is insufficient` и `handle is invalid` должны приводить к безопасному восстановлению, а не вечному зависанию. | `ОШИБКА`. |
| ANCS-005 | Телеметрия iPhone: точная батарея, зарядка, блокировка, тип сети и сигнал обновляются без минутной задержки. | `ОШИБКА`. |
| ANCS-006 | В Natro и Helper есть расширенный журнал этапов подключения, причин backoff/quarantine и экспорта диагностик. | `ТРЕБОВАНИЕ`. |
| ANCS-007 | Экспорт логов на магнитоле должен работать даже при отсутствии стороннего файлового приложения. | `ОШИБКА`. |
| ANCS-008 | Helper сохраняет управление климатом/функциями из Live Activity. | `ОШИБКА` в отдельных сборках. |
| ANCS-009 | Live Activity автоматически активируется только при подключении магнитолы по выбранному Bluetooth и выключается после отключения. | `ТРЕБОВАНИЕ`. |
| ANCS-010 | Взаимодействие с кнопками Live Activity не должно обнулять состояние управления; ограничение iOS на время гашения экрана фиксируется отдельно и не выдаётся за исправляемое Android-кодом. | `ТРЕБОВАНИЕ / ОГРАНИЧЕНИЕ ПЛАТФОРМЫ`. |
| ANCS-011 | Helper определяет LTE/5G/3G/2G только по активной линии мобильных данных (`dataServiceIdentifier`); неактивная SIM с 2G не может переопределять активную LTE/5G, смена data-SIM обновляется сразу. | `В КОДЕ / НУЖЕН IPHONE/KX11`; Helper 70. |
| ANCS-012 | Natro остаётся авторитетным источником состояния HFP/A2DP/PBAP; Helper не сбрасывает глобальный Bluetooth, не удаляет системную пару и не подменяет идентичность телефона ради восстановления ANCS. | `ТРЕБОВАНИЕ`; проверять один видимый телефон и один владелец ANCS. |

## 9. Яндекс Музыка и управление медиа

| ID | Требование | Статус / критерий |
|---|---|---|
| MUSIC-001 | Задержка автозапуска из настройки должна соответствовать реальному времени. При выбранных 3 с воспроизведение не должно начинаться через 1–3 минуты. | `ОШИБКА`. |
| MUSIC-002 | Предварительный запуск Яндекс Музыки выполняется в фоне без открытия Activity/интерфейса приложения. | `ТРЕБОВАНИЕ`. |
| MUSIC-003 | Само приложение Яндекс Музыки не должно самопроизвольно открываться при загрузке магнитолы. | `ОШИБКА`. |
| MUSIC-004 | Безопасный prewarm не отправляет PLAY раньше пользовательской задержки; в момент запуска используется готовая media session. | `ТРЕБОВАНИЕ`. |
| MUSIC-005 | После старта воспроизведение не должно сразу переходить на паузу из-за конкурирующих media-button попыток. | `ОШИБКА`. |
| MUSIC-006 | Кнопки руля и управление медиа становятся доступны быстро и не ждут инициализацию HUD-карты или других тяжёлых подсистем. | `ОШИБКА В 2.6.8 НА KX11`: пользователь повторно подтвердил ощутимую задержку кнопок руля; исправление включается в 2.6.9. |
| MUSIC-007 | Реализацию сравнивать с предоставленными `mSaver 2.6/2.7 HOME`, используя только допустимое исследование поведения. | `ТРЕБОВАНИЕ`. |
| MUSIC-008 | Лог запуска содержит время boot/unlock, prewarm, обнаружения сервиса/session, первой команды PLAY и фактического состояния playback. | `ТРЕБОВАНИЕ`. |
| MUSIC-009 | Запуск выполняется через целевую MediaSession Яндекс Музыки; запрещены глобальные media-key команды, которые могут запустить или поставить на паузу другой активный проигрыватель. Поиск сессии ограничен тайм-аутом и не блокирует HOME. | `ОШИБКА` в прежних сборках; текущий путь нужен проверить по временному логу KX11. |
| MUSIC-010 | Каждое нажатие предыдущего/следующего трека и play/pause на руле передаётся активной MediaSession без накопления в общей очереди, ожидания запуска карты, HUD, панели приборов, ANCS или фонового поиска сессии. Повторные нажатия не должны исполняться пачкой с задержкой. | `В КОДЕ 2.6.9 / НУЖЕН KX11`: accessibility key-filter отправляет команду прямо в заранее выбранную точную MediaSession; поиск сессии и запись диагностик вынесены из пути нажатия. Если точной сессии нет, событие остаётся штатной системе. Требуется `GATE-017`. |

## 10. Круиз-контроль и ограничитель скорости KX11

| ID | Требование | Статус / критерий |
|---|---|---|
| DRIVE-001 | В перспективе включать/выключать лимитер и круиз-контроль со штатной ГУ и задавать выбранную скорость, сохраняя штатную индикацию приборки/HUD. | `ТРЕБОВАНИЕ / ИССЛЕДОВАНИЕ`. |
| DRIVE-002 | Любая запись в шину допускается только после доказанного безопасного протокола, проверки модели/года и обратимого стендового теста. | `ЗАБЛОКИРОВАНО` до подтверждения команд. |
| DRIVE-003 | Для диагностики доступен root ADB через компьютер; отсутствие app-level root не считать окончательным ограничением. | `ПОДТВЕРЖДЕНО`. |

## 11. Текущие обязательные проверки перед следующим APK

| ID | Проверка | Ожидаемый результат |
|---|---|---|
| GATE-001 | Java-тест единиц камеры | MapKit `60/3.6 → 60`, `25 → 90`; HUD `KPH/MPH/MPS` нормализуются. |
| GATE-002 | Сравнение Navigator dex | Новый `classes19.dex` содержит `CameraSpeedNormalizer`, `RouteTurnMapLayer` и настройки направления камеры. |
| GATE-003 | Boundary baseline | В Navigator меняются только разрешённые manifest/hooks/dex entries. |
| GATE-004 | Подпись обновления | Пакет и сертификат совпадают со вчерашним нашим Navigator APK. |
| GATE-005 | Проверка на KX11 | Светофоры/секунды, полосы сбоку, 60/90, единый знак, отсутствие дублей, направление, улицы на маршруте, стрелки поворотов, fixed zoom, верх окна и левая кнопка. |
| GATE-006 | Честный отчёт | Для каждого пункта отдельно указать: проверено автоматически, проверено на KX11 или ещё не проверено. |
| GATE-007 | Длительная стабильность Navigator | По 30 минут без маршрута и с маршрутом, три возврата из фона; нет завершения bridge, исчезновения окна или возврата на HOME. |
| GATE-008 | Тихое восстановление accessibility | После холодной загрузки и двух входов в настройки разрешение действует, success Toast accessibility не появляется. |
| GATE-009 | Холодный старт карты панели | После перезапуска Natro/Navigator карта появляется на панели без единого входа в настройки; повторная публикация surface после изменения размера также работает. |
| GATE-010 | Длительное отсутствие мерцания HUD/панели | HUD и панель по отдельности работают не менее 30 минут без пустых/чёрных кадров с маршрутом и без маршрута; изменение позиции, пробок и surface не пересоздаёт renderer. |
| GATE-011 | Видимые штатные стрелки поворотов | На реальном маршруте видны не менее трёх последовательных стрелок штатного стиля непосредственно на актуальной полилинии; положение соответствует манёврам, после завершения маршрута стрелки очищаются. |
| GATE-012 | Штатные названия улиц MapKit | На HUD и панели приборов подписи имеют штатный вид Яндекса, следуют изгибу дорог, масштабируются настройкой, не заменяются отдельными bitmap-карточками и остаются читаемыми поверх/около линии маршрута. Проверить с маршрутом и без него, в обычном и прозрачном режиме «Только дороги». |
| GATE-013 | Оформление стрелок поворотов | Для HUD и панели приборов отдельно проверить сохранение и применение двух разных цветов, толщины обводки, длины тела 10/50/100/250% и размера треугольного наконечника 10/50/100/250%. Изменение длины не меняет установленный размер наконечника, изменение наконечника не меняет длину; ширина тела и толщина обводки не меняются. При изменении толщины маршрутной линии ширина стрелки следует за ней. Сброс цвета возвращает штатный цвет Яндекса. |
| GATE-014 | Сектор направления и компактный/составной знак камеры | Для HUD и панели приборов отдельно проверить сохранение и применение разных длины, ширины основания, цвета и прозрачности сектора. При изменении ширины обе крайние точки основания расходятся поперёк направления, но вершина и продольная дальность не двигаются; при изменении длины ширина основания сохраняется. Обычная камера с известной скоростью — один компактный круг с числом без маленькой камеры и большого прозрачного поля. На одной физической камере «контроль полос + скорость» должен появиться один составной знак из штатной синей пластины и круга скорости, как на `IMG_8208.jpeg`, без мини-камеры на круге и без второго соседнего маркера. Для двунаправленной камеры видны оба сектора. Камера без скорости сохраняет один штатный знак камеры. На масштабах 50/75/100% число скорости, красная окантовка и штатная пластина остаются чёткими, а фактический размер и collision-рамка уменьшаются вместе с ползунком. |
| GATE-015 | Изменение размеров HUD-карты без растягивания | Последовательно изменить только ширину, только высоту и обе стороны HUD-карты. После каждого изменения геометрия дорог, подписи, курсор и маршрут перекомпонованы новым viewport, круги остаются кругами; старый кадр не растянут, пустого/чёрного кадра и мерцания нет. Повторить во время активного маршрута. |
| GATE-016 | Верх окна и двусторонняя кнопка режима | В оконном режиме сравнить верхний край карты, карточку манёвра и правые элементы с `IMG_8199.jpeg`/`IMG_8200.jpeg`, затем с `IMG_8215.jpeg`/`IMG_8216.jpeg`: повторного системного отступа нет в свободном режиме и при активном ведении ни сразу, ни после 30 с и нескольких перестроений Guidance. После каждого из 10 тапов одна кнопка появляется в одной и той же координате слева непосредственно под дорожным событием и Алиской/голосом; не попадает на карточку манёвра и не прыгает при смене режима. Нажать её окно→полный экран, снова тапнуть карту и той же кнопкой выполнить полный экран→окно. Справа сверху и поверх дороги дубликатов нет; после автоскрытия штатный блок и карта сохраняют работу. |
| GATE-017 | Задержка кнопок музыки на руле | После холодной загрузки, во время запуска HUD/панели и после полного прогрева выполнить по 10 нажатий previous/next/play-pause. В журнале для каждого нажатия сопоставить время входного ECARX-события, выбора MediaSession, отправки команды и изменения playback/metadata; команды не копятся, не меняют чужой проигрыватель и визуально выполняются без ощутимой задержки. |
| GATE-018 | Свободная раскладка и хвостики | На загруженном перекрёстке с табличкой полос, минимум двумя светофорами и камерой проверить HUD и панель при движении, повороте и двух уровнях zoom. Полосы и светофоры выбирают разные свободные стороны, остаются внутри viewport и не складываются в одну кучу; фиксированная камера остаётся центром на своей координате маршрута, а подвижные карточки обходят её. Каждый светофор расположен сбоку, его хвост указывает на собственную точку маршрута. Штатная табличка полос также имеет хвост, который при переносе меняет сторону и остаётся привязан к `RoutePosition`. Сверить с `IMG_8210.png`, `IMG_8206.jpeg`, `IMG_8214.jpeg` и `IMG_8219.jpeg`; отсутствие хвоста или смещение камеры с точки — провал. |
| GATE-019 | Повторная сверка приоритетов всех слоёв | Составить фактическую таблицу штатного порядка Navigator 30.3.0 и порядка независимых карт Natro после `MAP-030…033`; проверить автоматический режим и крайние значения ручного порядка отдельно на HUD и панели. На маршруте и без него ни один новый слой не перекрывает курсор, обязательный манёвр, маршрут впереди или Guidance-карточку вопреки штатной логике; подписи и стрелки не прячутся под дорогами, сектора камер остаются под своими знаками, а взаимное размещение полос/светофоров/камер не меняет правильный порядок их отрисовки. Любое расхождение сначала исправляется и повторно проходит статический контракт, затем подтверждается на KX11. |
| GATE-020 | Чёткость всех масштабируемых значков | Отдельно на HUD и панели приборов снять близкие кадры при 50/75/100% для камеры со скоростью и составной пластиной, светофора с секундами, таблички полос, дорожного события, конечной точки и курсора. Контуры, цифры, пиктограммы и хвосты остаются чёткими без ступенек, ореолов и размытия; физический размер и collision-рамка совпадают с выбранным масштабом. Проверка исходников подтверждает, что каждый размер получен из vector/drawable или штатного renderer, а не из уже уменьшенного bitmap. |
| GATE-021 | Регрессия известных падений | Статический/DEX-аудит отклоняет `NavigationLayerFactory`, `NavigationLayerSettings`, `createNavigationLayer`, `setUseLayerCamera`, `setUseLayerRoadEvents`, `setRoadEventVisibleOnRoute` и `GuidanceCamera` в добавленном Natro-модулем DEX Navigator; hook оконного режима остаётся в конце `onResumeFragments`, несвязанные конфигурации не повторяют type-2038 `setAttributes`, а отложенные poll/inset callbacks имеют локальную аварийную границу. Unit-тест заполняет весь лимит connector-listener и доказывает, что следующий подписчик не завершает процесс. Аппаратная часть не заменяет `GATE-007`: по 30 минут без маршрута и с маршрутом плюс три возврата из фона. |
| GATE-022 | Аппаратная регрессия исправлений 03.09.2026 | На одиночных камерах, камерах без известной скорости и составных знаках центр совпадает с `worldToScreen` исходной точки и вершиной сектора, а на маршрутных камерах остаётся на линии при движении/zoom. Переключить свободный режим ↔ активное ведение не менее 5 раз: дополнительный верхний отступ Guidance не возвращается. Выполнить 10 тапов карты в обоих состояниях: координата кнопки режима не меняется более чем на 1 px и всегда остаётся третьей слева под штатной парой. Сверить с `IMG_8214.jpeg`, `IMG_8219.jpeg`, `IMG_8215.jpeg`, `IMG_8216.jpeg`, `IMG_8220.jpeg`–`IMG_8223.jpeg`. |
| GATE-023 | Внешний inset активного Guidance | На физическом KX11 в оконном режиме запустить маршрут и сверить с провальным `IMG_8227.jpeg`: верх карты, карточка манёвра и группа текущей/разрешённой скорости начинаются у одной верхней области окна, при этом у двух верхних виджетов остаётся только штатный отступ 12 dp, а не полный глобальный status-bar inset. Проверить сразу после входа, через 30 с, после пяти переключений свободный режим ↔ ведение, перестроения маршрута и возврата из фона. Убедиться, что поднялись все верхние Guidance-элементы вместе, нижняя ETA-панель не сместилась, полноэкранный режим не изменился и 30-минутный прогон `GATE-007` проходит без падения. |
| GATE-024 | Штатный левый контейнер кнопки режима | В свободном режиме и при активном Guidance по 10 раз тапнуть карту и сверить с `IMG_8221.jpeg`: существует ровно одна кнопка режима, она является третьим визуальным элементом того же вертикального контейнера сразу после штатной обёртки дорожного события и кнопки Алисы/голоса и перемещается/появляется/исчезает вместе с ним. Форма, фон, размер, padding, pressed-state, alpha и scale визуально совпадают с соседними штатными кнопками; отдельного круглого чёрного overlay, скачка координаты или собственного запаздывающего fade нет. Выполнить окно→полный экран→окно, пять смен свободный режим ↔ Guidance и перестроение маршрута; после каждой замены кнопка кликабельна, старого экземпляра в отсоединённом дереве и дубля поверх карты нет. |

Автоматический прогон выпуска 01.09.2026: `GATE-001…GATE-004` пройдены для CI run 167
и подписанной пары из дерева `7508e7bb81776321f0a4ac997cfaee6c4e61d90f`. `GATE-005` не
пройден без физического KX11 и остаётся обязательной пользовательской приёмкой. `GATE-006`
выполняется в отчёте выдачи: автоматические и аппаратные результаты не смешиваются.

Автоматический прогон исправляющего выпуска 2.6.1: `GATE-001…GATE-004` пройдены в CI run 173
для исходного commit `4abcf578aebf5e2c049ba0acd10fbe1bd794c00d` и дерева
`b0845312145e428fc69ef654fcd2329afb14dd5d`. Подписаны Natro 2.6.1 и Navigator 30.3.0;
сертификат обоих APK имеет SHA-256
`6e9855aedc008bbdd8a7fbf3f490be07f964b7ac658a837a1592647a08365c75`. `GATE-005`,
`GATE-007` и `GATE-008` остаются незакрытыми до проверки на физическом KX11.

Автоматический прогон исправляющего выпуска 2.6.2: CI run 181 полностью прошёл для commit
`36218be1d38c2f0c248ef4a21ba166af6884df48` и дерева
`78c0cf8da42465df4dd825f6c5382cfa8dc264ef`; `GATE-001…GATE-004` пройдены. Подписаны
Natro 2.6.2 (`59cb23f0593d30ba83e317f95411b9db4ade8914ebdd33210ed54266f7369167`),
Navigator 30.3.0 (`cd5831406335dc94e524f1ecfca39fa32a4994adc83439455511cd82812e40bf`)
и неизменённый по функциональности HUD Speed Bridge 76.0-L13
(`3468e987b2f25d569658fd39801bf0a9d1b69ba8d0ef99a614b3480e0b22bf20`). Все три APK
подписаны сертификатом `6e9855aedc008bbdd8a7fbf3f490be07f964b7ac658a837a1592647a08365c75`.
`GATE-005`, `GATE-007`, `GATE-008` и `GATE-009` остаются до проверки на физическом KX11.

Автоматический прогон исправляющего выпуска 2.6.4: CI run 204 полностью прошёл для commit
`f4249ed6ea0e95f8db34f0023b2230e871b59446` и дерева
`63bff38826e076951272e0bde7f0bb0bdff2e30f`. Из этого дерева собраны и стабильным сертификатом
подписаны Natro 2.6.4 (`ae76c55128ce971ddbb744caff012e8f5af567a4c0b4b43a5383441cca6c2735`),
Navigator 30.3.0 (`6f2fbc7853eda1ab090be0cd82f0a3940bacf8b12bd53b416c6d2c7efbd3fec8`)
и функционально неизменённый HUD Speed Bridge 76.0-L13
(`3468e987b2f25d569658fd39801bf0a9d1b69ba8d0ef99a614b3480e0b22bf20`). Статические gates
пройдены; `GATE-010` и `GATE-011` остаются открытыми до длительной проверки на физическом KX11.

Автоматический прогон выпуска 2.6.5: CI run 208 полностью прошёл для commit
`21956ddf1c5b98ad85c1cb901972a5746013e183` и дерева
`91b066ce75d4b1e7905c2b02c7852afa22569071`. Из этого дерева стабильным сертификатом подписаны
Natro 2.6.5 (`08c61c220a7e114d8e2992c9e4632570aef1b1c4df74ceb012a65247003a787f`) и
Navigator 30.3.0 (`e6bb944a18d9f4d6659e61e3be50a863537f1a7c57b4cb4860070c5b6c67dcb5`).
`GATE-001…GATE-004` и статический KX11 compatibility gate пройдены; `GATE-010`, `GATE-011` и
новый визуальный `GATE-012` остаются открытыми до проверки на физическом KX11.

Автоматический прогон выпуска 2.6.6: CI run 212 полностью прошёл для commit
`5277b4b852f7f2b76b82fd4f68ace65b57e7ba4b` и дерева
`588cdbf268c729ead231766d43543fb666063018`. Из этого дерева стабильным сертификатом подписаны
Natro 2.6.6 (`6640d70f4aedea10324dcb003816b3f11f7adbb51b4ba43432b78ee6f5e6bf87`) и
Navigator 30.3.0 (`11ca3bd08dc80b7fa53cb646a6d221396a777e5847314a1a5c7c346192251bd7`).
Автоматические release-gates, общий сертификат и установка поверх подписанной пары 2.6.5
проверены; визуальный `GATE-013` остаётся открытым до проверки на физическом KX11.

Автоматический прогон выпуска 2.6.7: CI run 216 полностью прошёл для commit
`abed6c2353ad17100a811d7a51decca41c664e24` и дерева
`615134b6033fccd6eee9a04d05c5051122d61fb6`. Из этого дерева стабильным сертификатом подписаны
Natro 2.6.7 (`f4c7aa1bdf16a76ebe214f32c752e694da75f06ba89a42cc7e874f836c28a8e5`),
Navigator 30.3.0 (`3517d06b33bba74d9c9a9e108fcbf2b10c6c5b9245342c646535b7b97f0733cd`) и
функционально неизменённый HUD Speed Bridge 76.0-L13
(`3468e987b2f25d569658fd39801bf0a9d1b69ba8d0ef99a614b3480e0b22bf20`). Автоматические
release-gates, общий сертификат и обновление Natro поверх 2.6.6 проверены; визуальные
`GATE-013…GATE-015` остаются открытыми до проверки на физическом KX11.

## 12. Журнал изменений реестра

- **01.09.2026:** создан единый реестр; восстановлены требования за 25.08–01.09; зафиксирован
  обязательный разрыв между «есть в коде» и «подтверждено на KX11»; добавлен release gate для
  преобразования скорости камер и правило постоянного обновления файла.
- **01.09.2026:** CI run 167 полностью прошёл; подтверждены автоматические проверки нормализации
  скорости, состава Navigator DEX, границ baseline и общей подписи. Визуальная и аппаратная
  проверка перечисленных функций на KX11 намеренно оставлена незакрытой.
- **01.09.2026:** повторный аудит истории и документов выпусков выявил, что часть точных условий
  была спрятана в общих формулировках. Добавлены отдельные пункты для DIM-панели, пяти панелей
  11–15, второго курсора/скачков карты, фоновой работы карт, кнопки окна, конечной точки,
  свежести маршрута, порядка и размеров слоёв, обратимой линии маршрута, точных карточек,
  сигналов 360/парктроника, активной data-SIM, границ ответственности Bluetooth и MediaSession.
- **01.09.2026:** по журналу `status-widget-debug(20260901-144416).txt` добавлен P0-дефект
  `NAV-011`: три последовательных завершения Navigator через 13/10/9 секунд. Повторно запрещён
  automotive `NavigationLayer` на независимых картах; аппаратный gate увеличен до 30 минут в
  каждом режиме. По `IMG_8179.jpeg` добавлен `NATRO-014`: accessibility восстанавливается тихо,
  а полный и сокращённый форматы компонента считаются одной записью.
- **01.09.2026:** для исправлений `NAV-011` и `NATRO-014` собрана и подписана пара 2.6.1 из
  дерева `b0845312145e428fc69ef654fcd2329afb14dd5d`; CI run 173 пройден. Итог не помечен как
  аппаратно подтверждённый: обязательны `GATE-007` (по 30 минут в двух режимах) и `GATE-008`
  (холодная загрузка и два входа в настройки без повторного Toast).
- **01.09.2026:** аппаратная проверка 2.6.1 по `IMG_8187.jpeg`, `IMG_8185.jpeg`,
  `IMG_433214B6-4907-4D12-93FB-E8D24594356F.jpeg`, `IMG_8184.jpeg` и `IMG_8183.jpeg`
  повторно открыла `WIN-005`, `WIN-006`, `WIN-009`, `MAP-008` и `MAP-009`. Добавлен строгий
  `MAP-023`: самодельные знаки поворотов запрещены, нужен штатный стиль стрелок полилинии MapKit.
  По отдельному сообщению добавлены `CLUSTER-017` и `GATE-009`: карта панели обязана запускаться
  на холодном старте без входа в настройки.
- **01.09.2026:** в 2.6.2 самодельные знаки поворотов заменены на штатные стрелки MapKit с точной
  `PolylinePosition`, ось названий улиц исправлена, показ кнопки перенесён в глобальный touch hook,
  top inset повторно применяется к позднему дереву контролов, а приборная панель сама запускает
  Binder endpoint до первой surface. CI run 181 и статические release-gates пройдены; результат
  честно оставлен `В КОДЕ / НУЖЕН KX11` до аппаратной проверки.
- **01.09.2026:** по `IMG_8188.mp4` и `status-action-session(6).json` добавлены `SHADE-001…007`:
  отдельная редактируемая шторка Natro должна заменить окно `ecarx.notificationcenterui` без
  одновременного показа, не отключая весь SystemUI; отключение обязательно обратимо и требует
  точного component-level исследования и recovery через подтверждённый root ADB.
- **01.09.2026:** пользователь дал прямую команду начать реализацию `SHADE-001…007`; статусы
  переведены в `В РАБОТЕ`. Первоначальный каталог строится из уже поддержанных Natro элементов,
  а отключение ECARX остаётся заблокированным до точного определения безопасного компонента.
- **01.09.2026:** добавлена первая реализация системной шторки: отдельный main-display overlay,
  жест/анимация/Back, часы, дата, медиа, громкость, яркость, независимые действия, живой
  move/resize/style-редактор, boot-autostart и safety-сокрытие при внешних автомобильных окнах.
  Android CI компилирует обе конфигурации и завершает все 1760 unit-тестов; аппаратные пункты не
  переводятся в `ГОТОВО` до проверки KX11. Точный компонент `ecarx.notificationcenterui` всё ещё
  должен быть получен read-only аудитом до любого отключения штатной панели.
- **02.09.2026:** аппаратная проверка 2.6.3 открыла критический дефект `SHADE-008`: стандартный
  жест не открывает панель, а после попытки открытия невидимое полноэкранное overlay-окно
  перехватывает все касания Natro и не позволяет снять лог. `SHADE-001` и `SHADE-005` возвращены
  в статус `ОШИБКА`; до исправления пользователю предписано отключить шторку после перезагрузки.
- **02.09.2026:** пользователь сообщил о мерцании всей карты одновременно на HUD и панели
  приборов и подтвердил отсутствие видимых стрелок поворотов на полилинии. Добавлены `NAV-012`,
  `GATE-010`, `GATE-011`; `MAP-009` и `MAP-023` возвращены в статус аппаратной ошибки до
  доказанного видимого результата на KX11.
- **02.09.2026:** аудит нашёл две конкретные причины: обе независимые карты заменяли геометрию
  маршрута до 10 раз/с и могли пересоздавать `OffscreenMapWindow` при resize/временной
  невидимости, а стрелки копировали документированный штатный флаг `enabled=false`. Геометрия
  маршрута теперь неизменна, прогресс скрывается обратимым `Subpolyline`, lease surface не
  отзывается при кратком скрытии и не публикуется заново при resize; повороты добавляются
  `RouteHelper.addManeuvers` и получают явно включённый штатный стиль. Просроченный маршрут также
  отбрасывается перед повторным включением слоя, поэтому старые стрелки не могут вернуться после
  паузы. Автоматические контракты добавлены, но `GATE-010` и `GATE-011` остаются открытыми до
  проверки на KX11.
- **02.09.2026:** исправлен ошибочный вывод об отсутствии подписи: в постоянном хранилище найдены
  `status-widget-ha-update.jks` и приватный резервный архив с паролем. Сертификат и alias сверены,
  выпуск 2.6.4 подписан тем же ключом; добавлено постоянное правило `REL-015`, запрещающее
  повторно запрашивать комплект у пользователя без проверки обеих сохранённых копий.
- **02.09.2026:** пользователь заменил самодельные подписи улиц на требование брать их штатный
  вид непосредственно из дорожного слоя Яндекса по эталону `IMG_8189.jpeg`. `MAP-008` отмечен
  заменённым, добавлены `MAP-024` и `GATE-012`: bitmap/Canvas-надписи запрещены, прежний режим
  «только на маршруте» мигрирует на нативные дорожные подписи MapKit.
- **02.09.2026:** реализация `MAP-024` собрана в Natro 2.6.5 и Navigator 30.3.0 из commit
  `21956ddf1c5b98ad85c1cb901972a5746013e183`; CI run 208 и все статические release gates
  пройдены. Пункт переведён в `В КОДЕ / НУЖЕН KX11`, а визуальный `GATE-012` намеренно не
  закрыт до проверки штатного вида и изгиба подписей на HUD и панели приборов автомобиля.
- **02.09.2026:** добавлено `MAP-025`: пользователь потребовал отдельные настройки цвета,
  цвета и толщины обводки и длины штатных стрелок поворотов. Уточнено, что длина пропорционально
  меняет наконечник, но не ширину тела; ширина обязана следовать толщине маршрутной линии.
  Добавлен аппаратный критерий `GATE-013`.
- **02.09.2026:** пользователь дал прямое разрешение выпустить подписанную совместимую пару с
  реализацией `MAP-025`. Для следующего выпуска зафиксированы Natro `2.6.6` / `208021299` и
  неизменная идентичность Navigator `30.3.0` / `739564630`; HUD Speed функционально не меняется.
- **02.09.2026:** CI run 212 и окончательная проверка подписанной пары 2.6.6 прошли. Natro и
  Navigator имеют один сертификат `6e9855ae…65c75`, Natro увеличил `versionCode` ровно на один,
  а Navigator сохранил пакет и версию; исходное дерево и контрольные суммы зафиксированы выше.
- **02.09.2026:** по `IMG_8192.jpeg` и `IMG_8196.jpeg` добавлены `MAP-026…028`: общий размер
  сектора камеры заменён независимыми длиной и шириной только дальнего основания, добавлен цвет,
  знак камеры/скорости должен стать одним компактным штатноподобным кругом, а минимальная длина
  стрелок поворота снижена с 50% до 10%. Старое значение сектора мигрирует в обе новые настройки.
- **02.09.2026:** пользователь сообщил, что изменение размеров карты HUD растягивает старый кадр.
  Добавлены `NAV-013` и `GATE-015`; установлено, что прежний resize менял лишь метаданные уже
  созданного viewport. Теперь новая геометрия получает новую generation и отдельный
  `OffscreenMapWindow`, а последний готовый буфер остаётся на экране во время короткой пересборки.
  Это уточнение заменяет более раннее предположение, что surface нельзя повторно публиковать при
  resize: повторная публикация необходима, но выполняется без detach-only интервала.
- **02.09.2026:** пользователь дал прямую команду немедленно собрать APK. Для выпуска зафиксированы
  Natro `2.6.7` / `208021300` и совместимый Navigator `30.3.0` / `739564630`; оба должны быть
  подписаны стабильным сертификатом проекта. HUD Speed Bridge функционально не меняется.
- **02.09.2026:** первый CI run 214 был остановлен до выпуска из-за необъявленной локальной
  переменной только в новом source-contract тесте; ни один APK из него не выдавался. Тест исправлен
  отдельным опубликованным commit, после чего CI run 216 прошёл Android unit-тесты, сборку всех
  трёх DEX, baseline boundary и упаковку кандидата. Финальные Natro, Navigator и неизменённый HUD
  Speed Bridge подписаны сертификатом `6e9855ae…65c75`; версии, схемы подписи, `zipalign`,
  совместимость обновления 2.6.6→2.6.7 и контрольные суммы проверены.
- **02.09.2026:** пользователь уточнил визуальное требование после `IMG_8197.jpeg` и
  `IMG_8198.jpeg`: на знаке камеры с известной скоростью не должно быть даже маленького значка
  камеры. `MAP-027` заменён на `MAP-029`; отдельный значок сохраняется только при отсутствии
  числового ограничения, а `GATE-014` обновлён.
- **02.09.2026:** фото `IMG_8199.jpeg` и `IMG_8200.jpeg` повторно подтвердили три дефекта
  Navigator: большой верхний резерв в оконном режиме, кнопка режима справа сверху вместо штатного
  левого блока и отсутствие обратной кнопки в полном экране. `WIN-005`, `WIN-006`, `WIN-009`
  повторно открыты; добавлены строгие `WIN-010…WIN-012` и аппаратный сценарий `GATE-016`.
- **02.09.2026:** исходное дерево 2.6.8 `eb1b09892c4fb906cc0d6c2265bf5989c8a724e3`
  опубликовано commit `07e1e07aff9166e06fd37fcd010f00a9fd5d6b73`. CI run 218 успешно
  выполнил Android unit-тесты, сборку Natro, трёх изолированных DEX и baseline
  boundary. Natro `2.6.8` (`208021301`) и Navigator `30.3.0` (`739564630`) подписаны
  стабильным сертификатом `6e9855ae…65c75`; SHA-256 APK: Natro
  `debdc86a6823beae55ba66bdfab19a36b5fc313005a3d7d776c695d28c1e5ac7`, Navigator
  `3298bed59d644fc12903c6c64c409ab8b92657ffa425ff338a5ea23ca0837244`. `GATE-014` и
  `GATE-016` остаются открытыми до физической приёмки на KX11.
- **02.09.2026:** аппаратная проверка 2.6.8 провалила обе части `GATE-016`: в оконном режиме
  тап по карте вообще не показывает кнопку режима, а верхние элементы остались слишком низко.
  `WIN-005`, `WIN-006`, `WIN-009`, `WIN-010` и `WIN-012` возвращены в статус ошибки.
  Техническое требование внедрять кнопку дочерним элементом в изменяемый штатный `LinearLayout`
  заменено `WIN-013`: сохраняется точное визуальное место в левом блоке, но владельцем остаётся
  стабильный touch-прозрачный слой Natro. Добавлено `WIN-014`: для окна нужно переопределять
  listener каждого поздно созданного `PaddingtonView`, потому что штатная реализация повторно
  читает глобальный root inset и отменяет разовую коррекцию padding.
- **02.09.2026:** пользователь сообщил о повторной ощутимой задержке кнопок переключения музыки
  на руле в 2.6.8. `MUSIC-006` повторно подтверждён как аппаратная ошибка; добавлены строгие
  `MUSIC-010` и `GATE-017`, запрещающие общей очереди и тяжёлым подсистемам задерживать передачу
  каждого события в активную MediaSession.
- **02.09.2026:** пользователь повторно подтвердил провал `CLUSTER-017/GATE-009` в 2.6.8:
  включённая карта панели приборов не стартует после холодной загрузки и появляется немедленно
  только после открытия настроек Natro. В 2.6.9 boot-путь должен вызывать тот же запуск
  endpoint/lease, который сейчас неявно активирует экран настроек.
- **02.09.2026:** в дереве 2.6.9 реализован единый пакет исправлений по повторной аппаратной
  обратной связи 2.6.8. Кнопка режима остаётся в стабильном слое Natro и переустанавливается из
  глобального touch-hook; вложенные `PaddingtonView` больше не могут поздно вернуть верхний inset.
  Для руля добавлен прямой cached-MediaSession путь без синхронного поиска и общей очереди.
  Холодный старт приборной карты защищён с двух сторон: повтором публикации живой `TextureView`
  поверхности и немедленным восстановлением Binder/HELLO после внешнего DIM-запуска. Автоматические
  контракты проверяют реализацию; `GATE-009`, `GATE-016` и `GATE-017` остаются открыты до KX11.
- **02.09.2026:** по `IMG_8208.jpeg`/`IMG_8209.jpeg` пользователь подтвердил потерю второго типа
  у составной камеры, потребовал отдельный размер наконечника стрелок, а по
  `IMG_8210.png`/`IMG_8207.jpeg`/`IMG_8206.jpeg` — общую раскладку карточек вокруг маршрута и
  штатные хвостики. Добавлены `MAP-030…032`, обновлены `MAP-006`, `MAP-025`, `MAP-029` и
  `GATE-013/014`, создан `GATE-018`. Записи камер теперь объединяют типы и оба направления, а
  светофоры получают восемь штатных `LegPlacement` вместо принудительного `NONE`.
- **02.09.2026:** отдельным уточнением пользователь потребовал хвостик и у таблички движения по
  полосам. Добавлен `MAP-033`: слой больше не вырезает один `View` вручную, а использует полную
  штатную `BalloonTexture` Яндекса с `BalloonAnchor` и точным `imageAnchor`; проверка включена в
  `GATE-018` и остаётся открытой до установки 2.6.9 на KX11.
- **02.09.2026:** перед финальной сборкой пользователь потребовал заново сверить приоритеты всего
  набора слоёв со штатным Navigator после добавления новых слоёв. Добавлены `MAP-034` и
  `GATE-019`; финальная сборка 2.6.9 заблокирована до аудита исходного порядка, текущих
  `zIndex`/collection-приоритетов, безопасного ручного диапазона и повторной проверки на KX11.
- **02.09.2026:** статический аудит `MAP-034` выполнен по исходному APK Navigator 30.3.0
  `SHA-256 663018fb66074e001eed7caba8e33bee1bcf78f6798bc84949d253dcb348f27f` и текущему
  дереву `5fa67eacc6f3`. Выявлены блокирующие расхождения: custom-ID не включены в штатную
  feature-type-aware схему, ручной алгоритм переносит только первый sublayer каждого ID и ставит
  выбранные слои выше всех непереставленных слоёв, камера смешивает ground-сектор и placemark-знак,
  штатные стрелки являются частью маршрутной полилинии, поэтому отдельный priority-slider сейчас
  ничего не меняет. Исправление в рамках аудита не вносилось; `GATE-019` остаётся открытым.
- **02.09.2026:** после разрешения пользователя исправление `MAP-034` внесено для обеих
  независимых карт. Камерный root разделён на ground-сектор (`IGNORE`) и placemark-знак (`EQUAL`),
  все sublayer находятся с явным feature type, а пользовательские ID ставятся относительно
  штатных якорей без перемещения слоёв Яндекса в конец. Стрелки MapKit остаются внутри маршрутной
  полилинии и наследуют её приоритет; ложный отдельный ползунок удалён, точный пресет 2.6.8
  мигрирует на порядок `30/40/50/60/70/80/90`. Статический контракт проходит локально;
  фактическая композиция HUD/панели остаётся на `GATE-019` для KX11.
- **02.09.2026:** по замечанию пользователя проверено качество значков при уменьшении. Штатные
  пластины камер исходно векторные, но MapKit получает уже bitmap; число скорости раньше
  растрировалось прямо в минимальный экранный диаметр 26–40 px. Добавлен `MAP-035`: камера теперь
  создаётся минимум в 80 px и уменьшается `IconStyle.scale` при неизменных видимом размере и
  collision-рамке. Чёткость 50/75/100% добавлена в аппаратный `GATE-014`.
- **02.09.2026:** исходное дерево `bbe3ae6214ed610f2215e528c31672e7869e8902` опубликовано как
  commit `bc6500506fdfc0cd086ff75c17ca51471d369023`; push CI run 228 прошёл полностью. Подписаны
  Natro 2.6.9 (`208021302`, SHA-256 `fa71ecf9…d2527`) и Navigator 30.3.0
  (SHA-256 `9ec2b265…8c347`) стабильным сертификатом `6e9855ae…65c75`. Статическая совместимость
  KX11 пройдена; аппаратные `GATE-007`, `GATE-009…019`, включая верхние элементы оконного
  Navigator, чёткость камеры и фактический порядок слоёв, не закрыты без установки на ГУ.
- **02.09.2026:** пользователь распространил требование чёткого уменьшения камеры на все значки
  карты. Добавлены `MAP-036` и `GATE-020`: каждый размер камеры, светофора/секунд, полос,
  дорожного события, конечной точки и курсора должен формироваться из исходного vector/drawable
  или штатного рендера, а не из уже уменьшенного bitmap. Требование пока не помечено выполненным.
- **02.09.2026:** повторно проверены известные причины падений Navigator и Natro. Automotive
  `NavigationLayer`, дополнительная `GuidanceCamera`, ранний type 2038 и повторная оконная
  транзакция в исполняемый путь не вернулись. Добавлены `NAV-014`, `NATRO-015`, `GATE-021` и
  отдельный отчёт аудита; DEX-запрет расширен, отложенные poll/inset callbacks изолированы от
  главного looper, а полный лимит connector-listener больше не бросает исключение в процесс.
  Аппаратная стабильность остаётся открытой до `GATE-007` на KX11.
- **02.09.2026:** исходный commit `078591241a580067291c0eff33594212a1ab1dc0`, дерево
  `16c01838204a3371771186bf9b21b2b19d67c93f`, прошёл CI run 231: Android unit-тесты, сборку
  Natro, изолированный Navigator `classes19.dex`, HUD Speed bridge и baseline-verifier self-tests.
  Подписанная финальная APK-пара из этого пост-релизного аудита не выпускалась; `GATE-007`,
  `GATE-020` и аппаратная часть `GATE-021` остаются открытыми до KX11.
- **03.09.2026:** аппаратная проверка по `IMG_8214.jpeg`, `IMG_8219.jpeg`, `IMG_8215.jpeg`,
  `IMG_8216.jpeg`, `IMG_8220.jpeg`–`IMG_8223.jpeg` выявила три регрессии: все типы камер
  смещались с маршрута восьмипозиционным anchor, отдельный Guidance `PaddingtonView` возвращал
  верхний inset только при активном ведении, а кнопка оконного режима выбирала разные позиции по
  глобальным Alice/ghost-контролам и моменту layout. Добавлены `MAP-037`, `WIN-015`, `WIN-016` и
  `GATE-022`; `MAP-032`, `WIN-013`, `WIN-014`, `GATE-016` и `GATE-018` уточнены без удаления
  прежней истории. В коде камера закреплена центральным anchor и первой резервирует footprint,
  Guidance-root ищется заново и получает отдельный inset-guard, а третий левый слот блокируется
  до показа только по точной штатной паре. Исходный commit
  `836da17ab359a8d2da3d1d646c84586b31ca541b` (дерево
  `e64dfa5e9597dd0777d5b06ee6150deef06efba5`) прошёл CI run 232: Android unit-тесты, сборку
  Natro, изолированный Navigator `classes19.dex`, HUD Speed bridge, baseline-verifier self-tests
  и упаковку неподписанной пары. Задание подписи пропущено; итог остаётся `НУЖЕН KX11`.
- **03.09.2026:** пользователь дал прямое разрешение на подписанную сборку после исправлений
  `MAP-037`, `WIN-015` и `WIN-016`. Промежуточная подпись с прежней идентичностью Natro 2.6.9
  остановлена до выдачи: после уже выпущенной 2.6.9 в Natro добавлен production safety-fix
  `NATRO-015`, поэтому повторное использование `208021302` нарушило бы `REL-003`. Для финального
  выпуска зафиксированы Natro `2.7.0` / `208021303` и неизменная идентичность Navigator
  `30.3.0` / `739564630`; подписывать разрешено только заново проверенный опубликованный source
  commit, а аппаратные `GATE-007`, `GATE-009…022` остаются открытыми до KX11.
- **03.09.2026:** подписанная пара собрана только из опубликованного commit
  `6395f98e38f5042cdbd747823b651c14dc37e286` (дерево
  `e456c7db089086016d73b3474243d952760d4387`) после успешного push CI run 234
  (`33717713006`); SHA-256 архива кандидата `a4d864cd035d0f678cbe83ea779d83b487e7367569cdd5c50424ff9ef74ed4d5`.
  Итоговые APK: Natro 2.7.0 / `208021303`, SHA-256
  `a8172394dc7cc795dc4ac4eff961e90ebc99384ea80f29d4df8dbc0fe8229d88`; Navigator 30.3.0 /
  `739564630`, SHA-256 `7c598f4e60723ac789aed74b791a62cc7140a6726fb3262a7fc0a8d85f50a22a`.
  Оба подписаны стабильным сертификатом
  `6e9855aedc008bbdd8a7fbf3f490be07f964b7ac658a837a1592647a08365c75`; Natro проверен как
  допустимое обновление поверх выпущенной 2.6.9 (`208021302`, SHA-256
  `fa71ecf9a3a31ebf6bb477afaf7ed7a548885751e0b0dc70bc9f39de2e0d2527`): package и сертификат
  совпадают, новый `versionCode` строго больше. Статический KX11-verifier и контрольные суммы
  пройдены; у Navigator относительно точного baseline изменены только `AndroidManifest.xml`,
  `classes4.dex`, `classes12.dex`, добавлен `classes19.dex`. Аппаратные `GATE-007` и
  `GATE-009…022` остаются открытыми до установки и проверки на KX11.
- **03.09.2026:** аппаратная проверка подписанного Navigator 30.3.0 из пары Natro 2.7.0 по
  `IMG_8227.jpeg` повторно отклонила `WIN-015`: карта начинается у верхней границы окна, но
  карточка манёвра и вся группа скорости сохраняют одинаковый большой отступ. Проверка baseline
  30.3.0 показала, что оба элемента являются прямыми детьми одного безымянного
  `CarGuidanceController` со штатным верхним margin 12 dp; следовательно, смещён не отдельный
  составной знак, а внешний экран активного ведения. Добавлены `WIN-017` и `GATE-023`. В коде
  оконного режима отдельно контролируются общий `activity_container_controller`, динамический
  визуальный корень с `contextmaneuverview` + `speed_group` и все поздние `PaddingtonView` внутри
  Conductor-дерева. Отрицательный `translationY` не используется, полноэкранное состояние
  сохраняется для восстановления; новая APK без отдельного разрешения пользователя не собиралась.
- **03.09.2026:** пользователь потребовал до следующих точечных правок полностью разобрать
  исполняемую UI-цепочку карты Navigator 30.3.0 и отдельно добраться до настоящего контейнера
  левых кнопок. Добавлен `NAV-015`. По `IMG_8221.jpeg` существующая координатная overlay-кнопка
  отклонена не только по положению, но и по подложке и поведению появления. `WIN-013` заменён,
  `WIN-016` уточнён новым `WIN-018`: кнопка должна стать третьим ребёнком точного штатного
  контейнера, использовать его прямоугольный стиль и наследовать его полный цикл видимости и
  анимации без собственного таймера. Добавлен аппаратный `GATE-024`; APK не собиралась.
- **03.09.2026:** статический аудит `NAV-015` завершён и сохранён в
  `docs/NAVIGATOR_30_3_UI_CONTAINER_AUDIT_RU.md`. Он выявил различие landscape Guidance:
  `guidance_add_road_event` вложен в `MapControlsFrameLayoutRect`, поэтому валидируется общий
  вертикальный rail и предыдущая штатная обёртка, а не одинаковый непосредственный parent двух
  ID. Реализован `WIN-018`: координатный overlay, блокировка экранной позиции и собственный
  5-секундный fade удалены; один элемент создаётся в штатной прямоугольной оболочке, получает
  векторную иконку конечного размера 48 dp и вставляется третьим после voice-control в
  free-drive/Guidance. Во время Conductor transition он хранится только скрытым в слое Natro.
  Неактуальные ползунки размера/opacity убраны из двух экранов настроек, wire-поля сохранены для
  совместимости. Пройдено 20 статических тестов (1 platform-skip), baseline boundary self-check
  прошёл; запрещённые аварийные NavigationLayer/GuidanceCamera пути не возвращены. APK не
  собиралась и в git remote ничего не отправлялось до аппаратного `GATE-023/024`.
- **03.09.2026:** пользователь дал прямую команду собрать подписанный APK после `WIN-017/018`.
  Поскольку Natro 2.7.0 / `208021303` уже выпущена, новая пара получает Natro 2.7.1 /
  `208021304`; Navigator сохраняет 30.3.0 / `739564630`. Выдача разрешена только после публикации
  точного source commit, успешной CI-сборки этого commit, локальной подписи стабильным сертификатом
  `6e9855ae…65c75` и всех статических release-gates. `GATE-023/024` остаются аппаратными и не могут
  считаться закрытыми до проверки пользователем на KX11.
- **03.09.2026:** первый CI-прогон 236 для 2.7.1 корректно остановил выпуск на двух устаревших
  Java contract-assertions: они всё ещё требовали удалённый собственный opacity-slider и прежнюю
  текстовую форму условия видимости кнопки. Исполняемый код скомпилировался; тесты приведены к
  `WIN-018` и теперь явно запрещают собственные size/opacity sliders в обоих экранах настроек.
  Кандидат из проваленного run не подписывается и не выдаётся.
