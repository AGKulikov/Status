# Единый реестр требований Natro

Это основной и обязательный источник требований для Natro, iPhone Helper, модификации Яндекс
Навигатора, HUD Speed Bridge, HUD и панели приборов. Перед любой разработкой или выпуском нужно
прочитать этот файл целиком. Если сообщение пользователя меняет или добавляет требование, запись
добавляется сюда **до** реализации или одновременно с ней.

- Репозиторий: `AGKulikov/Status`.
- Рабочая ветка навигации: `feature/navigation-hud-v2`.
- Первично восстановленный период: **25.08.2026–01.09.2026**.
- Последнее обновление: **05.09.2026**; полный аудит актуальности: **04.09.2026**.
- Текущий выпуск: **Natro 2.8.0 / 208021313** и совместимый Navigator 30.3.0, собраны и подписаны 05.09.2026; [состав, матрица и проверки](docs/releases/2.8.0/README_RU.md). Аппаратная приёмка открыта.
- Предыдущая пара **2.7.9 / 208021312**: [навигация, топливо, громкость, медиа и мягкие края](docs/releases/2.7.9/README_RU.md).
- Изменения пары **2.7.8 / 208021311**: [запуск приборки, ANCS и избранное](docs/STARTUP_RECOVERY_2026_09_05.md); её изменения сохранены, прежние аппаратные критерии не закрыты автоматически.
- Сверка после новых сообщений: [дополнение 05.09.2026](docs/REQUIREMENTS_AUDIT_2026_09_05.md); 230 требований и 58 критериев, прежние ID сохранены.
- Аудит реестра: [docs/REQUIREMENTS_AUDIT_2026_09_04.md](docs/REQUIREMENTS_AUDIT_2026_09_04.md).
- Владелец требований и единственный источник окончательной приёмки: пользователь.

В первичный реестр включены доступная история разговоров, исправления пользователя, переданные
скриншоты/видео, опубликованные исходники и история выпусков за указанный период. Если точный
визуальный эталон был передан только изображением или видео, словесное требование ниже не заменяет
эталон: при проверке необходимо снова сопоставить результат с исходным вложением.

Техническая база автомобиля: [Geely KX11 / ECARX — цифровая инструкция](GEELY_KX11_KNOWLEDGE_RU.md).
Она дополняет этот реестр проверенными результатами исследования, источниками доказательств и
открытыми вопросами; наличие описанного интерфейса не означает подтверждённую работу на KX11.

## Как вести этот файл

1. Каждое новое требование получает постоянный идентификатор. Идентификаторы не переиспользуются.
2. Старое требование не удаляется. При изменении решения оно получает статус `ЗАМЕНЕНО` и ссылку
   на новое требование.
3. `В КОДЕ` не означает `ГОТОВО`. Для автомобильного интерфейса готовность наступает только после
   проверки на целевом KX11 и подтверждения пользователя.
4. Перед выпуском формируется матрица: требование → исходный файл → автоматическая проверка →
   результат на KX11. Непроверенный пункт нельзя описывать как исправленный без оговорки.
5. После каждого нового сообщения пользователя обновляются разделы требований, открытых дефектов
   и журнал изменений в конце файла.

Статусы:

- `ТРЕБОВАНИЕ` — принято, реализация не подтверждена.
- `В РАБОТЕ` — выполняется сейчас.
- `В КОДЕ / НУЖЕН KX11` — найдено в опубликованном коде и покрыто доступными статическими тестами,
  но не подтверждено на машине.
- `ПОДТВЕРЖДЕНО` — пользователь подтвердил правильную работу на целевом устройстве.
- `ПРОВЕРЕНО ПО АРТЕФАКТУ` — технический факт проверен по исходникам, CI, версии или сертификату; это не аппаратная приёмка.
- `В СОСТАВЕ ПОДПИСАННОЙ … / НУЖЕН KX11` — код включён в указанный выпущенный APK, но работа на машине ещё не подтверждена.
- `ОШИБКА` — пользователь сообщил, что реализация не соответствует требованию.
- `ЗАБЛОКИРОВАНО` — требуется внешняя возможность, оборудование или выбор пользователя.
- `ЗАМЕНЕНО` — действует более поздняя формулировка.

## 1. Неподлежащие изменению правила проекта и выпусков

| ID | Требование | Статус / критерий |
|---|---|---|
| REL-001 | Приложение называется **Natro**, а не Status Widget. | `ТРЕБОВАНИЕ`; проверять label и пользовательские тексты APK. |
| REL-002 | Сохранять прежний `applicationId` Natro и данные пользователя при обновлении. | `ТРЕБОВАНИЕ`; установка только поверх текущей сборки без очистки данных. |
| REL-003 | Каждый новый Natro имеет строго больший `versionCode`, обычно предыдущий +1. | `ТРЕБОВАНИЕ`; проверять через `aapt` и против предыдущего APK. |
| REL-004 | Номер версии содержит цифры патча только `0…9`: после `2.5.9` идёт `2.6.0`, а не `2.5.10`. | `ПРОВЕРЕНО ПО АРТЕФАКТУ` для `2.6.0`; правило действует для будущих выпусков. |
| REL-005 | Все устанавливаемые APK подписываются стабильной общей подписью Natro, чтобы обновляться поверх наших сборок. | `ТРЕБОВАНИЕ`; SHA-256 сертификата `6e9855aedc008bbdd8a7fbf3f490be07f964b7ac658a837a1592647a08365c75`. |
| REL-006 | Перед выдачей проверять подпись, схемы v2/v3, одного подписанта, `zipalign`, пакет, label, `versionName`, `versionCode` и установочную совместимость с предыдущим APK. | `ТРЕБОВАНИЕ`; отчёт должен прилагаться к выпуску. |
| REL-007 | Все написанные нами исходники всегда сохраняются в публичном `AGKulikov/Status`, включая Natro, Navigator mod, HUD Speed Bridge, Helper, тесты, патчеры и сценарии сборки. | `ТРЕБОВАНИЕ`; удалённое дерево сверяется с локальным. |
| REL-008 | Оригинальные APK, декомпилированные чужие исходники, базы камер, лицензии, токены и закрытые ключи в публичный Git не помещаются. | `ТРЕБОВАНИЕ`; baseline принимается только по закреплённому SHA-256. |
| REL-009 | Подписанные APK передаются отдельно; наличие APK без опубликованного соответствующего дерева исходников недопустимо. | `ТРЕБОВАНИЕ`. |
| REL-010 | Нельзя выдавать старую версию под видом новой или собирать от непроверенного/неопубликованного дерева. | `ТРЕБОВАНИЕ`; provenance содержит commit и tree SHA. |
| REL-011 | Успешная компиляция и статические тесты не доказывают работу интерфейса на KX11. Это нужно явно указывать до проверки пользователем. | `ТРЕБОВАНИЕ`. |
| REL-012 | При разработке дизайнов панели приборов APK не собирать до одобрения дизайна, если пользователь отдельно не дал прямую команду на сборку. | `ТРЕБОВАНИЕ`; текущая прямая команда на исправление/сборку является разрешением для данного выпуска. |
| REL-013 | Этот реестр является живым: новые требования добавлять при поступлении, статусы менять только с доказательством. | `В РАБОТЕ`; контроль через `AGENTS.md` и историю Git. |
| REL-014 | Все переданные для выпуска логи, видео и скриншоты должны быть просмотрены; необъяснённые падения, критические ошибки и расхождения с визуальным эталоном нельзя молча считать закрытыми. | `ТРЕБОВАНИЕ`; в отчёте указывать источник доказательства или отсутствие проверки. |
| REL-015 | Стабильный комплект подписи уже создан проектом и хранится в защищённом постоянном хранилище: `status-widget-ha-update.jks` и приватный резервный архив с файлом пароля. Перед выводом об отсутствии ключа обязательно проверять это хранилище и восстанавливать комплект самостоятельно; повторно запрашивать ключ у пользователя можно только после доказанной утраты или повреждения обеих копий. | `ПРОВЕРЕНО ПО АРТЕФАКТУ` 02.09.2026; восстановлены alias `status-widget-ha` и сертификат SHA-256 `6e9855aedc008bbdd8a7fbf3f490be07f964b7ac658a837a1592647a08365c75`. |
| REL-016 | Цифровую инструкцию Geely KX11 / ECARX хранить в этом репозитории рядом с реестром требований и использовать при дальнейшей работе с автомобильными интерфейсами. Дополнять её результатами новых дампов и логов, сохраняя источники, уровень доказательности, ограничения и открытые вопросы. | `В РАБОТЕ`: опубликована [база знаний](GEELY_KX11_KNOWLEDGE_RU.md); статические результаты отделены от подтверждения на автомобиле, неизвестные функции не объявлены раскрытыми. |
| REL-017 | Выпуск проверяет совместимость реально выдаваемой пары Natro/Navigator по возможностям протокола. Если Natro требует штатный artwork манёвра, Navigator должен объявлять CAP_MANEUVER_ARTWORK и содержать соответствующий IPC-путь. Одинаковый базовый versionName 30.3.0 не доказывает одинаковую версию нашего мода. При несовместимости передаётся совместимый подписанный Navigator вместе с указанием необходимости его обновления; старый мод не принимается за подходящий только потому, что карта работает. | `ПРОВЕРЕНО ПО АРТЕФАКТАМ 2.7.8`: DEX sendHello объявляет 0x7fe7 вместо старого 0x3fe7, artwork IPC и общий сертификат присутствуют; Navigator включён в выдачу. Отрисовку на KX11 подтверждает отдельный GATE-041. |

## 2. Целевая платформа и базовые компоненты

| ID | Требование | Статус / критерий |
|---|---|---|
| BASE-001 | Целевая магнитола — ECARX KX11, Android API 28, `arm64-v8a`. | `ТРЕБОВАНИЕ`. |
| BASE-002 | Основная рабочая область — `1760×720`. | `ТРЕБОВАНИЕ`. |
| BASE-003 | HUD: display ID `2`, поверхность `1920×1080`, активная область `x=0, y=720, 728×190`. | `ТРЕБОВАНИЕ`; любое изменение требует нового подтверждения пользователя. |
| BASE-004 | Базовый Яндекс Навигатор — `30.3.0`, пакет `ru.yandex.yandexnavi`, versionCode `739564630`. | `ТРЕБОВАНИЕ`. |
| BASE-005 | Базовый HUD Speed — `76.0-L13`, пакет `air.StrelkaHUDFREE`, versionCode `76000013`. | `ТРЕБОВАНИЕ`. |
| BASE-006 | Наш Navigator APK меняет только проверенные точки интеграции и новый dex; остальные baseline entries должны оставаться идентичными. | `ТРЕБОВАНИЕ`; проверяется boundary verifier. |
| BASE-007 | HUD Speed Bridge является read-only мостом уже загруженных камер и не должен извлекать/публиковать чужую базу. | `ТРЕБОВАНИЕ`. |
| BASE-008 | Оригинальная подпись HUD Speed недоступна. Первый переход с оригинального APK на NatroBridge требует резервной копии настроек и однократного удаления; последующие наши сборки обновляются поверх. | `ТРЕБОВАНИЕ`; миграция допускает root ADB с компьютера. |

## 3. Яндекс Навигатор, отдельные карты, HUD и карта панели приборов

### 3.1 Независимость и жизненный цикл карт

| ID | Требование | Статус / критерий |
|---|---|---|
| NAV-001 | Основной Навигатор, HUD и панель приборов должны иметь отдельные поверхности карты и независимые экземпляры/профили камеры. | `В КОДЕ / НУЖЕН KX11`. |
| NAV-002 | Масштабирование, перемещение, наклон и изменение основной карты не должны менять HUD или карту панели приборов. | `ОШИБКА` по прежним сборкам; текущую реализацию проверять на KX11. |
| NAV-003 | HUD и панель приборов используют общий источник маршрута/позиции, но рисуют карты самостоятельно, а не скриншот основной карты. | `В КОДЕ / НУЖЕН KX11`. |
| NAV-004 | HUD-карта должна запускаться автоматически при холодном старте, а не только после входа в её настройки. | `ОШИБКА` в прошлых сборках; нужен холодный старт на KX11. |
| NAV-005 | Элементы HUD не должны исчезать после обновления; карта, часы и настроенные слои должны восстанавливаться без ручного открытия редактора. | `ОШИБКА`; нужен тест обновления и холодного старта. |
| NAV-006 | После завершения или отмены маршрута на HUD не должен оставаться фантомный маршрут. | `ОШИБКА`; проверять остановку, новый маршрут и потерю Guidance. |
| NAV-007 | Полоса маршрута/пробок не должна мерцать ни с маршрутом, ни без него. | `ОШИБКА`; требуется длительный визуальный тест. |
| NAV-008 | Повороты карты, особенно около 90°, должны быть плавными и не перегружать систему. | `ТРЕБОВАНИЕ`; сравнивать с основной картой и измерять frame pacing. |
| NAV-009 | На основной карте не должно быть двух курсоров/стрелок; карта не должна прыгать из-за конкурирующих владельцев камеры или позиции. | `ОШИБКА` в прежних сборках; нужен маршрутный тест на KX11. |
| NAV-010 | При открытии Музыки, климата или другого приложения независимые карты продолжают движение не менее двух минут; возврат в Навигатор не создаёт второй курсор и не вызывает скачок основной карты. | `В КОДЕ / НУЖЕН KX11`. |
| NAV-011 | Обычный и оконный Навигатор не должны самопроизвольно завершаться после запуска. Automotive `NavigationLayer`/дополнительная `GuidanceCamera` запрещены на независимом `OffscreenMapWindow`, если они приводят к асинхронному падению MapKit. | `ОШИБКА` подтверждена журналом 01.09.2026: три завершения через 13, 10 и 9 с после подключения HUD-карты; безопасный hotfix в коде, нужен тест без маршрута и на маршруте по 30 минут. |
| NAV-012 | Вся независимая карта HUD и вся карта панели приборов не должны мерцать, кратко очищаться или показывать пустой кадр при обновлении позиции, маршрута, пробок, настроек либо surface. Последний полностью отрисованный кадр сохраняется до готовности следующего; запрещено периодически пересоздавать `OffscreenMapWindow`, renderer или bitmap только ради обновления данных. | `В КОДЕ / НУЖЕН KX11`; после ошибки 02.09.2026 устранены обновление геометрии маршрута до 10 раз/с, повторная публикация surface при resize и отзыв lease при временной невидимости окна. Пройденный маршрут скрывается одним обратимым `Subpolyline.hide`, lease сохраняет последний кадр. |
| NAV-013 | При изменении ширины или высоты элемента карты HUD карта перестраивает реальный viewport и композицию под новую геометрию, а не растягивает последний кадр старого размера. Во время resize сохраняется последний готовый кадр до готовности нового, поэтому исправление не должно возвращать мерцание или пустую surface из `NAV-012`. | `В КОДЕ / НУЖЕН KX11`; resize публикует новую неизменяемую generation и пересоздаёт `OffscreenMapWindow` после короткого объединения изменений, требуется `GATE-015`. |
| NAV-014 | Известные аварийные механизмы считаются постоянными запретами для каждого следующего выпуска: независимый `OffscreenMapWindow` не создаёт Automotive `NavigationLayer` и собственную `GuidanceCamera`; оконная identity типа 2038 не применяется до конца `onResumeFragments` и не переотправляется при обновлении несвязанных настроек; смена surface сначала отсоединяет старую поверхность, а все отложенные Natro-callback оконного режима изолируют свои исключения от главного looper Навигатора. | `В КОДЕ / НУЖЕН KX11`: повторный аудит 02.09.2026 не нашёл возвращённых нативных путей, DEX-verifier расширен, новые poll/inset callbacks получили аварийную границу; требуется `GATE-007` и `GATE-021`. |
| NAV-015 | Перед следующими правками оконного интерфейса должна существовать проверяемая карта фактической UI-иерархии Navigator 30.3.0 для `MapActivity`, свободного режима, активного Guidance, верхних уведомлений, штатных контролов и слоя Natro. Для каждого изменяемого узла фиксируются layout/владелец, способ появления, inset/padding/margin, динамическая замена, допустимое вмешательство и восстановление. Утверждение о владельце отступа или координаты без этой цепочки запрещено. | `СТАТИЧЕСКИ ЗАВЕРШЕНО`: точный baseline `663018fb…f27f` разобран в `docs/NAVIGATOR_30_3_UI_CONTAINER_AUDIT_RU.md`; аппаратные свойства vendor WindowManager/анимаций подтверждаются только соответствующими KX11-gates. |

### 3.2 Оконный режим Навигатора

| ID | Требование | Статус / критерий |
|---|---|---|
| WIN-001 | Навигатор должен стабильно запускаться в ограниченном окне из лаунчера и панели приборов, без промежуточного падения. | `ОШИБКА`; проверять холодный/тёплый запуск не менее 10 раз. |
| WIN-002 | Оконный режим не должен сам переходить в полный экран, скрывать HOME, системную строку или нижнее меню. | `ТРЕБОВАНИЕ`. |
| WIN-003 | Вокруг окна не должно быть чёрного непрозрачного фона; фон и углы соответствуют выбранным настройкам. | `ОШИБКА`; нужен визуальный тест. |
| WIN-004 | Должны настраиваться положение, размер, фиксация окна и радиус скругления. | `ТРЕБОВАНИЕ`. |
| WIN-005 | При тапе по карте слева должна появляться кнопка переключения полноэкранного/оконного режима. | `В КОДЕ / НУЖЕН KX11`: реализовано заново через фактический штатный контейнер по `WIN-018`, требуется `GATE-024`. |
| WIN-006 | Верхние элементы в оконном режиме должны располагаться выше; не допускается двойной внутренний отступ от системного inset. | `ОШИБКА В 2.6.8 НА KX11`: именованные контейнеры получили нулевой padding, но штатные inset-listener вложенных `PaddingtonView` снова добавляют root inset; исправление 2.6.9 ведётся по `WIN-014`. |
| WIN-007 | Для оконного режима нужны отдельные переключатели скрытия элементов интерфейса. | `ТРЕБОВАНИЕ`. |
| WIN-008 | Поведение и расположение кнопки сверять с предоставленным рабочим модом 29.4.2. | `ТРЕБОВАНИЕ`; копировать поведение, не чужой код. |
| WIN-009 | Кнопка режима не является постоянным оверлеем: появляется после касания карты третьей в левом столбце штатных контролов, переключает окно/полный экран и затем скрывается по штатному сценарию. | `В КОДЕ ДЛЯ 2.7.3 ПОСЛЕ ОШИБКИ / НУЖЕН KX11`: безусловный `VISIBLE` удалён; перед каждым draw кнопка зеркалирует фактическую видимость exact-pair road-event/voice, а poller выполняет только rebind. Требуется повтор `GATE-024/GATE-025`. |
| WIN-010 | Переключение в обе стороны выполняет один и тот же элемент: в оконном режиме он переводит в полный экран, в полном экране — обратно в окно. Раздельные кнопки и ветвь, отсутствующая в одном режиме, запрещены. | `В КОДЕ / НУЖЕН KX11`: один reparentable-экземпляр сохраняет двусторонний listener; требуется `GATE-016` и `GATE-024`. |
| WIN-011 | Кнопка режима должна быть реальным дочерним элементом штатного левого вертикального блока управления сразу после кнопок дорожного события и Алисы/голоса. Экранный оверлей с вычисляемой координатой, а также сохранённые положения справа/снизу запрещены. | `ЗАМЕНЕНО WIN-013`: аппаратная проверка 2.6.8 доказала, что внедрение дочернего элемента в перестраиваемое дерево Яндекса делает кнопку недоступной. |
| WIN-012 | В оконном режиме нулевой верхний системный inset применяется не только к карте, но и к отдельному штатному `PaddingtonView`/`controls_engine_container` и контейнеру верхних уведомлений; при возврате в полный экран исходные padding/inset восстанавливаются. | `ОШИБКА В 2.6.8 НА KX11`; перечисления именованных контейнеров недостаточно, расширено `WIN-014`. |
| WIN-013 | Единственная кнопка режима принадлежит стабильному слою Natro, после каждого тапа гарантированно выводится поверх интерфейса и визуально занимает третье место в левом штатном столбце сразу после дорожного события и Алисы/голоса. Яндекс не должен удалять, скрывать или переподключать её при перестройке своего дерева; справа и в постоянном виде кнопки нет. | `ЗАМЕНЕНО WIN-018`: пользователь потребовал использовать фактический штатный контейнер, его подложку и цикл появления; стабильный overlay остаётся только безопасным владельцем/fallback до готовности контейнера. |
| WIN-014 | В оконном режиме повторный верхний inset подавляется у каждого фактически созданного `PaddingtonView` в деревьях контролов и верхних уведомлений, включая поздно добавленные вложенные контейнеры. Нельзя ограничиваться разовой записью padding: штатный listener, повторно читающий root inset, должен быть переопределён на всё время жизни оконного Activity; полноэкранный Activity сохраняет штатные insets. | `ОШИБКА НА KX11 03.09.2026`: свободный режим поднялся, но отдельный поздний контейнер активного Guidance сохранил повторный inset; уточнено `WIN-015`. |
| WIN-015 | При активном ведении маршрута верхняя карточка манёвра, группа текущей скорости/ограничения и остальные верхние Guidance-элементы в оконном режиме используют тот же нулевой повторный системный inset, что и свободный режим. Отдельный `PaddingtonView` вокруг `navi_guidance_controls_touch_container` должен находиться заново после каждой поздней замены Guidance-контроллера и удерживаться собственным listener/guard; ссылка на уже отсоединённое дерево недопустима. | `ОШИБКА НА KX11 03.09.2026`: в подписанном Navigator 30.3.0 из пары Natro 2.7.0 реализация ограничивалась внутренним `PaddingtonView`, но по `IMG_8227.jpeg` карточка манёвра и вся группа скорости по-прежнему вместе смещены вниз; исправление продолжено на внешних уровнях в `WIN-017`. |
| WIN-016 | Кнопка оконного режима всегда находится слева непосредственно под штатной парой «дорожное событие + Алиса/голос» и не меняет координату между тапами, появлением/скрытием карточки манёвра, свободным режимом и активным ведением. Позицию нельзя вычислять по глобальному `alice_fab`, нулевым ghost-view или другим плавающим контролам. После первого валидного layout точный третий слот фиксируется на всё время жизни Activity; до layout используется один детерминированный левый fallback. | `ЧАСТИЧНО ЗАМЕНЕНО WIN-018`: запрет на глобальные/ghost-якоря сохраняется, но экранная координата больше не является основным механизмом — третий слот должен принадлежать фактическому левому контейнеру. |
| WIN-017 | Исправление верхнего отступа активного ведения применяется ко всему экрану Guidance, а не к одному составному знаку: в оконном режиме общий `activity_container_controller` и безымянный корень `CarGuidanceController`, содержащий одновременно `contextmaneuverview` и `speed_group`, не получают глобальный status-bar inset. Штатные внутренние отступы элементов (в baseline 30.3.0 — 12 dp сверху) сохраняются; фиксированный отрицательный `translationY` и индивидуальное смещение отдельных знаков запрещены. Поздняя замена контроллера должна переопределяться по фактической identity, полноэкранный режим восстанавливает штатное состояние. | `ОШИБКА В 2.7.2 НА KX11 / ЗАМЕНЕНО WIN-020`: guard точного `PaddingtonView` не убрал большой общий пустой пояс на `IMG_8236.jpeg`; фактический владелец экранной координаты должен быть установлен заново. |
| WIN-018 | Кнопка оконного/полноэкранного режима должна быть третьим визуальным элементом фактического левого вертикального контейнера сразу после точной штатной пары `guidance_add_road_event` + `guidance_open_voice_search` либо `navi_service_add_road_event` + `navi_service_open_voice_search`. Валидируется ближайший общий вертикальный контейнер: для Guidance прямым ребёнком является штатная обёртка `MapControlsFrameLayoutRect`, внутри которой находится `guidance_add_road_event`, а голосовая кнопка является следующим прямым ребёнком; для свободного режима оба штатных контрола являются прямыми детьми. Поиск по глобальным Alice/ghost/координатам запрещён. Кнопка использует штатный прямоугольный фон, размер, внутренние отступы и нажатое состояние семейства `MapControlsFrameLayoutRect`, а появление, скрытие, перемещение, alpha/scale и перестроение наследует от того же штатного контейнера, без отдельного пятиисекундного fade/таймера Natro. После замены свободного режима/Guidance элемент заново подключается только к новой валидной паре; до её появления touch-прозрачный слой Natro может держать кнопку скрытой, но не рисовать отличающийся постоянный дубль. | `В КОДЕ ДЛЯ 2.7.3 ПОСЛЕ ОШИБКИ / НУЖЕН KX11`: точный третий слот сохранён; `StockControlRail` хранит реальные road-event/voice views, pre-draw зеркалирует их объединённую visibility и штатные alpha/scale/translation, не меняя иерархию. Нужен повтор `GATE-024`. |
| WIN-019 | Тап по свободной области карты в оконном и полноэкранном режимах обязан доходить до штатного обработчика Navigator и переключать видимость его vanishing/Guidance-контролов так же, как в неизменённом 30.3.0. Natro не перехватывает жест и не изменяет штатный rail синхронно внутри `ACTION_DOWN`: поиск/подключение кнопки режима выполняется только после штатной диспетчеризации тапа. Полноэкранный touch-прозрачный `controlLayer` принимает события лишь в реальных границах собственных ручек/кнопок; pan, pinch и штатное скрытие/появление элементов карты остаются рабочими. | `В КОДЕ ПОСЛЕ ОШИБКИ 2.7.1 / НУЖЕН KX11`: full-size слой больше не переопределяет `dispatchTouchEvent`; hook только наблюдает полный жест, отменяет rebind при move/multitouch и ставит подключение кнопки в main-queue лишь после завершённого `ACTION_UP`. Poller не меняет иерархию во время жеста; требуется `GATE-025`. |
| WIN-020 | В оконном режиме с активным маршрутом вертикальная координата всей верхней Guidance-группы должна вычисляться относительно фактического верхнего края видимого map viewport. Карточка манёвра и `speed_group` сохраняют только штатный внутренний отступ baseline, без резервирования отсутствующего полноэкранного status/header/banner-контейнера. Исправление обязано воздействовать на реального владельца `top/margin/padding/inset`, найденного по исполняемой цепочке layout, и повторяться после поздней замены контроллера; запрещено маскировать ошибку постоянным `translationY` отдельных элементов. | `В КОДЕ / НУЖЕН KX11`: общий `MATCH_PARENT`-корень сопоставляется с реальным map viewport через `getLocationInWindow`, излишек компенсируется его margin; штатные 12 dp, физический низ/ETA и полноэкранный режим сохраняются. Есть защита от накопления и восстановление по identity; эталон отказа `IMG_8236.jpeg`, критерий `GATE-027`. |
| WIN-021 | Тап по избранному маршруту из Natro открывает точку в уже настроенном окне Navigator, а не переводит Navigator в полный экран. Первичный deep link адресуется точному `MapActivity` с оконным признаком; после обработки ссылки Natro повторно подтверждает оконный режим через аутентифицированный bridge, чтобы поздняя перестройка task не сбросила surface. Полноэкранный fallback допустим только при доказанно недоступном оконном запуске, а не как обычный результат успешного тапа. | `В СОСТАВЕ ПОДПИСАННОЙ 2.7.6 / НУЖЕН KX11`: exact-component launch и два отложенных подтверждения window mode добавлены; требуется `GATE-045`. |

### 3.3 Навигационные слои и визуальные требования

| ID | Требование | Статус / критерий |
|---|---|---|
| MAP-001 | Светофоры с обратным отсчётом должны отображаться. Если приватный stock-view недоступен, резервный слой сохраняет сигнал и секунды. | `В КОДЕ / НУЖЕН KX11`; положение и хвост уточнены в `MAP-032`. |
| MAP-002 | Знаки движения по полосам должны располагаться сбоку от дороги, как в основном Навигаторе, и не перекрывать дорогу впереди. | `В КОДЕ / НУЖЕН KX11`; автоматическая сторона уточнена в `MAP-032`, штатный хвост — в `MAP-033`. |
| MAP-003 | Ограничения камер из MapKit переводятся из м/с в км/ч: около `16,67 → 60`, `25 → 90`. Нельзя рисовать `17` и `25` как готовые км/ч. | `В КОДЕ / НУЖЕН KX11`; `CameraSpeedNormalizer` и Java-регрессия прошли CI 01.09.2026. |
| MAP-004 | HUD Speed передаёт числовой лимит вместе с единицей; `KPH`, `MPH` и `MPS` нормализуются в целые км/ч. | `В КОДЕ / НУЖЕН KX11`; неизвестная/отсутствующая единица сохраняет KPH для обратной совместимости, Java-регрессия прошла CI 01.09.2026. |
| MAP-005 | Камера и её скорость отображаются одним цельным знаком. Отдельная камера плюс отдельный круг скорости рядом запрещены. | `В КОДЕ / НУЖЕН KX11`. |
| MAP-006 | Одна физическая камера не должна порождать несколько соседних знаков. Дубликаты между Yandex/HUD Speed и внутри одного источника объединяются, сохраняется запись с наиболее полной информацией. | `В КОДЕ / НУЖЕН KX11`; правило объединения типов и направлений уточнено в `MAP-031`. |
| MAP-007 | Направление камеры должно иметь отдельные настройки размера и прозрачности; изменения применяются сразу. | `ЗАМЕНЕНО MAP-026`; единый параметр размера больше не используется. |
| MAP-008 | Названия улиц рисуются непосредственно на полосе маршрута отдельным плоским слоем, ориентированным вдоль маршрута, а не отдельной карточкой рядом. | `ЗАМЕНЕНО MAP-024`; аппаратная проверка показала, что самостоятельно нарисованный bitmap не повторяет штатный вид и изгиб подписи Яндекса. |
| MAP-009 | На линии маршрута должны отображаться стрелки предстоящих поворотов, как в основном Навигаторе. | `В КОДЕ / НУЖЕН KX11`; после невидимого результата 2.6.2/2.6.3 найдено, что штатный `createDefaultManeuverStyle` возвращает `enabled=false`, а код повторно присваивал это значение стрелкам. Теперь полный `DrivingRoute.geometry` передаётся в `RouteHelper.addManeuvers`, стиль явно создаётся с `enabled=true`, стрелки явно видимы. |
| MAP-010 | Пользователь может отключить автоматическое изменение масштаба от скорости и задать фиксированный zoom. | `В КОДЕ / НУЖЕН KX11`. |
| MAP-011 | В режиме свободной камеры фиксируется географическая точка, но последующие настройки zoom/tilt продолжают работать. | `В КОДЕ / НУЖЕН KX11`. |
| MAP-012 | В режиме «Только дороги» всё между дорогами прозрачно, но Guidance, знаки, манёвры и обязательные маршрутные элементы не фильтруются. | `ОШИБКА` в прежней версии; исправление требует визуальной проверки. |
| MAP-013 | Фон HUD-панели может быть полностью прозрачным. | `ТРЕБОВАНИЕ`. |
| MAP-014 | Дорожные события имеют отдельное подменю и режимы `всегда`, `только на маршруте`, `скрыто`. | `ОШИБКА`, уточнена MAP-053: прежнее включение общего слоя при свежем маршруте пропускает соседние дороги. Персональный native-фильтр добавлен в код 2.8.0; аппаратный GATE-057 открыт, аварийный automotive-слой запрещён. |
| MAP-015 | При активном маршруте пробки отображаются на маршруте; без маршрута допускается показ всех пробок. Пройденный участок не должен снова выделяться как непройденный. | `В КОДЕ / НУЖЕН KX11`. |
| MAP-016 | Настройки карты HUD и панели приборов должны быть человеческими: переключатели и ползунки вместо обязательного ручного ввода чисел. | `ТРЕБОВАНИЕ`. |
| MAP-017 | Размер, видимость и приоритет каждого пользовательского слоя настраиваются отдельно, без влияния на основную карту. | `ТРЕБОВАНИЕ`. |
| MAP-018 | На HUD и панели приборов отображается конечная точка активного маршрута; после завершения маршрута маркер исчезает вместе с маршрутными элементами. | `В КОДЕ / НУЖЕН KX11`. |
| MAP-019 | Манёвр, расстояние, полосы, маршрутные пробки, светофоры и повороты показываются только при свежем активном маршруте; после завершения маршрута или потери bridge устаревшие элементы очищаются не позднее 3 секунд. | `В КОДЕ / НУЖЕН KX11`. |
| MAP-020 | По умолчанию используется feature-aware порядок MapKit 30.3.0; ручной порядок включается отдельным переключателем и имеет ползунок для каждого независимо переставляемого root-layer. Нативные стрелки не являются отдельным слоем и всегда следуют приоритету маршрутной полилинии. При отключении ручного режима восстанавливается проверенный автоматический порядок без перестановки Yandex-owned слоёв. | `В КОДЕ 2.6.9 / НУЖЕН KX11`; старый отдельный ползунок стрелок удалён как неработающий, JSON-ключ сохранён alias к маршруту. |
| MAP-021 | Для HUD и панели приборов независимо настраиваются размеры курсора, знаков полос, камер и их информации, светофоров и секунд, названий улиц, дорожных событий, конечной точки, а также толщина маршрута и обычных улиц. | `В КОДЕ / НУЖЕН KX11`. |
| MAP-022 | При активном маршруте курсор использует привязанную к маршруту позицию MapKit. Обрезка линии обратима: после ложного скачка GPS вперёд и возврата скрытый участок маршрута восстанавливается сразу. | `В КОДЕ / НУЖЕН KX11`. |
| MAP-023 | Повороты на линии нельзя выводить самостоятельно нарисованными `Canvas`-пиктограммами или угадывать по строке действия. Используются штатные `PolylineMapObject.addArrow` и `RouteHelper.createDefaultManeuverStyle/applyManeuverStyle`, привязанные к точной `PolylinePosition` активного маршрута. | `В КОДЕ / НУЖЕН KX11`; основной путь — штатный `RouteHelper.addManeuvers` на неизменённой полной геометрии маршрута, с клоном штатного стиля и принудительным `enabled=true`; `addArrow` оставлен только совместимым штатным fallback без самодельной графики. Требуется `GATE-011`. |
| MAP-024 | На независимых картах HUD и панели приборов названия улиц отрисовывает штатный дорожный слой MapKit: шрифт, контур, ориентацию по изгибу дороги, плотность и разрешение конфликтов выбирает Яндекс. Самодельные `Bitmap`/`Canvas`-надписи и отдельные placemark-карточки запрещены. Публичный MapKit не фильтрует штатные подписи по принадлежности активному маршруту, поэтому режим «только на маршруте» заменяется включением штатных названий дорог; прежняя сохранённая настройка мигрирует без исчезновения подписей. | `В КОДЕ / НУЖЕН KX11`; Natro 2.6.5 и Navigator собраны из commit `21956ddf1c5b98ad85c1cb901972a5746013e183`, эталон — `IMG_8189.jpeg`, требуется `GATE-012`. |
| MAP-025 | Для штатных стрелок поворотов на полилинии HUD и панели приборов независимо настраиваются цвет заливки, цвет обводки, толщина обводки и длина. Изменение длины пропорционально меняет общую длину и наконечник, но не ширину стрелки и не толщину обводки. Ширина тела стрелки всегда определяется фактической толщиной маршрутной полилинии; отдельный множитель ширины стрелки запрещён. Если пользователь не выбрал цвета, сохраняются штатные цвета `ManeuverStyle` Яндекса. | `ЧАСТИЧНО ЗАМЕНЕНО MAP-030`: цвета, обводка и привязка ширины тела сохраняются, но наконечник больше не обязан меняться вместе с длиной. |
| MAP-026 | Единая настройка размера треугольного сектора направления камеры удаляется. Для HUD и панели приборов независимо задаются длина сектора, ширина только его широкого основания, цвет и прозрачность. Изменение ширины не меняет длину, положение вершины и дальность; изменение длины не растягивает основание. Старое значение размера мигрирует одновременно в длину и ширину. | `В КОДЕ / НУЖЕН KX11`; независимая геометрия, настройки и миграция покрыты статическими регрессиями, требуется `GATE-014`. |
| MAP-027 | Единый знак камеры со скоростью должен быть компактным и максимально близким к штатному виду Яндекс Навигатора: белый круг ограничения с красной окантовкой, число скорости по центру и маленький значок камеры у края. Большая прямоугольная карточка, хвост, подпись источника и дополнительные условные символы внутри запрещены; прозрачное поле bitmap должно быть минимальным. | `ЗАМЕНЕНО MAP-029`; пользователь отдельно уточнил, что маленький значок камеры у круга скорости тоже нужно удалить. |
| MAP-028 | Диапазон настройки длины штатных стрелок поворотов на полилинии начинается с `10%`, а не с `50%`, отдельно для HUD и панели приборов. Остальная геометрия и правила `MAP-025` сохраняются. | `В КОДЕ / НУЖЕН KX11`; диапазон конфигурации, обеих настроек и слоя изменён на 10–250%, требуется обновлённый `GATE-013`. |
| MAP-029 | Если источник передал ограничение скорости камеры, единый знак состоит только из компактного белого круга с красной окантовкой и числом скорости. Дополнительный маленький значок камеры на краю или рядом запрещён. Значок камеры допускается только для камеры без известного ограничения скорости, чтобы такой объект оставался распознаваемым. | `УТОЧНЕНО MAP-031`: для обычной камеры скорости остаётся только круг; если одна физическая камера одновременно контролирует полосы/перекрёсток/остановку, рядом внутри одного составного знака сохраняется штатная пластина соответствующего контроля, но маленькая камера на круг скорости всё равно запрещена. |
| MAP-030 | Для HUD и панели приборов размер треугольного наконечника штатной стрелки поворота на маршрутной полилинии настраивается отдельно от длины тела в диапазоне 10–250%. Изменение наконечника не меняет длину и ширину тела; изменение длины не обязано менять наконечник. При невозможной комбинации MapKit может только ограничить наконечник длиной стрелки. Ширина тела остаётся привязана к толщине маршрутной линии. Старое единое значение длины мигрирует одновременно в длину и наконечник, чтобы сохранить прежний вид. | `В КОДЕ 2.6.9 / НУЖЕН KX11`; отдельное поле, два экрана настроек, миграция и статические контракты добавлены; требуется обновлённый `GATE-013`. |
| MAP-031 | Одна физическая камера может содержать несколько типов контроля и два направления. При объединении записей Yandex и HUD Speed нельзя выбрасывать второй тип или второе направление: метаданные объединяются в один маркер. Для контроля полос/разметки, перекрёстка/светофора или остановки используется точная компактная штатная пластина Яндекса, состыкованная с кругом скорости без большого пустого поля. Внутри круга скорости и у его края дополнительный маленький значок камеры запрещён. Разные последовательные физические камеры одного источника не объединяются только из-за близости дальше 12 м. | `В КОДЕ 2.6.9 / НУЖЕН KX11`; требуется обновлённый `GATE-014` по эталонам `IMG_8208.jpeg`/`IMG_8209.jpeg`. |
| MAP-032 | Табличка полос, светофоры и составные знаки камер на независимых картах HUD/панели участвуют в общем экранном размещении. На каждом движении/масштабировании карты выбирается свободное из восьми положений вокруг точной географической точки с учётом краёв viewport и уже занятых прямоугольников; карточки не должны складываться друг на друга или закрывать маршрут впереди. Светофор всегда находится сбоку от маршрута и имеет видимый штатный хвост к своей координате; региональный fallback также обязан рисовать хвост. | `ЧАСТИЧНО ЗАМЕНЕНО MAP-037/MAP-038`: восемь положений сохраняются только для карточек полос и светофоров; перенос любого знака камеры отклонён аппаратной проверкой 03.09.2026, а фактический коридор и повороты маршрута учитываются по `MAP-038`. |
| MAP-033 | Табличка движения по полосам должна передаваться штатной текстурой Яндекса целиком вместе с хвостиком. Хвост меняет сторону вместе с выбранным свободным положением и остаётся привязан к точной `RoutePosition`; ручной bitmap без хвостика запрещён. | `В КОДЕ 2.6.9 / НУЖЕН KX11`; слой использует `LaneSignBalloonTextureFactory.createTexture`, `BalloonAnchor` и штатный `BalloonGeometry.imageAnchor`, требуется `GATE-018`. |
| MAP-034 | После добавления или изменения любого слоя нужно повторно сверять полный порядок отрисовки HUD и панели приборов со штатным Яндекс Навигатором. Проверка охватывает дорожную подложку и подписи, пробки, маршрут, скрываемый пройденный участок, тело и наконечник стрелок манёвров на маршрутной полилинии, конечную точку, полосы, светофоры, камеры и их сектора/составные пластины, дорожные события, карточки Guidance и курсор. Нельзя считать корректным порядок, проверенный только между старыми слоями до появления новых; ручные приоритеты пользователя должны быть ограничены безопасным диапазоном, не позволяющим спрятать обязательные Guidance-элементы под дорогами или фоном. | `В КОДЕ 2.6.9 / НУЖЕН KX11`: отдельный `MapSublayerOrder` ищет каждый sublayer по `id + SublayerFeatureType`, перемещает только Natro-owned ID через `moveAfter/moveBefore`, делит камеру на `GROUND`-сектор и `PLACEMARKS_AND_LABELS`-знак и связывает стрелки с приоритетом маршрута. Автоматический порядок и безопасные feature-группы ручного режима описаны в `docs/NATRO_2_6_9_LAYER_ORDER_AUDIT_RU.md`; требуется `GATE-019`. |
| MAP-035 | Уменьшение единого/составного знака камеры не должно делать число ограничения или штатную пластину нечёткими. Поскольку MapKit принимает placemark как bitmap-текстуру, знак камеры нужно растрировать с запасом разрешения и уменьшать до выбранного экранного размера через `IconStyle`, а не рисовать текст непосредственно в минимальные 26–40 px. | `ЗАМЕНЕНО MAP-046`: исторический минимум текстуры 80 px не является текущим правилом. Действует единый путь к целевому физическому размеру с одной контролируемой стадией масштабирования; аппаратная проверка — `GATE-020/GATE-032`. |
| MAP-036 | Правило чёткого масштабирования распространяется на все масштабируемые значки независимых карт: камеры и скорость, светофоры и секунды, таблички полос, дорожные события, конечную точку, курсор и будущие placemark/card-слои. Для каждого выбранного размера финальная текстура должна заново формироваться из исходного vector/drawable или штатного фабричного рендера Яндекса в достаточном разрешении; запрещены увеличение ранее уменьшенного bitmap, повторное bitmap→bitmap сжатие и рисование мелкого текста сразу в минимальном растре. Если публичный MapKit принимает только bitmap, допускается один качественный oversampling/downsampling до целевого физического размера; геометрические маршрут, сектора и штатные стрелки остаются нативными объектами MapKit. | `УТОЧНЕНО MAP-046 / В СОСТАВЕ ПОДПИСАННОЙ 2.7.4 / НУЖЕН KX11`: полный охват слоёв и результаты ревизии находятся в `docs/NAVIGATION_SHARPNESS_AUDIT_RU.md`; прежний статус незавершённого аудита provider устарел, но аппаратные `GATE-020/GATE-032` остаются открытыми. |
| MAP-037 | Любой знак камеры — одиночный круг скорости, камера без скорости или составной знак с пластиной — остаётся визуальным центром на точной географической точке камеры и не переносится в одно из восьми положений. На активном маршруте знак не должен съезжать с линии из-за collision-layout; вершина сектора направления и центр placemark используют одну координату. Центрированная рамка камеры первой резервирует место в общем screen-space проходе, а карточки полос/светофоров обходят её. | `В КОДЕ 2.6.9 / НУЖЕН KX11`: все камерные знаки используют anchor `(0.5, 0.5)` через `reserveCentered`, порядок reservation изменён на камера → полосы → светофоры; требуется `GATE-018` и `GATE-022` по `IMG_8214.jpeg`/`IMG_8219.jpeg`. |
| MAP-038 | Автоматическое размещение подвижных карточек полос и светофоров должно учитывать не только viewport и соседние карточки, но и фактический экранный коридор активного маршрута около точки события. Все восемь штатных положений оцениваются после `worldToScreen` для участков до и после точки: участок после точки и ближайший изгиб/поворот имеют повышенный вес, поэтому карточка уходит на свободную сторону и не закрывает дорогу впереди. Азимут, tilt и zoom учитываются самим экранным проектированием; дополнительно защищаются курсор автомобиля и центрированные камерные знаки. Выбор детерминирован и устойчив: hash/id не выбирает сторону, а уже выбранная сторона меняется только при материальном выигрыше. Точечные камеры, курсор, конечная точка и нативные стрелки маршрута не сдвигаются этим механизмом. | `В КОДЕ ПОСЛЕ ОШИБКИ 2.7.1 / НУЖЕН KX11`: для каждой карточки сохраняется точная `PolylinePosition`, маршрут проецируется текущим `MapWindow`, все восемь реальных геометрий оцениваются по viewport, занятым объектам, подходу, участку после события и экранному изгибу. Курсор и центрированные камеры резервируются первыми, смена стороны имеет порог устойчивости. Java-harness проходит прямой, левый/правый поворот и S-участок при 50/75/100% и разных азимутах; аппаратный критерий — `GATE-026`. |
| MAP-039 | У каждого вынесенного светофора должна быть хорошо видимая векторная ножка от края плашки до точной географической координаты светофора. Конец ножки совпадает с `worldToScreen` точки события, направление меняется вместе с выбранной стороной, а сама координата placemark не подменяется смещённой. Требование действует и для приватной штатной текстуры Navigator, и для регионального fallback: если штатный renderer не даёт проверяемую ножку, слой добавляет собственный screen-facing connector, не перенося сигнал с маршрута. | `В КОДЕ ПОСЛЕ ОШИБКИ 2.7.1 / УТОЧНЕНО MAP-046 / НУЖЕН KX11`: штатная `TrafficLightViewImpl` получает выбранный `LegPlacement`, а тот же placemark через `CompositeIcon` всегда содержит отдельную vector-path ножку с tip-anchor в исходной координате; после ревизии 04.09.2026 она растрируется сразу в выбранный физический размер и передаётся с `IconStyle.scale=1`, без второго уменьшения. Named parts заменяются без `removeAll`, региональный fallback сохраняет собственный хвост; эталоны — `IMG_8229.jpeg`/`IMG_8230.jpeg`, критерии — `GATE-026/GATE-032`. |
| MAP-040 | Все одновременно валидные светофоры, полученные из активного маршрута Navigator, должны независимо присутствовать на HUD и панели приборов. Collision/leg-placement меняет только положение карточки и не может исключать событие. Ключ события включает устойчивую identity маршрута и точную позицию на полилинии; одинаковый цвет/секунды, обновление таймера, перестроение маршрута и совпадение экранных прямоугольников не являются причиной удалить соседний светофор. Светофор удаляется только после явного исчезновения из актуального snapshot, прохождения события или смены route epoch. | `В КОДЕ / НУЖЕН KX11`: удалены радиусная дедупликация 30 м, выбор только MAIN/ближайшего, внутреннее усечение опубликованного snapshot и MapKit-conflict `EQUAL`; каждый wrapper изолирован, identity включает route epoch/позицию/секцию/стрелку. Критерий `GATE-028`. |
| MAP-041 | На независимых картах HUD и панели приборов опционально отображаются все искусственные неровности только активного маршрута штатным знаком Navigator. Источник — `DrivingRoute.getSpeedBumps()`, точка вычисляется штатным `PolylineUtils.pointByPolylinePosition()` из точной `PolylinePosition` конкретного маршрута; запрещено искать знак среди POI, подменять неровности глобальными `DANGER`, угадывать координату или подключать аварийный Automotive `NavigationLayer`. Для HUD и панели существуют независимые переключатель, масштаб и приоритет; `mapkit_styling_automotive_route_speed_bump` растрируется сразу под целевой физический размер и передаётся MapKit с `scale=1`. Режим «везде» не создаётся: Navigator 30.3.0 экспортирует неровности только через активный `DrivingRoute` и не имеет `EventTag.SPEED_BUMP`. | `В КОДЕ / НУЖЕН KX11`: отдельный безопасный `SpeedBumpMapLayer`, критерий `GATE-030`. |
| MAP-042 | На независимых картах HUD и панели приборов опционально отображаются маленькие ограничения скорости, относящиеся к участкам активного маршрута, как на `IMG_8242.jpeg`. Источник и позиция должны быть подтверждены публичными данными `DrivingRoute`/его метаданных; запрещено принимать текущий лимит автомобиля за набор маршрутных знаков или дублировать камерные ограничения. Для HUD и панели существуют независимые переключатель и масштаб; слой следует безопасному автоматическому порядку и не закрывает курсор, камеры, манёвры и искусственные неровности. | `ЗАМЕНЕНО MAP-043`: пользователь уточнил, что дополнительные ограничения скорости не нужны. |
| MAP-043 | Дополнительный слой локальных ограничений скорости на HUD и панели приборов не создаётся. Сохраняются только уже предусмотренные текущий разрешённый лимит и камерные знаки; `DrivingRoute.getSpeedLimits()` не используется для новых placemark, чтобы не создавать дубли. | `ТРЕБОВАНИЕ`, уточнено пользователем 03.09.2026. |
| MAP-044 | На HUD и панели приборов доступен отдельный независимо размещаемый модуль прогноза текущей пробки, не являющийся частью карты и не заменяющий общий прогресс маршрута. Единственный источник — структурированное `com.yandex.navikit.guidance.Guidance.leftInTrafficJam()` и его `JamForecast.getDuration()/getMeters()`; OCR, чтение текста с экрана и вычисление разницы ETA запрещены. При наличии `JamForecast` модуль показывает строку вида `Пробка на 10 мин (1,2 км)`, при `null`, неактивном маршруте, смене route epoch или просрочке snapshot полностью скрывается. HUD и панель имеют независимые экземпляры, включение, геометрию, текст, цвет, фон, прозрачность, скругление, рамку и отступы. | `ЗАМЕНЕНО MAP-047`: аппаратная проверка доказала, что одного ненулевого `leftInTrafficJam()` недостаточно для совпадения со штатной видимостью. |
| MAP-045 | Для независимых карт HUD и панели приборов цвет плашки нашего светофора и непрерывного хвостика к его географической точке выбирается пользователем отдельно в каждом `MapProfile`. Один выбранный ARGB-цвет применяется к телу и гарантированному connector, не меняя цвет сигнала, цифры секунд, стрелку направления, точную координату, anchor, collision-рамку или порядок слоя. Изменение настройки немедленно инвалидирует текстуру; старые конфигурации мигрируют на прежний тёмный цвет. | `В ПОДПИСАННОЙ 2.7.4 / НУЖЕН KX11`: Apple color picker в обоих редакторах карт; у точной реализации 30.3.0 меняются внутренние body Paint и leg color штатного `TrafficLightViewImpl`, а при несовместимой региональной реализации используется целевой vector/Canvas fallback. Критерий `GATE-032`. |
| MAP-046 | После изменения размера любого масштабируемого слоя HUD/панели нельзя повторно растягивать или уменьшать уже готовый малый bitmap. Аудит охватывает камеры/скорость/составную пластину, светофоры/секунды/хвост, полосы, штатные дорожные события и подписи, неровности, конечную точку, курсор, исходный знак манёвра и все отдельные Canvas-модули. Vector/drawable и собственный текст строятся из исходника под текущую физическую геометрию; штатные фабрики Яндекса вызываются с текущим scale; Yandex-owned road-event provider сохраняет исходный provider и меняет только нативную zoom-функцию/размер caption; допустим только один контролируемый oversampling/downsampling, без цепочки bitmap→bitmap. Кэш обязан зависеть от физического размера/плотности и сбрасываться при изменении масштаба. | `В ПОДПИСАННОЙ 2.7.4 / НУЖЕН KX11`: уточняет и завершает ревизию `MAP-035/MAP-036`, статический аудит и аппаратная матрица `GATE-020/GATE-032`. |
| MAP-047 | Модуль прогноза пробки на HUD и панели отображается только тогда, когда штатный Navigator выбрал и показывает именно плашку пробки, а не при любом ненулевом `Guidance.leftInTrafficJam()`. Решение о видимости повторяет штатный `StatusPanel`: активный маршрут, свежий forecast, длительность не меньше штатного порога 240 000 мс, фактическая видимость панели и отсутствие вытеснившего её приоритетного состояния. При прекращении любого условия оба независимых модуля скрываются целиком в том же поколении данных; старое значение не удерживается таймером. | `В СОСТАВЕ ПОДПИСАННОЙ 2.7.6 / НУЖЕН KX11`: publisher проверяет активный маршрут, штатный порог и фактически видимую jam-плашку точного `StatusPanel`; иначе публикует пустое значение в том же snapshot. Требуется `GATE-033`. |
| MAP-048 | Знак камеры и треугольник/сектор её направления образуют один атомарный визуальный объект с общей identity, generation, географической точкой, признаком прохождения, TTL и видимостью. Направление никогда не отображается без соответствующего знака: если знак удалён, просрочен, пройден, отсечён viewport или подавлен collision-логикой, сектор скрывается в том же кадре. Разные conflict modes, позволяющие оставить только ground-сектор, запрещены. | `В СОСТАВЕ ПОДПИСАННОЙ 2.7.6 / НУЖЕН KX11`: знак создаётся первым, обе части имеют одну generation/identity/TTL и одинаковый collision mode; любой отказ очищает всё поколение. Требуется `GATE-034`. |
| MAP-049 | Обычные светофоры активного маршрута без доступного сигнала или обратного отсчёта отображаются на независимых картах HUD и панели штатным маршрутным знаком. В основном Navigator это не отдельный пользовательский map-layer, а feature группы route pins внутри `NaviGuidanceLayer`/`ManualGuidanceLayer`, управляемый `setTrafficLightsPinsVisible()` и оформляемый `RoutePinsStyleProvider.provideTrafficLightStyle()`. Эти владельцы запрещено подключать к независимому `OffscreenMapWindow` по `NAV-011/NAV-014`, поэтому Natro безопасно воспроизводит только данный feature собственной коллекцией. Источник — полный список `DrivingRoute.getTrafficLights()` с точными `TrafficLight.id` и `PolylinePosition`; список `Windshield.getTrafficLightsWithSignal()` используется только для живой цветной плашки. Для HUD и панели независимо настраиваются включение, масштаб и безопасный приоритет обычных светофоров. Если для той же route identity/позиции появился живой сигнал, обычный знак скрывается и заменяется одной цветной плашкой без дубля; после потери живого сигнала он может вернуться, пока точка ещё впереди. Все знаки исчезают после прохождения, смены route epoch или окончания маршрута и растрируются из штатных day/night vector-drawable непосредственно в выбранный физический размер. | `В СОСТАВЕ ПОДПИСАННОЙ 2.7.6 / НУЖЕН KX11`: отдельный `RouteTrafficLightMapLayer` читает полный маршрутный список, вычисляет точную точку штатным `PolylineUtils`, подавляет дубль живого сигнала и рисует day/night drawable сразу в целевом размере. Требуется `GATE-037`. |
| MAP-050 | При уходе с активного маршрута курсор и камера на независимых картах HUD и панели продолжают следовать текущему местоположению Navigator вне полилинии. Сохранённый маршрут и его последняя RoutePosition не являются текущей координатой автомобиля. Привязка к RoutePosition допустима только при подтверждённом статусе движения по маршруту; ROUTE_LOST/NOT_ON_ROUTE и неизвестный статус используют живую Guidance location и её heading, без удержания старой маршрутной точки. Возврат на маршрут, пересчёт и отсутствие маршрута обрабатываются без скачка к старой epoch. | `В СОСТАВЕ ПОДПИСАННОЙ 2.7.9 / НУЖЕН KX11`: ошибка после 2.7.8 разобрана; выбор позиции учитывает RouteStatus, 8 исполняемых replay-тестов пройдены. GATE-050 открыт. |
| MAP-051 | Обычные светофоры активного маршрута (без сигнала/обратного отсчёта) на обеих независимых картах находятся в нижней части слоя обозначений: сразу над маршрутной линией и названиями улиц, ниже дорожных событий, камер, неровностей, курсора, табличек полос и активных светофоров с секундами. Автоматический порядок и неизменённый стандартный ручной пресет согласованы; собственные приоритеты пользователя сохраняются. Проверяется реальный native sublayer по ID/feature type, а не только числовой zIndex. | `В СОСТАВЕ ПОДПИСАННОЙ 2.7.9 / НУЖЕН KX11`: native-слой и стандартный пресет обновлены, пользовательские приоритеты сохраняются; уточняет MAP-034. GATE-019 открыт. |
| MAP-052 | У карты есть независимо регулируемые степень размытия (мягкость перехода края в прозрачность) и размер области по краю. Настройки доступны для HUD и панели отдельно, сохраняются и действуют на живую независимую карту Android 9; нулевые степень/размер выключают эффект. Курсор/карта внутри области сохраняют чёткость, отдельные виджеты не размываются. Эффект не вводит копирование кадров/скриншоты, новый чёрный фон, смену Surface lease или перезапуск MapWindow при изменении только мягких краёв. | `В СОСТАВЕ ПОДПИСАННОЙ 2.7.9 / НУЖЕН KX11`: запрос пользователя 05.09.2026, GATE-053 открыт. |
| MAP-053 | Режим знаков «только на маршруте» на панели приборов и HUD фильтрует принадлежность события маршруту, а не просто наличие активного маршрута. Общий RoadEventsLayer не должен показывать события соседних дорог в этом режиме; настройки двух профилей независимы. IMG_8290.jpeg — аппаратное расхождение именно приборки; по уточнению пользователя HUD таких значков не показывает. Версия пары на фото не установлена. | `В СОСТАВЕ ПОДПИСАННОЙ 2.8.0 / НУЖЕН KX11`: ScaledRoadEventStyleProvider проверяет native isOnRoute каждого события и обновляет кэш видимости при смене настроек. Профили независимы; пять исполняемых проверок пройдены. HUD Speed имеет отдельное включение и не выдаёт native-признак Yandex, аппаратный источник значков на фото не доказан. GATE-057 открыт. |

## 4. Карточка предупреждений HUD и левая таблица знаков

| ID | Требование | Статус / критерий |
|---|---|---|
| CARD-001 | Макет предупреждений состоит ровно из двух блоков: основное предупреждение и ниже меньший блок «Далее». | `ОШИБКА` в предыдущих вариантах; эталон — переданные изображения. |
| CARD-002 | Главный блок должен повторять согласованный визуальный эталон, а не произвольную большую карточку. | `ТРЕБОВАНИЕ`. |
| CARD-003 | Знаки в левой таблице передаются как есть из источника; нельзя заново перерисовывать или интерпретировать их пиктограммы. | `ЗАМЕНЕНО CARD-013`: аппаратная проверка показала, что даже построение по semantic `Action` не гарантирует точную штатную геометрию знака. |
| CARD-004 | Для левой таблицы нужны расширенные настройки каждого элемента: размер каждого шрифта, внутренние отступы, размеры строк/ячеек, интервалы и видимость. | `В КОДЕ / НУЖЕН KX11`; добавлены независимые размеры шрифтов, четыре группы внутренних отступов, размеры зон/строк, интервалы, фон, рамка и видимость. |
| CARD-005 | Кнопка «Отмена» в редакторе не сохраняет изменения и возвращает состояние, существовавшее до открытия. | `ОШИБКА`; нужен тест снимка настроек. |
| CARD-006 | Изменение масштаба не должно фиксировать обрезанное изображение или делать дальнейшую настройку невозможной. | `ОШИБКА`; границы рассчитывать от исходной геометрии. |
| CARD-007 | Масштаб меняется шагом 1%. Рядом с ползунками масштаба и положения есть `−`/`+`: положение меняется на 1 px, масштаб — на 1%. | `ТРЕБОВАНИЕ`. |
| CARD-008 | Перемещение/масштабирование должно происходить визуально в реальном времени и сохранять возможность последующей правки. | `ТРЕБОВАНИЕ`. |
| CARD-009 | У карточки ближайшего манёвра отдельно настраиваются три размера шрифта, доля и масштаб исходного знака, четыре отступа знака, четыре отступа текста, интервалы, фон, прозрачность, рамка и скругление; при отсутствии bitmap нельзя рисовать предполагаемую стрелку. | `В КОДЕ / НУЖЕН KX11`. |
| CARD-010 | Маршрутная карточка панели приборов отдельно показывает оставшееся расстояние, ETA и оставшееся время; настраивает их шрифты, отступы, знак, прогресс и маркер, использует реальные сегменты пробок и не содержит кнопку завершения маршрута. | `В КОДЕ / НУЖЕН KX11`. |
| CARD-011 | Знак манёвра, его semantic action, расстояние, основная/дополнительная информация и видимость передаются на HUD и панель одним версионированным кадром с общей `routeEpoch` и identity текущего манёвра. Bitmap от прежнего манёвра нельзя присоединять к новым текстам/направлению только потому, что он ещё не просрочен; при отсутствии изображения с совпадающим ключом старый знак очищается немедленно. | `ЧАСТИЧНО ЗАМЕНЕНО CARD-013`: единая identity и атомарная очистка сохраняются, но semantic action больше никогда не используется для самостоятельной отрисовки знака. Требуется `GATE-035/GATE-041`. |
| CARD-012 | Карточка ближайшего манёвра на HUD и панели повторяет структурированное содержание штатного `ManeuverView`, а не сводит его к `descriptionText/toponym`. Отдельно поддерживаются: основной блок знака и дистанции; следующая дорога `RegularManeuverDescription.nextRoadName`; список цветных указателей `DirectionSignItem` (`Road`, `Toponym`, `Exit`, `Icon`) с исходными `bgColor/textColor`; и условный дополнительный блок `ManeuverAuxiliaryContent` (`ExitNumberInfo`, `TurnNumberInfo`, `NextManeuverInfo`, `NextUpcomingManeuver` либо `NextUpcomingLaneSigns`). На эталоне `IMG_8251.jpeg` цветной «Подольск» является отдельной областью указателя, а `2-й съезд` — пристыкованным нижним auxiliary-блоком; при отсутствии каждого типа его область схлопывается без пустого места. | `В СОСТАВЕ ПОДПИСАННОЙ 2.7.6 / НУЖЕН KX11`: publisher безопасно читает фактически показанный `ContextManeuverView`, передаёт цветные items и conditional auxiliary в одном snapshot; оба renderer-а рисуют отдельные схлопывающиеся области. Требуется `GATE-036`. |
| CARD-013 | Знак ближайшего манёвра на HUD и панели приборов берётся только из фактически видимого штатного `image_maneuverballoon_maneuver` Navigator 30.3.0 вместе с его текущей отображаемой дистанцией. Самостоятельная Canvas/vector-отрисовка по semantic `Action`, догадка по тексту и повторное использование прежнего bitmap запрещены. Изображение передаётся отдельным ограниченным IPC-сообщением с `maneuverIdentity` и `sequence`; renderer принимает его только при точном совпадении с текущим snapshot. Если штатный знак временно отсутствует или ещё относится к предыдущей дистанции/identity, весь основной блок знака скрывается, а не показывает неверное направление. Синяя карточка не добавляет synthesized-текст вида «улица · направо»: дополнительные строки допускаются только из реально видимых структурированных stock-полей `CARD-012`. | `ОШИБКА НА СВЯЗКЕ С 2.7.7` 05.09.2026: на фото основной Navigator показывает левый поворот 100 м, HUD — без синей карточки. Его handshake `0x3fe7` не содержит `CAP_MANEUVER_ARTWORK=0x4000`; нужен совместимый Navigator. Самостоятельная отрисовка остаётся запрещена; `GATE-041` открыт. |
| CARD-014 | В каталогах HUD и панели приборов существует отдельный независимо размещаемый элемент «Сводка маршрута · как в Навигаторе», визуально приближенный к верхней части штатного блока на `IMG_8268.jpeg`: тёмная скруглённая плашка, три значения в одной строке (оставшееся расстояние, ETA, оставшееся время) и ниже тонкая полоса фактических сегментов пробок с маркером текущего прогресса. Кнопки варианта маршрута, закрытия и нижние стрелки выбора не переносятся. Элемент доступен как самостоятельная запись и на HUD, и на панели, имеет собственную геометрию/видимость/оформление и целиком скрывается без свежего активного маршрута. | `В СОСТАВЕ ПОДПИСАННОЙ 2.7.6 / НУЖЕН KX11`: отдельные типы, настройки и direct Canvas-renderer добавлены в оба каталога; требуется `GATE-042`. |
| CARD-015 | Карточка ближайшего манёвра сохраняет реально видимую строку следующей дороги и её место под общим рядом знака/дистанции. На IMG_8278.jpeg это «Еринское ш.» под «2,5 км»; HUD на IMG_8279.jpeg обязан показывать ту же строку. Запрет synthesized-текста не разрешает удалять видимое stock-поле. Отсутствующие строки не резервируют пустую площадь; цветные указатели и auxiliary остаются самостоятельными областями. Пользовательские размеры шрифтов/отступы сохраняются, исходная пиктограмма не перерисовывается. | `В СОСТАВЕ ПОДПИСАННОЙ 2.7.9 / НУЖЕН KX11`: после аппаратного расхождения 2.7.8 восстановлена настоящая nextRoad под общим рядом знака/дистанции; 7 тестов состава/геометрии пройдены. Уточняет CARD-012/013; GATE-041 открыт. |
| CARD-016 | Изменение рисунка внутри прежнего Drawable штатного знака должно обновлять artwork HUD/приборки: недопустимо бессрочно сохранять переходный кадр только с наконечником. Исходная пиктограмма передаётся целиком без самостоятельной дорисовки. IMG_8288/8289 сняты в разное время: пользователь исключил различие расстояний из признаков ошибки. | `В СОСТАВЕ ПОДПИСАННОЙ 2.8.0 / НУЖЕН KX11`: ограниченный stock ImageView перерисовывается в переиспользуемый буфер; фактическое изменение пикселей создаёт новую immutable-копию и revision. Пять replay-проверок пройдены; причинность конкретного фото не подтверждена. GATE-055 открыт. |

### Уточнение механизма оригинальных знаков 05.09.2026

Пользователь не требовал снимать изображение с видимого ImageView. Его исходное требование —
использовать оригинальные знаки Navigator без собственной перерисовки — остаётся неизменным.
Ограничение способа передачи снимком в CARD-013 было техническим решением ассистента; в этой
части CARD-013/CARD-016 заменяются CARD-017. История дефектов и их аппаратные критерии сохраняются.

| ID | Требование | Статус / критерий |
|---|---|---|
| CARD-017 | Использовать штатные команды ManeuverView и исходные ResourceId, выбранные движком Navigator, для таблички HUD и приборки. Запрещены собственные пиктограммы и сопоставление semantic Action с похожей стрелкой. Оригинальные drawable загружаются из установленного совместимого Navigator и рисуются непосредственно в целевой размер. Снимок видимого ImageView не является обязательным или основным механизмом. | `В КОДЕ / НУЖЕН KX11`: механизм реализован и описан в docs/NATIVE_MANEUVER_COMMANDS_2026_09_05_RU.md; мост и новые классы ресурсов компилируются. 39 локальных проверок прошли. Ветка опубликована после прямого разрешения; полный Gradle-прогон и 50 проверок инструментов CI 33987507476 прошли, включая исправление подписи следующих полос. GATE-058 открыт. |
| CARD-018 | Захватывать полный набор команд ManeuverView: манёвр/дистанция/auxiliary, основные и следующие указатели, полосы, режим, содержательная видимость, масштаб и screensaver; учитывать штатные правила компоновки и взаимоисключающие области. Состояние передаётся атомарно с source generation и route epoch, без смешивания прежнего знака с новыми текстами. Оригинальный presenter, его подписка и штатный интерфейс продолжают работать; observer не создаёт второй Guidance/NavigationLayer. Смена владельца/маршрута, скрытие, потеря данных и неподдержанная версия очищают устаревшую карточку. | `В КОДЕ / НУЖЕН KX11`: наблюдаются 8 команд и 6 setter компоновки/владельца; команды и итоговая видимость областей передаются одним кадром. Подключение и replay не означают восстановления C++-алгоритмов или приёмки на KX11. GATE-058 открыт. |
| CARD-019 | При проверке оригинальности полос различать карточку манёвра, отдельный HUD-модуль и знак на карте у линии маршрута. Уточнение пользователя по IMG_8283.jpeg относится именно к последнему. Его графика должна поступать от штатной LaneSignBalloonTextureFactory с исходным LaneSign; собственные стрелки или fallback-перерисовка недопустимы. Оригинальность графики не означает тождественность наших условий показа/размещения штатному NavigationLayer. | `ПРОВЕРЕНО В ИСХОДНИКАХ 05.09.2026`: LaneGuidanceMapLayer уже использует штатную фабрику, масштаб передаёт до растеризации, итоговый IconStyle.scale=1; при ошибке скрывает знак. Аппаратная сверка положения/видимости остаётся открытой. |

## 5. Панель приборов Natro

| ID | Требование | Статус / критерий |
|---|---|---|
| CLUSTER-001 | В Natro существует отдельный раздел «Панель приборов» для внешнего дисплея с более высоким разрешением, чем HUD. | `В КОДЕ / НУЖЕН KX11`. |
| CLUSTER-002 | Панель редактируется в реальном времени по принципу HUD: элементы можно перемещать, масштабировать, скрывать и настраивать. | `В КОДЕ / НУЖЕН KX11`. |
| CLUSTER-003 | Карта панели приборов является отдельной независимой картой со своим профилем. | `В КОДЕ / НУЖЕН KX11`. |
| CLUSTER-004 | Нужны как цифровые, так и аналоговые варианты спидометров/тахометров; целевой каталог — около 10 вариантов каждого типа. | `ТРЕБОВАНИЕ`. |
| CLUSTER-005 | Визуальные ориентиры: современные панели BMW, Mercedes-Benz и Rolls-Royce; копировать уровень качества и композицию, а не товарные знаки/закрытые ассеты. | `ТРЕБОВАНИЕ`. |
| CLUSTER-006 | Последнее решение: оставить пять новых вариантов панели и удалить прежние варианты из выбора. | `ТРЕБОВАНИЕ`; перечень пяти фиксируется согласованными макетами. |
| CLUSTER-007 | Все пять вариантов модульные. В любом варианте можно убрать отдельный элемент: например спидометр, цифру скорости внутри него, либо добавить слева информационный блок. | `ТРЕБОВАНИЕ`. |
| CLUSTER-008 | Штатные нижние «крылья» KX11 нужно визуально маскировать фоном, потому что перекрасить прошивочные элементы нельзя. | `ТРЕБОВАНИЕ`. |
| CLUSTER-009 | Фон панели настраивается отдельно: сверху чёрный, снизу выбираемый пользователем цвет, регулируется размер/глубина чёрной зоны и переход градиента. | `ТРЕБОВАНИЕ`. |
| CLUSTER-010 | Геометрия и безопасные зоны соответствуют фактической панели KX11; прошивочные участки не должны конфликтовать с нашими элементами. | `ТРЕБОВАНИЕ`. |
| CLUSTER-011 | Скорость, обороты и другие быстрые параметры должны иметь минимальную задержку, сравнимую с переданным референсным дашбордом. | `ТРЕБОВАНИЕ`; измерять задержку, не оценивать только визуально. |
| CLUSTER-012 | Архитектура телеметрии выбирается по принципу максимальной скорости без перегрузки: быстрые сигналы не ждут медленных, тяжёлые данные не должны блокировать UI. | `ТРЕБОВАНИЕ`; допускается один транспорт с разными частотами/очередями данных. |
| CLUSTER-013 | Пользователь должен сначала одобрить макеты панели; сборка APK без одобрения дизайна запрещена, кроме отдельной прямой команды. | `ТРЕБОВАНИЕ`. |
| CLUSTER-014 | Зафиксированные варианты 11–15: **Slate Horizon** — два аналоговых прибора и центральная карта; **Glacier Map** — цифровая скорость, горизонтальный RPM и карта; **Aerowave** — зеркальные дуги; **Steel Vector** — цифровая скорость и вертикальный RPM; **Continuum** — левая полукруглая шкала и отдельный правый навигационный блок. | `В КОДЕ / НОВЫЕ МАКЕТЫ НЕ СОГЛАСОВАНЫ`: сохранён прежний набор; его наличие не подтверждает соответствие новым требованиям и отклонённым эскизам 04.09.2026. Действуют `CLUSTER-019…CLUSTER-021`, финальная композиция требует отдельного одобрения. |
| CLUSTER-015 | Каждый из пяти вариантов хранит собственные положение, размер, слой, видимость, прозрачность, стиль и состав модулей; переключение и миграция не сбрасывают пользовательскую конфигурацию. | `В КОДЕ / НУЖЕН KX11`. |
| CLUSTER-016 | В аналоговом приборе независимо скрываются фон, шкала, цифры, стрелка, цифровое значение и единица; в цифровом — фон, единица и линия значения. Информационный блок имеет три независимо выбираемые строки. | `В КОДЕ / НУЖЕН KX11`. |
| CLUSTER-017 | Включённая карта панели приборов запускается на холодном старте самой панели без предварительного входа в настройки Natro. Панель должна создать Binder endpoint до публикации первой `TextureView` surface и повторно передать сохранённый lease после подключения Navigator. | `ЧАСТИЧНО ЗАМЕНЕНО CLUSTER-018`: ранний endpoint и сохранённый lease остаются обязательными, но аппаратный журнал 04.09.2026 показал, что ожидание внешнего запуска и одного callback `SurfaceTextureListener` не гарантируют холодный старт; требуется `GATE-009/GATE-043`. |
| CLUSTER-018 | После reboot, QuickBoot, замены пакета или обычного перезапуска Natro включённая панель и её карта запускаются тем же автоматическим lifecycle-путём, что и HUD, без открытия настроек и без `force-stop` собственного пакета. Автоматический путь напрямую переключает DIM, будит дисплей и запускает `InstrumentPanelActivity`; Settings-only внешний handoff не участвует. Если Android 9 уже сообщает `TextureView.isAvailable()`, но пропустил начальный `SurfaceTextureListener`, панель обязана сама получить текущий `SurfaceTexture`, создать валидный `Surface` и опубликовать lease. Если Navigator отверг холодное подключение `OffscreenMapWindow`, Natro ограниченно повторяет тот же живой producer с новой generation и экспоненциальной задержкой; старый/отозванный lease не воскресает. | `ОШИБКА НА 2.7.7` 05.09.2026: HUD подключился в 14:28:15, cluster lease появился только после открытия SettingsHubActivity в 14:29:22. Ранее внесённые direct launch и Surface-retry остаются; проверяется недостающий вызов восстановления из обычного integration host, `GATE-043` открыт. |
| CLUSTER-019 | Панель приборов и экран магнитолы — разные физические экраны. Новые макеты разрабатываются именно для водительского дисплея KX11, с сохранением штатных неизменяемых элементов, их состава и безопасных зон; композиция экрана магнитолы не принимается как макет приборки. | `ТРЕБОВАНИЕ / НОВЫЕ ЭСКИЗЫ ОТКЛОНЕНЫ 04.09.2026`; сохраняются модульность пяти вариантов и фон `CLUSTER-006…CLUSTER-010`. |
| CLUSTER-020 | Новые варианты должны опираться на изученные реальные автомобильные приборные панели, естественно включать карту и другие информационные данные и существенно отличаться друг от друга композицией. Нельзя сводить все варианты к одной карте или менять обязательные штатные элементы. | `ТРЕБОВАНИЕ`; новые эскизы 04.09.2026 пользователем не приняты, `GATE-047`. |
| CLUSTER-021 | Последний конкретный визуальный ориентир адаптации — изображение пользователя `https://mobile-review.com/all/wp-content/uploads/2021/04/06-19.jpg`. Геометрия и пропорции колодцев должны соответствовать этому изображению при адаптации к реальному экрану KX11; произвольная замена формы не считается выполнением. | `ТРЕБОВАНИЕ / НУЖНО СОГЛАСОВАНИЕ МАКЕТА`; ссылка фиксирует источник, текущий аудит не заменяет отдельный просмотр и визуальное сравнение. Сборка исправления парктроников не является одобрением нового дизайна. |
| CLUSTER-022 | Автоматическое восстановление включённой приборки входит также в обычный запуск integration host, его восстановление watchdog и окончание lifecycle-паузы. Нельзя полагаться только на BootReceiver и открытие настроек. Повторный вызов сохраняет единственного launcher/живую Activity; переключения DIM выполняются вне main thread, без остановки Natro и его ANCS. | `В ПОДПИСАННОЙ 2.7.8 / НУЖЕН KX11`: вызовы из host добавлены и компиляция/CI пройдены; дополняет CLUSTER-018 / GATE-043. |

## 5.1 Панель штатной вкладки DIM

| ID | Требование | Статус / критерий |
|---|---|---|
| DIM-001 | Отдельная настраиваемая панель открывается на дисплее водителя в штатной вкладке «Навигация» и управляется прокруткой и подтверждением на руле. | `В КОДЕ / НУЖЕН KX11`. |
| DIM-002 | В панель добавляются сохранённые маршруты, приложения, функции автомобиля, сценарии умного дома, звонки и Android Intent; порядок действий редактируется. | `В КОДЕ / НУЖЕН KX11`. |
| DIM-003 | Настраиваются координаты, размер панели и строк, число видимых строк, отдельные шрифты, иконки, внутренние отступы, интервалы, цвета, рамка и прозрачность. Пользовательские параметры оформления применяются при отключённом точном пресете mNavi. | `В КОДЕ / НУЖЕН KX11`. |
| DIM-004 | Панель автоматически уступает дисплей mNavi, штатному звонку, медиаменю, другой DIM-вкладке и выключенному экрану; при недоступности ECARX API мост восстанавливается в фоне без блокировки Natro. Полная приборная панель Natro не является конфликтом: меню экрана водителя обязано открываться поверх неё и оставаться управляемым кнопками руля. | `В СОСТАВЕ ПОДПИСАННОЙ 2.7.6 / НУЖЕН KX11`: собственная instrument-панель удалена из conflict-policy и controller-gate, реальные внешние конфликты сохранены. Требуется `GATE-038`. |
| DIM-005 | Для действий умного дома меню экрана водителя показывает рядом с названием актуальное состояние из той же явной `stateBinding`, что и HOME/панель водителя: включено/выключено, положение, температура или другое значение с единицей. Недоступное устройство помечается «Недоступно», устаревшее значение — символом обновления, а отсутствие подтверждённого значения — «Нет данных»; последнее старое значение не выдаётся за текущее. Статус обновляется без сброса выбранной строки и не блокирует выполнение действия. | `В СОСТАВЕ ПОДПИСАННОЙ 2.7.6 / НУЖЕН KX11`: overlay подписан на общий connector listener, очищает состояние при смене сервиса и обновляет правую подпись строки без сброса выбора. Требуется `GATE-039`. |
| DIM-006 | Точный пресет оформления меню повторяет mNavi 2.0, включая цвета: прозрачная подложка `#00FFFFFF`, обычный текст/заголовок `#FF6C7984`, выбранная строка `#FF197BC5`, её текст `#FFFFFFFF`; заголовок 14 sp по центру, строки 24 sp bold с отступом 8 dp, скругление выбранной строки 6 dp, без внешней рамки, иконок и счётчика позиции. Старые конфигурации автоматически переходят на этот пресет, а сохранённые пользовательские цвета, рамка и отступы не влияют на него. | `В СОСТАВЕ ПОДПИСАННОЙ 2.7.6 / НУЖЕН KX11`: значения сняты с исполняемых ресурсов и реализации mNavi 2.0, закреплены константами renderer-а и schema-v2 migration. Требуется `GATE-040`. |

## 6. Natro: запуск, производительность и базовый интерфейс

| ID | Требование | Статус / критерий |
|---|---|---|
| NATRO-001 | Ускорить холодную загрузку Natro и убрать длительный период тормозов после старта. | `ОШИБКА`; измерять от подачи питания до готовности UI/телеметрии. |
| NATRO-002 | Не допускать чёрного экрана при первой загрузке и зависания на логотипе. | `ОШИБКА`. |
| NATRO-003 | Тяжёлые подсистемы запускаются параллельно там, где это безопасно; единственный владелец BLE и публикация UI-состояния сохраняют последовательность. | `ТРЕБОВАНИЕ`. |
| NATRO-004 | HUD/панель/медиа не должны перегружать hot path; частоты обновления и перерисовки ограничиваются фактическим изменением данных. | `ТРЕБОВАНИЕ`. |
| NATRO-005 | В статус-баре корректно определяются активная SIM и LTE/2G без обязательной перезагрузки. | `ОШИБКА`. |
| NATRO-006 | Иконка Bluetooth показывает состояние подключения и ANCS; иконка Wi‑Fi соответствует согласованному iOS-подобному стилю. | `ТРЕБОВАНИЕ`. |
| NATRO-007 | Заряд iPhone передаётся точным процентом, а не округлением по 5%. | `ТРЕБОВАНИЕ`. |
| NATRO-008 | Два независимо настраиваемых порога уведомления о низком заряде используют точный процент. | `ТРЕБОВАНИЕ`. |
| NATRO-009 | Последняя цветовая корректировка: при заряде ниже 20% батарея уже красная, а не жёлтая. | `ТРЕБОВАНИЕ`; заменяет прежнюю цветовую градацию для этой зоны. |
| NATRO-010 | Скрытие системных иконок, часов, BT и других выбранных элементов действительно применяется и восстанавливается. | `ОШИБКА`. |
| NATRO-011 | Для медиаплитки есть отдельное скрытие play/pause; прогресс поддерживает прямое перемещение. | `ТРЕБОВАНИЕ`. |
| NATRO-012 | Нужны любимые маршруты/кнопки маршрутов, триггер потери маршрута и геофильтр, где это технически возможно. | `ТРЕБОВАНИЕ`. |
| NATRO-013 | Иконки и элементы не должны показывать пользователю технические состояния «без лицензии», если функция доступна штатно. | `ТРЕБОВАНИЕ`. |
| NATRO-014 | Автоматическое восстановление специальных возможностей после загрузки выполняется без повторного Toast «Разрешения получены автоматически». Уже включённый компонент в полном или сокращённом Android-формате не переписывается и не считается новым разрешением. | `ОШИБКА` подтверждена фото 01.09.2026; исправление в коде, нужен холодный старт KX11 и два входа в настройки. |
| NATRO-015 | Повторное создание информационных плиток и других UI-потребителей не должно бесконтрольно расходовать лимит слушателей значений коннекторов. Плитки используют один мультиплексированный upstream-listener на экземпляр `WidgetService`, каждый прямой владелец отписывается при смене сервиса/остановке, а исчерпание защитного лимита отклоняет только лишнего подписчика и никогда не бросает исключение в главный поток Natro. | `В КОДЕ / НУЖЕН KX11`: hub и lifecycle-контракты сохранены; прежний `IllegalStateException: Too many connector value listeners` заменён безопасным отказом с диагностикой и unit-регрессией, требуется `GATE-021`. |
| NATRO-016 | Избранные маршруты лаунчера не исчезают и не теряют подписи при повторных snapshot/rebind и нажатии системной кнопки «Домой». Неизменившийся список сохраняет существующую view-иерархию и пользовательское состояние; HOME/Работа и пользовательские точки всегда имеют читаемую подпись под иконкой, а изменение размера иконки не схлопывает строку текста. | `ПОВТОРНАЯ ОШИБКА ПОСЛЕ 2.7.7`: 05.09 пользователь подтвердил периодическое исчезновение после «Домой». В подписанной 2.7.8 дополнен пересчёт проекций после реального layout дочерних view, включая неизменные bounds workspace; добавлена диагностика configured/routeActive/frames/visible без названий точек. Приёмка `GATE-044` остаётся открытой. |
| NATRO-017 | У HUD-виджетов остатка топлива и «Долить» доступны визуальные опции по IMG_8276.jpeg: жёлтый ниже (л), красный ниже (л), общий объём бака (л), режим объёма по умолчанию 64 л либо ручной, скрытие остатка выше порогов и показ «Долить» только в подтверждённом P. Начальный пресет нового профиля — 15/10/64 л; выбранный объём реально участвует в расчёте долива. Настройки общие для топливных виджетов этого HUD, подпись сообщает используемый объём/режим. «Отмена» не меняет состояние, применение обновляет редактор и HUD, экспорт/импорт и обновление APK сохраняют выбор; прежние отдельные параметры действуют до явного применения общих. Недоступное топливо не превращается в ноль или полный объём долива. | `В СОСТАВЕ ПОДПИСАННОЙ 2.7.9 / НУЖЕН KX11`: запрос пользователя 05.09.2026, эталон IMG_8276.jpeg; GATE-051 открыт. |
| NATRO-018 | Каталог и поведение HUD-виджетов сверять с переданным mHUD 6.1, SHA-256 `8ba8da9aea2b6072cd5ec3cb42fc145a82547dc48f5e7b1d9e705cfee9a25d2e`: публиковать таблицу соответствия и оставшихся различий. Наличие enum или отдельного значения не означает готовность составного виджета и всех его режимов. APK, извлечённые ресурсы/код и исходные пользовательские материалы не публиковать. | `АУДИТ 05.09.2026`: таблица [mHUD 6.1](docs/MHUD_6_1_WIDGET_AUDIT_2026_09_05_RU.md); оставшиеся различия открыты, полного паритета не заявляем. |
| NATRO-019 | Поворотники HUD — единый составной модуль, как парный блок mHUD 6.1: одна рамка выбора, общие положение/размер/цвет/видимость, две стрелки по сторонам. Левый и правый сигналы читаются независимо; аварийка показывает обе стрелки в общей фазе. Новое меню предлагает один элемент «Поворотники» вместо двух; сохранённые отдельные элементы совместимы и не удаляются автоматически. | `В СОСТАВЕ ПОДПИСАННОЙ 2.8.0 / НУЖЕН KX11`: единый тип TURN_SIGNALS, общая рамка и настройки, две read-only подписки, синхронная фаза аварийки. Старые типы импортируются, новое меню предлагает общий модуль. GATE-056 открыт. |

### 6.1 Пользовательская системная шторка

| ID | Требование | Статус / критерий |
|---|---|---|
| SHADE-001 | Вместо штатной верхней шторки KX11 нужна собственная шторка Natro, открываемая тем же жестом сверху вниз поверх лаунчера и других приложений. | `ОШИБКА НА KX11 02.09.2026`; в 2.6.3 стандартный жест не открывает панель. Исправление нельзя считать готовым до аппаратной проверки. |
| SHADE-002 | Состав и геометрия новой шторки редактируются по принципу лаунчера: пользователь добавляет нужные элементы, перемещает и масштабирует их, меняет порядок, видимость и оформление. | `РЕАЛИЗОВАНО В КОДЕ / НЕ ПРОВЕРЕНО НА KX11`; часы, дата, медиа, яркость, громкость и отдельный каталог действий имеют независимую геометрию/оформление и живой редактор. |
| SHADE-003 | Штатная шторка не должна показываться одновременно с Natro или кратко мигать перед ней. Отключается только доказанно относящийся к шторке компонент ECARX; запрещено вслепую отключать весь SystemUI, системную строку либо автомобильные защитные окна. | `В РАБОТЕ`; диагностика 01.09.2026 определила владельца окна как `ecarx.notificationcenterui`, но точный компонент ещё не установлен. |
| SHADE-004 | Отключение штатной шторки должно быть обратимым: перед изменением сохраняется исходное состояние пакета/компонента, предоставляется отдельная команда восстановления, а при неудачном запуске Natro штатная панель может быть возвращена без сброса магнитолы. | `ЧАСТИЧНО / НУЖЕН АУДИТ KX11`; добавлены read-only `kx11-shade-audit.sh` и строгий exact-component disable/restore, применение заблокировано до получения имени компонента. |
| SHADE-005 | Пользовательская шторка запускается после холодной загрузки, плавно открывается сверху вниз, закрывается обратным жестом/Back и не блокирует сенсор после закрытия. | `ОШИБКА НА KX11 02.09.2026`; после попытки открыть 2.6.3 невидимое окно блокирует все касания внутри Natro. Нужны 50 аппаратных циклов после исправления. |
| SHADE-006 | Новая шторка обязана уступать приоритет штатным окнам 360/парктроника, звонка, критическим автомобильным предупреждениям и выключению экрана; её показ не должен менять существующие правила паузы уведомлений. | `РЕАЛИЗОВАНО В КОДЕ / НЕ ПРОВЕРЕНО НА KX11`; используется общее итоговое удержание камеры и парковочного окна (`NOTIF-009`); очередь уведомлений не получает третьего источника паузы. Нужен тест 360/звонка/критических окон. |
| SHADE-007 | Текущий APK Natro работает как обычное приложение UID `10081`, имеет `SYSTEM_ALERT_WINDOW`, но диагностика сообщает `root_available=false`; поэтому бесшовное отключение ECARX-компонента допускает одноразовую подготовку с root ADB либо установку Natro как привилегированного приложения. Имитация замены через мгновенное закрытие уже открывшейся штатной шторки не считается готовым решением. | `В РАБОТЕ / ИССЛЕДОВАНИЕ`; root ADB с компьютера ранее подтверждён в `DRIVE-003`. |
| SHADE-008 | Закрытая шторка никогда не оставляет полноэкранное touch-окно. Во время распознавания жеста окно остаётся узкой верхней полосой; расширение допускается только после принятого решения открыть. `CANCEL` без достигнутого порога, короткий тап, неудачный свайп, подавление автомобильным окном и закрытие всегда физически возвращают высоту окна к зоне жеста, даже если логическое состояние уже было `closed`. Если KX11 завершает настоящий стандартный свайп событием `CANCEL` после достаточного движения вниз, измеренный жест допускается принять как открытие. | `В КОДЕ ПОСЛЕ ОШИБКИ / НУЖЕН KX11`; `SystemShadeRootLayout` и gesture-policy содержат обработку CANCEL и восстановление узкой области; добавлено после критического аппаратного дефекта 2.6.3. Критерий — 50 неудачных/отменённых жестов и 50 открытий/закрытий без единого заблокированного касания. |

## 7. Уведомления, камера 360 и парктроники

| ID | Требование | Статус / критерий |
|---|---|---|
| NOTIF-001 | Уведомления не теряются и не приходят с многоминутной задержкой из-за общей очереди или залипшей паузы. | `ОШИБКА`. |
| NOTIF-002 | Пауза уведомлений допускается только от двух настроенных источников: 360/парктроники и выбранные пользователем приложения. Общая пауза «при любом overlay» удаляется. | `ТРЕБОВАНИЕ`. |
| NOTIF-003 | При показе 360/парктроника новые уведомления принимаются в очередь, но не перекрывают автомобильное изображение. После закрытия показ возобновляется. | `ОШИБКА`; проверять перекрывающиеся удержания. |
| NOTIF-004 | Если 360 и парктроник создают два удержания, закрытие одного не возобновляет показ преждевременно, а закрытие последнего обязательно снимает паузу. | `ТРЕБОВАНИЕ`; нужен reference-count/token test. |
| NOTIF-005 | Уведомления аккумулятора и внутренние уведомления Natro проходят через надёжный диспетчер и не теряются даже без ANCS. | `ОШИБКА`. |
| NOTIF-006 | Одновременные уведомления показываются FIFO; позиция оверлея может быть зафиксирована. | `ТРЕБОВАНИЕ`; ранее зафиксированный интервал очереди — 1 с на элемент, пока пользователь не изменит. |
| NOTIF-007 | После отключения парктроника показ уведомлений должен продолжаться автоматически, без перезагрузки Natro. | `ОШИБКА / УТОЧНЕНО NOTIF-009`: уведомления должны продолжаться уже после исчезновения графики, даже когда датчики остаются включёнными. |
| NOTIF-008 | Удержание 360/парктроника определяется штатными сигналами KX11: `SwtDispOnAndOffStsResp` 29021, `VisnImgDispModResp` 29043 и прежде всего `PrkgDstCtrlSts` 28995 (`2/3` — активно, `1` — закрыто). Медленный сигнал одного источника не должен преждевременно снять удержание другого. | `ОШИБКА / ЗАМЕНЕНО NOTIF-009`: после закрытия парковочного интерфейса уведомления остаются на паузе до принудительного выключения системы; значения `2/3` не доказывают видимость окна. |
| NOTIF-009 | Пауза от парктроника определяется фактически видимым парковочным окном на основном дисплее, а не включением датчиков или `PrkgDstCtrlSts=2/3`. Источник — штатный список окон ECARX с точным владельцем `com.ecarx.parking`, видимостью и пересечением границ с экраном; UID системного процесса, произвольный overlay, имя похожего пакета и окно на HUD/DIM не являются подтверждением. Закрытие подтверждается повторным свежим снимком. Пользователь отдельно запретил снимать паузу по ограниченному сроку/TTL: время само по себе не означает закрытие. Ошибка чтения является неизвестным состоянием; канал восстанавливается, затем запрашивается фактическое состояние. Возвращённый ECARX повторно тот же массив окон после скрытого `RemoteException` не является свежим подтверждением; проверка идентичности массива сохраняется при переподключении. Независимое подтверждённое удержание камеры 360 и выбранные пользователем приложения сохраняются. Очередь и оставшееся время карточки возобновляются после снятия последнего удержания без выключения парковочной системы. | `В СОСТАВЕ ПОДПИСАННОЙ 2.7.7 / НУЖЕН KX11`; 64 локальные проверки и Android CI `33917088264` прошли; выпускной CI `33918568644` выполнил unit-тесты и сборку 2.7.7; аппаратный `GATE-046` открыт. |

## 8. ANCS и iPhone Helper

| ID | Требование | Статус / критерий |
|---|---|---|
| ANCS-001 | После обновления Natro ANCS должен восстанавливаться без перезагрузки магнитолы и без повторного сопряжения. | `ОШИБКА НА 2.7.7` 05.09.2026: после package replacement в 14:28:05 журнал до 14:30:05 не содержит READY; обычное HFP-подключение не означает готовность ANCS. |
| ANCS-002 | Сохраняется один владелец обратного ANCS GATT-канала; Helper не создаёт конкурирующее второе BLE-подключение после handoff. | `ТРЕБОВАНИЕ`. |
| ANCS-003 | Длительный «тихий» владелец не должен задерживать вход в scan примерно на четыре минуты; повторные ступени поиска должны быть короткими и ограниченными. | `ОШИБКА` 05.09.2026: точные LE advertisements постоянно опережают таймер и не увеличивают счётчик reassert, поэтому зарегистрированный owner не достигает уже предусмотренного retirement. Таймер сам по себе не даёт разрешения создать второго владельца. |
| ANCS-004 | Ошибки `encryption key size is insufficient` и `handle is invalid` должны приводить к безопасному восстановлению, а не вечному зависанию. | `ОШИБКА`. |
| ANCS-005 | Телеметрия iPhone: точная батарея, зарядка, блокировка, тип сети и сигнал обновляются без минутной задержки. | `ОШИБКА`. |
| ANCS-006 | В Natro и Helper есть расширенный журнал этапов подключения, причин backoff/quarantine и экспорта диагностик. | `ТРЕБОВАНИЕ`. |
| ANCS-007 | Экспорт логов на магнитоле должен работать даже при отсутствии стороннего файлового приложения. | `ОШИБКА`. |
| ANCS-008 | Helper сохраняет управление климатом/функциями из Live Activity. | `ОШИБКА` в отдельных сборках. |
| ANCS-009 | Live Activity автоматически активируется только при подключении магнитолы по выбранному Bluetooth и выключается после отключения. | `ТРЕБОВАНИЕ`. |
| ANCS-010 | Взаимодействие с кнопками Live Activity не должно обнулять состояние управления; ограничение iOS на время гашения экрана фиксируется отдельно и не выдаётся за исправляемое Android-кодом. | `ТРЕБОВАНИЕ / ОГРАНИЧЕНИЕ ПЛАТФОРМЫ`. |
| ANCS-011 | Helper определяет LTE/5G/3G/2G только по активной линии мобильных данных (`dataServiceIdentifier`); неактивная SIM с 2G не может переопределять активную LTE/5G, смена data-SIM обновляется сразу. | `В КОДЕ / НУЖЕН IPHONE/KX11`; Helper 70. |
| ANCS-012 | Natro остаётся авторитетным источником состояния HFP/A2DP/PBAP; Helper не сбрасывает глобальный Bluetooth, не удаляет системную пару и не подменяет идентичность телефона ради восстановления ANCS. | `ТРЕБОВАНИЕ`; проверять один видимый телефон и один владелец ANCS. |
| ANCS-013 | Внутри одной проверенной BLE-эпохи восстановление соединения сбрасывает временную аутентификацию и подписки, сохраняя уже загруженный проверенный enrollment snapshot до завершения эпохи. Ошибка GATT не должна превращаться в ложное требование повторного сопряжения. Новая эпоха заново загружает запись вне main thread и проверяет выбранную пару и обе установки; доверие не расширяется. | `В ПОДПИСАННОЙ 2.7.8 / НУЖЕН KX11`: RESET_SESSION_STATE теперь очищает handshake отдельно от enrollment snapshot; новая эпоха/остановка сохраняют полный reset. В старом логе transient C4 error сменялся ложным no exact enrollment record. |

## 9. Яндекс Музыка и управление медиа

| ID | Требование | Статус / критерий |
|---|---|---|
| MUSIC-001 | Задержка автозапуска из настройки должна соответствовать реальному времени. При выбранных 3 с воспроизведение не должно начинаться через 1–3 минуты. | `ОШИБКА`. |
| MUSIC-002 | Предварительный запуск Яндекс Музыки выполняется в фоне без открытия Activity/интерфейса приложения. | `ТРЕБОВАНИЕ`. |
| MUSIC-003 | Само приложение Яндекс Музыки не должно самопроизвольно открываться при загрузке магнитолы. | `ОШИБКА`. |
| MUSIC-004 | Безопасный prewarm не отправляет PLAY раньше пользовательской задержки; в момент запуска используется готовая media session. | `ТРЕБОВАНИЕ`. |
| MUSIC-005 | После старта воспроизведение не должно сразу переходить на паузу из-за конкурирующих media-button попыток. | `ОШИБКА`. |
| MUSIC-006 | Кнопки руля и управление медиа становятся доступны быстро и не ждут инициализацию HUD-карты или других тяжёлых подсистем. | `ОШИБКА В 2.6.8 НА KX11`: пользователь повторно подтвердил ощутимую задержку кнопок руля; код прямой отправки включён начиная с 2.6.9 (`MUSIC-010`); аппаратный `GATE-017` остаётся открытым. |
| MUSIC-007 | Реализацию сравнивать с предоставленными `mSaver 2.6/2.7 HOME`, используя только допустимое исследование поведения. | `ТРЕБОВАНИЕ`. |
| MUSIC-008 | Лог запуска содержит время boot/unlock, prewarm, обнаружения сервиса/session, первой команды PLAY и фактического состояния playback. | `ТРЕБОВАНИЕ`. |
| MUSIC-009 | Запуск выполняется через целевую MediaSession Яндекс Музыки; запрещены глобальные media-key команды, которые могут запустить или поставить на паузу другой активный проигрыватель. Поиск сессии ограничен тайм-аутом и не блокирует HOME. | `ОШИБКА` в прежних сборках; текущий путь нужен проверить по временному логу KX11. |
| MUSIC-010 | Каждое нажатие предыдущего/следующего трека и play/pause на руле передаётся активной MediaSession без накопления в общей очереди, ожидания запуска карты, HUD, панели приборов, ANCS или фонового поиска сессии. Повторные нажатия не должны исполняться пачкой с задержкой. | `В КОДЕ 2.6.9 / НУЖЕН KX11`: accessibility key-filter отправляет команду прямо в заранее выбранную точную MediaSession; поиск сессии и запись диагностик вынесены из пути нажатия. Если точной сессии нет, событие остаётся штатной системе. Требуется `GATE-017`. |
| MUSIC-011 | Виджет громкости HUD показывает реальные деления системной медиагромкости, а не проценты и не обратный пересчёт из процентов; содержит иконку динамика (mute при нуле). В работе появляется только после фактического изменения громкости, затем скрывается после короткого интервала от последнего изменения. Первый снимок/запуск, повтор одинакового значения и смена трека не вызывают и не продлевают показ. В редакторе элемент остаётся видимым/выбираемым. | `В СОСТАВЕ ПОДПИСАННОЙ 2.7.9 / НУЖЕН KX11`: запрос и IMG_8277.MOV, 05.09.2026; начальная длительность 2 с, GATE-052 открыт. |
| MUSIC-012 | Медиаэлементы HUD скрываются при остановке/паузе воспроизведения, в том числе вызванной телефонным звонком, и возвращаются только при реальном возобновлении. Обложка, общий текст, название, исполнитель, альбом, приложение и таймер используют одну политику; редактор остаётся видимым. Громкость имеет отдельный краткий показ MUSIC-011. Не отправлять PLAY при завершении звонка и не уничтожать media session/настройки. | `В СОСТАВЕ ПОДПИСАННОЙ 2.7.9 / НУЖЕН KX11`: в переданном mHUD подтверждено условие is_playing, а не самостоятельный источник состояния звонка. Сценарий реального звонка проверяет GATE-054; сохранённое playing=true само по себе не является доказательством отсутствия звонка. |

## 10. Круиз-контроль и ограничитель скорости KX11

| ID | Требование | Статус / критерий |
|---|---|---|
| DRIVE-001 | В перспективе включать/выключать лимитер и круиз-контроль со штатной ГУ и задавать выбранную скорость, сохраняя штатную индикацию приборки/HUD. | `ТРЕБОВАНИЕ / ИССЛЕДОВАНИЕ`. |
| DRIVE-002 | Любая запись в шину допускается только после доказанного безопасного протокола, проверки модели/года и обратимого стендового теста. | `ЗАБЛОКИРОВАНО` до подтверждения команд. |
| DRIVE-003 | Для диагностики доступен root ADB через компьютер; отсутствие app-level root не считать окончательным ограничением. | `ПОДТВЕРЖДЕНО`. |

## 10.1 Цифровой справочник прошивки Geely

| ID | Требование | Статус / критерий |
|---|---|---|
| KNOW-001 | Повторно разобрать все доступные дампы и логи прошивки Geely и создать полноценный цифровой справочник проекта: какие функции автомобиля существуют, через какие компоненты и API читаются и управляются. Особый приоритет — функции, чей механизм пока не установлен. | `В РАБОТЕ`; опубликована редакция 1.0 [базы знаний](GEELY_KX11_KNOWLEDGE_RU.md) и её каталоги из коммита `a2174b7`. Указаны охват, ограничения и 14 открытых вопросов. Полнота всех механизмов и аппаратная проверка не объявлены завершёнными; `GATE-048` открыт. |
| KNOW-002 | Справочник хранится в публичном `AGKulikov/Status` рядом с `PROJECT_REQUIREMENTS_RU.md`, содержит ссылки на требования и обновляется при новых доказательствах и исправлениях. | `ПРОВЕРЕНО ПО АРТЕФАКТУ` для размещения: корневой [GEELY_KX11_KNOWLEDGE_RU.md](GEELY_KX11_KNOWLEDGE_RU.md) опубликован рядом с реестром (`REL-016`). Ранее намеченное имя `GEELY_KX11_DIGITAL_MANUAL_RU.md` заменено фактическим именем опубликованного документа. Актуализация продолжается; ограничения `REL-008` сохраняются. |
| KNOW-003 | Для каждого механизма справочник различает подтверждённый факт, гипотезу и неизвестное; фиксирует версию/компонент, источник в дампе/логе, точку входа, единицы и значения, ограничения и результат реальной проверки. Включение подсистемы не принимается за доказательство отображения её UI. | `КРИТЕРИЙ ИСПОЛНЕНИЯ KNOW-001`; парковочная ошибка `NOTIF-008/NOTIF-009` — обязательный пример такого разграничения. Команды движения остаются под `DRIVE-002`. |
| KNOW-004 | Вести расширяемую матрицу охвата всех систем целевого Geely KX11 и журнал исследования. Для каждой системы фиксировать подтверждение наличия оборудования и ECU/владельца, имеющиеся и отсутствующие источники, состояния и команды, путь доступа из ГУ, условия/ограничения, способ независимого подтверждения и следующий недостающий факт. Различать неизвестное наличие, неизученную область и недоступный в точно исследованной области путь. Прежние 14 открытых вопросов сохраняются как приоритетная очередь и не считаются исчерпывающим перечнем. Сначала использовать уже полученные дампы/логи; новые данные запрашивать только для конкретного зафиксированного пробела. | `В РАБОТЕ`; подготовлены [матрица охвата](docs/geely-kx11/research-2026-09-05/COVERAGE_RU.md), [журнал исследования](docs/geely-kx11/research-2026-09-05/RESEARCH_LOG_RU.md) и [план недостающих данных](docs/geely-kx11/research-2026-09-05/DATA_PLAN_RU.md). Первичный каталог деклараций и отдельный аудит нулевых значений расширяют доказательную базу, но не подтверждают полноту всех систем, наличие конкретного оборудования или физическое исполнение команд; `GATE-048` остаётся открытым. |
| KNOW-005 | Продолжать доскональный разбор относящегося к задаче доступного корпуса до исчерпания его содержимого; публикация промежуточных результатов не является завершением исследования. Сначала исчерпывать уже полученные файлы, затем для конкретно установленного пробела подготовить адресный скрипт досбора недостающих файлов на macOS и инструкцию запуска. Итог фиксирует точные источники, SHA-256, область проверки, проверенные результаты и статусы недостающих/недоступных данных; количество записей и файлов не превращается в процент понимания автомобиля. | `В КОДЕ / НУЖЕН KX11`; исходники и49 офлайн-проверок адресного сборщика готовы, дальнейшее исследование продолжает `KNOW-004`. Инструкция и исходники сборщика — [tools/geely-macos-collector/README_RU.md](tools/geely-macos-collector/README_RU.md); `GATE-049` открыт. Исчерпание доступных материалов не закрывает аппаратные вопросы и `GATE-048`. |

## 11. Текущие обязательные проверки перед следующим APK

| ID | Проверка | Ожидаемый результат |
|---|---|---|
| GATE-001 | Java-тест единиц камеры | MapKit `60/3.6 → 60`, `25 → 90`; HUD `KPH/MPH/MPS` нормализуются. |
| GATE-002 | Сравнение Navigator dex | Новый `classes19.dex` содержит `CameraSpeedNormalizer`, `RouteTurnMapLayer` и настройки направления камеры. |
| GATE-003 | Boundary baseline | В Navigator меняются только разрешённые manifest/hooks/dex entries. |
| GATE-004 | Подпись обновления | Для каждого реально выдаваемого APK пакет и сертификат совпадают с предыдущим нашим опубликованным APK того же приложения. У Natro versionCode строго возрастает; для 2.7.7 сравнение выполняется с 2.7.6 / 208021309. Неизменённый Navigator не пересобирается только ради номера Natro. |
| GATE-005 | Проверка на KX11 | Светофоры/секунды, полосы сбоку, 60/90, единый знак, отсутствие дублей, направление, штатные названия улиц по MAP-024, стрелки поворотов, fixed zoom, верх окна и левая кнопка. |
| GATE-006 | Честный отчёт | Для каждого пункта отдельно указать: проверено автоматически, проверено на KX11 или ещё не проверено. |
| GATE-007 | Длительная стабильность Navigator | По 30 минут без маршрута и с маршрутом, три возврата из фона; нет завершения bridge, исчезновения окна или возврата на HOME. |
| GATE-008 | Тихое восстановление accessibility | После холодной загрузки и двух входов в настройки разрешение действует, success Toast accessibility не появляется. |
| GATE-009 | Холодный старт карты панели | После перезапуска Natro/Navigator карта появляется на панели без единого входа в настройки; повторная публикация surface после изменения размера также работает. |
| GATE-010 | Длительное отсутствие мерцания HUD/панели | HUD и панель по отдельности работают не менее 30 минут без пустых/чёрных кадров с маршрутом и без маршрута; изменение позиции, пробок и surface не пересоздаёт renderer. |
| GATE-011 | Видимые штатные стрелки поворотов | На реальном маршруте видны не менее трёх последовательных стрелок штатного стиля непосредственно на актуальной полилинии; положение соответствует манёврам, после завершения маршрута стрелки очищаются. |
| GATE-012 | Штатные названия улиц MapKit | На HUD и панели приборов подписи имеют штатный вид Яндекса, следуют изгибу дорог, масштабируются настройкой, не заменяются отдельными bitmap-карточками и остаются читаемыми поверх/около линии маршрута. Проверить с маршрутом и без него, в обычном и прозрачном режиме «Только дороги». |
| GATE-013 | Оформление стрелок поворотов | Для HUD и панели приборов отдельно проверить сохранение и применение двух разных цветов, толщины обводки, длины тела 10/50/100/250% и размера треугольного наконечника 10/50/100/250%. Изменение длины не меняет установленный размер наконечника, изменение наконечника не меняет длину; ширина тела и толщина обводки не меняются. При изменении толщины маршрутной линии ширина стрелки следует за ней. Сброс цвета возвращает штатный цвет Яндекса. |
| GATE-014 | Сектор направления и компактный/составной знак камеры | Для HUD и панели приборов отдельно проверить сохранение и применение разных длины, ширины основания, цвета и прозрачности сектора. При изменении ширины обе крайние точки основания расходятся поперёк направления, но вершина и продольная дальность не двигаются; при изменении длины ширина основания сохраняется. Обычная камера с известной скоростью — один компактный круг с числом без маленькой камеры и большого прозрачного поля. На одной физической камере «контроль полос + скорость» должен появиться один составной знак из штатной синей пластины и круга скорости, как на `IMG_8208.jpeg`, без мини-камеры на круге и без второго соседнего маркера. Для двунаправленной камеры видны оба сектора. Камера без скорости сохраняет один штатный знак камеры. На масштабах 50/75/100% число скорости, красная окантовка и штатная пластина остаются чёткими, а фактический размер и collision-рамка уменьшаются вместе с ползунком. |
| GATE-015 | Изменение размеров HUD-карты без растягивания | Последовательно изменить только ширину, только высоту и обе стороны HUD-карты. После каждого изменения геометрия дорог, подписи, курсор и маршрут перекомпонованы новым viewport, круги остаются кругами; старый кадр не растянут, пустого/чёрного кадра и мерцания нет. Повторить во время активного маршрута. |
| GATE-016 | Верх окна и двусторонняя кнопка режима | В оконном режиме сравнить верхний край карты, карточку манёвра и правые элементы с `IMG_8199.jpeg`/`IMG_8200.jpeg`, затем с `IMG_8215.jpeg`/`IMG_8216.jpeg`: повторного системного отступа нет в свободном режиме и при активном ведении ни сразу, ни после 30 с и нескольких перестроений Guidance. После каждого из 10 тапов одна кнопка появляется в одной и той же координате слева непосредственно под дорожным событием и Алиской/голосом; не попадает на карточку манёвра и не прыгает при смене режима. Нажать её окно→полный экран, снова тапнуть карту и той же кнопкой выполнить полный экран→окно. Справа сверху и поверх дороги дубликатов нет; после автоскрытия штатный блок и карта сохраняют работу. |
| GATE-017 | Задержка кнопок музыки на руле | После холодной загрузки, во время запуска HUD/панели и после полного прогрева выполнить по 10 нажатий previous/next/play-pause. В журнале для каждого нажатия сопоставить время входного ECARX-события, выбора MediaSession, отправки команды и изменения playback/metadata; команды не копятся, не меняют чужой проигрыватель и визуально выполняются без ощутимой задержки. |
| GATE-018 | Свободная раскладка и хвостики | На загруженном перекрёстке с табличкой полос, минимум двумя светофорами и камерой проверить HUD и панель при движении, повороте и двух уровнях zoom. Полосы и светофоры выбирают разные свободные стороны, остаются внутри viewport и не складываются в одну кучу; фиксированная камера остаётся центром на своей координате маршрута, а подвижные карточки обходят её. Каждый светофор расположен сбоку, его хвост указывает на собственную точку маршрута. Штатная табличка полос также имеет хвост, который при переносе меняет сторону и остаётся привязан к `RoutePosition`. Сверить с `IMG_8210.png`, `IMG_8206.jpeg`, `IMG_8214.jpeg` и `IMG_8219.jpeg`; отсутствие хвоста или смещение камеры с точки — провал. |
| GATE-019 | Повторная сверка приоритетов всех слоёв | Составить фактическую таблицу штатного порядка Navigator 30.3.0 и порядка независимых карт Natro после `MAP-030…033`; проверить автоматический режим и крайние значения ручного порядка отдельно на HUD и панели. На маршруте и без него ни один новый слой не перекрывает курсор, обязательный манёвр, маршрут впереди или Guidance-карточку вопреки штатной логике; подписи и стрелки не прячутся под дорогами, сектора камер остаются под своими знаками, а взаимное размещение полос/светофоров/камер не меняет правильный порядок их отрисовки. Обычные светофоры маршрута по MAP-051 находятся сразу над маршрутной линией и подписями улиц, ниже остальных навигационных обозначений; светофоры с секундами проверяются отдельно. Любое расхождение сначала исправляется и повторно проходит статический контракт, затем подтверждается на KX11. |
| GATE-020 | Чёткость всех масштабируемых значков | Отдельно на HUD и панели приборов снять близкие кадры при 50/75/100% для камеры со скоростью и составной пластиной, светофора с секундами, таблички полос, дорожного события, конечной точки и курсора. Контуры, цифры, пиктограммы и хвосты остаются чёткими без ступенек, ореолов и размытия; физический размер и collision-рамка совпадают с выбранным масштабом. Проверка исходников подтверждает, что каждый размер получен из vector/drawable или штатного renderer, а не из уже уменьшенного bitmap. |
| GATE-021 | Регрессия известных падений | Статический/DEX-аудит отклоняет `NavigationLayerFactory`, `NavigationLayerSettings`, `createNavigationLayer`, `setUseLayerCamera`, `setUseLayerRoadEvents`, `setRoadEventVisibleOnRoute` и `GuidanceCamera` в добавленном Natro-модулем DEX Navigator; hook оконного режима остаётся в конце `onResumeFragments`, несвязанные конфигурации не повторяют type-2038 `setAttributes`, а отложенные poll/inset callbacks имеют локальную аварийную границу. Unit-тест заполняет весь лимит connector-listener и доказывает, что следующий подписчик не завершает процесс. Аппаратная часть не заменяет `GATE-007`: по 30 минут без маршрута и с маршрутом плюс три возврата из фона. |
| GATE-022 | Аппаратная регрессия исправлений 03.09.2026 | На одиночных камерах, камерах без известной скорости и составных знаках центр совпадает с `worldToScreen` исходной точки и вершиной сектора, а на маршрутных камерах остаётся на линии при движении/zoom. Переключить свободный режим ↔ активное ведение не менее 5 раз: дополнительный верхний отступ Guidance не возвращается. Выполнить 10 тапов карты в обоих состояниях: координата кнопки режима не меняется более чем на 1 px и всегда остаётся третьей слева под штатной парой. Сверить с `IMG_8214.jpeg`, `IMG_8219.jpeg`, `IMG_8215.jpeg`, `IMG_8216.jpeg`, `IMG_8220.jpeg`–`IMG_8223.jpeg`. |
| GATE-023 | Внешний inset активного Guidance | На физическом KX11 в оконном режиме запустить маршрут и сверить с провальным `IMG_8227.jpeg`: верх карты, карточка манёвра и группа текущей/разрешённой скорости начинаются у одной верхней области окна, при этом у двух верхних виджетов остаётся только штатный отступ 12 dp, а не полный глобальный status-bar inset. Проверить сразу после входа, через 30 с, после пяти переключений свободный режим ↔ ведение, перестроения маршрута и возврата из фона. Убедиться, что поднялись все верхние Guidance-элементы вместе, нижняя ETA-панель не сместилась, полноэкранный режим не изменился и 30-минутный прогон `GATE-007` проходит без падения. |
| GATE-024 | Штатный левый контейнер кнопки режима | В свободном режиме и при активном Guidance по 10 раз тапнуть карту и сверить с `IMG_8221.jpeg`: существует ровно одна кнопка режима, она является третьим визуальным элементом того же вертикального контейнера сразу после штатной обёртки дорожного события и кнопки Алисы/голоса и перемещается/появляется/исчезает вместе с ним. Форма, фон, размер, padding, pressed-state, alpha и scale визуально совпадают с соседними штатными кнопками; отдельного круглого чёрного overlay, скачка координаты или собственного запаздывающего fade нет. Выполнить окно→полный экран→окно, пять смен свободный режим ↔ Guidance и перестроение маршрута; после каждой замены кнопка кликабельна, старого экземпляра в отсоединённом дереве и дубля поверх карты нет. |
| GATE-025 | Штатная реакция окна карты на тап | В оконном и полноэкранном режимах, отдельно в свободном режиме и при активном Guidance, выполнить по 10 одиночных тапов по свободной области карты. Каждый тап штатно переключает видимость vanishing/Guidance-контролов, а кнопка режима появляется/исчезает вместе с точным левым rail без отдельного цикла. Затем проверить pan и pinch: жесты доходят до карты, не обрываются при rebind кнопки и не активируют ручки Natro вне их видимых границ. Сверить с поведением baseline 30.3.0 и провалом `IMG_8228.jpeg`. |
| GATE-026 | Маршрутно-зависимое размещение и ножки светофоров | На HUD и панели приборов пройти прямой участок, левый/правый поворот, S-образный участок и развязку при 50/75/100% масштаба карточек. Для полос и светофоров зафиксировать кадры до, во время и после поворота: карточка не закрывает видимый маршрут впереди, не пересекает курсор/центрированную камеру, остаётся в viewport и не прыгает между сторонами без реального конфликта. У каждого светофора видна непрерывная ножка от плашки до точной точки события при всех восьми положениях; её конец совпадает с `worldToScreen`, а при pan/zoom/tilt связь не расходится. Сверить провал `IMG_8233.jpeg`, штатный эталон `IMG_8229.jpeg` и отсутствие ножки на `IMG_8230.jpeg`; 30-минутный прогон `GATE-007` обязателен. |
| GATE-027 | Реальный верх активного Guidance после 2.7.2 | На физическом KX11 в оконном режиме измерить зазор от верхнего края видимого map viewport до верхней границы карточки манёвра и `speed_group`: он равен штатному baseline-отступу, без большого пустого пояса как на `IMG_8236.jpeg`. Проверить сразу, через 30 с, после перестроения, пяти смен свободный режим ↔ Guidance и возврата из фона. Оба верхних элемента перемещаются как одна группа; нижняя ETA и полноэкранный режим не меняются. |
| GATE-028 | Полнота набора светофоров | Для одного маршрута сопоставить snapshot событий Navigator с placemark на HUD и панели приборов: для каждого одновременно валидного светофора существует ровно один объект с той же route identity/позиционной меткой. Проверить два и более близких светофора, одинаковые секунды, смену цвета, pan/zoom, перестроение и прохождение первого события. Collision solver не уменьшает количество объектов; исчезновение допускается только после удаления из нового snapshot, прохождения или смены route epoch. |
| GATE-029 | Искусственные неровности и маршрутные ограничения | `ЗАМЕНЕНО GATE-030`: первоначальная проверка включала дополнительный слой ограничений скорости, который пользователь отклонил. |
| GATE-030 | Искусственные неровности без дублей лимита | На одном маршруте с несколькими искусственными неровностями отдельно включить/выключить слой на HUD и панели приборов. Количество и порядок соответствуют `DrivingRoute.getSpeedBumps()`; хвост каждого знака стоит в точке, полученной `pointByPolylinePosition()`, знак исчезает после прохождения/смены маршрута и не дублирует камеры или текущий лимит. При 50/75/100/150% штатное векторное изображение остаётся чётким, физический размер и footprint совпадают, pan/zoom/поворот не сдвигают хвост с линии. Проверить, что `getSpeedLimits()` и глобальные `DANGER` не создают новый слой, а приоритет неровностей участвует в автоматическом и ручном порядке. Повторить при перестроении и после возврата из фона; `GATE-007/GATE-021` подтверждают отсутствие аварийного `NavigationLayer`. |
| GATE-031 | Отдельный прогноз пробки | На одном маршруте сопоставить значение штатной плашки Navigator с bridge snapshot и двумя отдельными модулями: длительность и расстояние совпадают с `leftInTrafficJam()`, формат читаем на русском, общий ETA/длина маршрута не используются. Независимо включить, разместить, уменьшить и оформить модуль на HUD и панели. При `leftInTrafficJam()==null`, остановке/перестроении маршрута и спустя не более 3 секунд без свежего snapshot оба модуля исчезают целиком без пустой подложки; при новом forecast появляются снова только при выполнении актуального stock visibility-gate `MAP-047/GATE-033`; одного ненулевого значения недостаточно. Проверить возврат из фона и 30-минутный прогон `GATE-007`. |
| GATE-032 | Цвет светофорной плашки и повторная ревизия чёткости | Отдельно для HUD и панели выбрать минимум светлый, тёмный и насыщенный ARGB-цвет. Тело плашки и вся ножка до `worldToScreen` имеют выбранный цвет, а сигнал, секунды, стрелка направления, anchor, географическая точка и collision-рамка не меняются; после перезапуска сохраняются разные цвета двух профилей. Затем при 50/75/100/150% снять близкие кадры всех типов из `MAP-046`: отсутствуют мыло, повторная интерполяция, обрезание и несоответствие footprint. Статический аудит подтверждает путь от vector/drawable/штатной фабрики или единственного исходного Yandex bitmap прямо к текущему физическому размеру. |
| GATE-033 | Точное совпадение видимости пробки | На одном маршруте записать штатный `StatusPanel`, `leftInTrafficJam()` и оба наших модуля при forecast меньше и больше 4 минут, при скрытии штатных контролов, приоритетном сообщении, перестроении и окончании пробки. HUD и панель показывают строку только в кадрах, где штатно выбрана плашка пробки, и скрывают её не позднее следующего snapshot; ненулевой residual forecast сам по себе ничего не рисует. Повторить возврат из фона и потерю свежего bridge-состояния. |
| GATE-034 | Атомарность камеры и направления | На HUD и панели пройти камеры с одним и двумя направлениями, со скоростью и без неё, у края viewport и рядом с конфликтующими объектами. Для камеры с подтверждённым направлением на каждом видеокадре видимость сектора согласована с видимостью её знака с той же identity; камера без данных направления сохраняет самостоятельный знак, сектор не выдумывается; после прохождения, удаления, смены route epoch и TTL оба исчезают одновременно. Кадр с одиночным треугольником, как на `IMG_8250.jpeg`, является провалом. |
| GATE-035 | Единый кадр манёвра без старого знака | На последовательности прямой → правый → левый → съезд сопоставить stock action/image/text/distance с bridge snapshot, HUD и панелью на каждом обновлении, включая момент временно отсутствующего bitmap и перестроение маршрута. Все части имеют один maneuver key и меняются атомарно; старый bitmap не переживает смену ключа ни одного кадра. Сверить `IMG_8255.jpeg`/`IMG_8261.jpeg`. |
| GATE-036 | Полная структура штатной карточки | На маршрутах с обычным названием следующей дороги, цветными `DirectionSignItem`, нумерованным съездом/поворотом, следующим манёвром и следующими полосами сравнить штатную карточку с HUD и панелью. Основной знак/дистанция, отдельные указатели с исходными цветами и условный auxiliary-блок совпадают по значениям и порядку; отсутствующие блоки схлопываются. Обязательный эталон — «Подольск» + «2-й съезд» на `IMG_8251.jpeg` против провала `IMG_8253.jpeg`. |
| GATE-037 | Обычные светофоры без отсчёта | На маршруте сопоставить весь `DrivingRoute.getTrafficLights()` со знаками HUD и панели при отсутствии живого сигнала, затем дождаться `Windshield.getTrafficLightsWithSignal()` для одной и той же точки. До живых данных виден ровно один штатный day/night маршрутный знак, при появлении сигнала он атомарно заменяется одной цветной плашкой с секундами либо без них, без двух соседних объектов и пустого кадра; после исчезновения сигнала обычный знак возвращается только если точка ещё не пройдена. Независимые переключатели, масштабы 50/75/100/150% и приоритеты двух карт сохраняются после перезапуска. После прохождения, перестроения, окончания маршрута и смены route epoch старые знаки отсутствуют; контуры остаются чёткими и совпадают с исходным штатным drawable. |
| GATE-038 | Меню поверх панели приборов Natro | Запустить полную панель приборов Natro, перейти на штатную DIM-вкладку «Навигация» и открыть меню кнопками руля. Меню остаётся поверх панели, прокрутка и подтверждение работают; закрытие/повторный запуск панели не оставляет дублей. Затем открыть mNavi, штатный звонок/медиаменю, другую DIM-вкладку и выключить дисплей — во всех этих реальных конфликтах меню скрывается и после их завершения восстанавливается. |
| GATE-039 | Статусы умного дома в DIM-меню | Для устройств с бинарным состоянием, температурой, положением шторы/ворот и недоступным источником сравнить строку меню с живым connector snapshot. Значение, единица, «Недоступно», признак устаревания и «Нет данных» совпадают с HOME/панелью водителя; изменение приходит без повторного открытия меню и не меняет выбранную строку. После разрыва/смены connector service старое значение очищается, а подтверждение строки по-прежнему выполняет именно выбранное действие. |
| GATE-040 | Точное оформление mNavi | На одном DIM сделать сравнительные снимки mNavi 2.0 и точного пресета Natro при одинаковой геометрии 414×284 и четырёх строках. Пипеткой и измерением подтвердить прозрачную подложку `#00FFFFFF`, текст `#FF6C7984`, выделение `#FF197BC5`, белый выбранный текст, 14/24 sp, внутренний отступ 8 dp и радиус 6 dp; штатная DIM-карточка видна через фон, отсутствуют рамка, иконки и счётчик. Импорт старой конфигурации сразу получает тот же вид, а её сохранённые кастомные цвета не изменяют точный пресет. |
| GATE-041 | Точный исходный знак манёвра без лишнего текста | На последовательности минимум из прямого участка, правого/левого поворота, съезда и перестроения маршрута покадрово сопоставить `image_maneuverballoon_maneuver`, штатную отображаемую дистанцию, `maneuverIdentity`, HUD и панель. Пиктограмма на обоих экранах визуально совпадает со штатной, не строится по semantic action, не переживает смену identity и не появляется без совпавшего bitmap. Состав зависит от реально видимых stock-полей: без следующей дороги остаются знак и дистанция как на `IMG_8266.jpeg`, а при её наличии «Еринское ш.» отображается отдельной строкой под общим рядом знака/«2,5 км» как на `IMG_8278.jpeg`. Пустая строка схлопывается; synthesized-строка «проспект Ленина · направо» с `IMG_8267.jpeg` отсутствует. Реальные `DirectionSignItem`/auxiliary из `CARD-012` при этом сохраняются отдельными областями. |
| GATE-042 | Отдельная сводка маршрута HUD/панели | В обоих редакторах найти отдельную запись «Сводка маршрута · как в Навигаторе», добавить её без карты и без карточки манёвра, затем сравнить с `IMG_8268.jpeg`. Значения расстояния, ETA и остатка времени совпадают с одним свежим snapshot; полоса использует фактические traffic-сегменты и маркер прогресса, а боковых и нижних кнопок нет. Проверить независимые размер/позицию/цвета на HUD и панели, уменьшение без мыла, скрытие после отмены маршрута и потерю snapshot не позднее 3 секунд. |
| GATE-043 | Автозапуск карты панели после перезагрузки | На включённых HUD и панели выполнить минимум пять полных reboot/QuickBoot и по два перезапуска Natro/Navigator без открытия настроек. Для каждого цикла по журналу сравнить boot receiver, прямой DIM launch, создание endpoint, доступность `TextureView`, публикацию cluster lease, аутентификацию bridge и attach MapKit: панель появляется за ограниченное время, карта подключается сразу после готовности Navigator и не требует постороннего window/settings события. Отдельно воспроизвести пропущенный initial callback при `TextureView.isAvailable()==true`, один отказ `MSG_CLUSTER_SURFACE_LOST`, resize и отзыв Surface; восстановление создаёт новую generation не более шести раз и никогда не возвращает отозванный producer. HUD при этом продолжает стартовать как раньше. |
| GATE-044 | Стабильность избранных кнопок | В лаунчере создать HOME, Работу и пользовательскую точку, затем выполнить 50 обновлений данных, три rebind/reload, смену размера иконок, 20 нажатий системной кнопки «Домой» из Navigator и других приложений, возврат после скрытия HOME/очистки графического кэша и перезапуск Natro. В режиме без активного маршрута все плитки и их подписи остаются на месте; при активном маршруте действует установленное переключение общего блока на навигацию. Одинаковый список не создаёт новую view-иерархию, выбранные размеры сохраняются, пустой безымянный синий блок не появляется. Проверить первый layout после возврата при неизменных bounds workspace; сопоставить launcher-favorites configured/routeActive/frames/visible с экраном. |
| GATE-045 | Избранная точка остаётся в окне | По десять раз открыть HOME, Работу и пользовательскую точку из лаунчера при холодном и тёплом Navigator. После deep link остаются видны HOME/системная строка/нижнее меню, bounds Navigator совпадают с настроенным окном, а Activity — точный `MapActivity`; через 0,22 и 0,9 секунды окно не разворачивается. Полный экран считается провалом, кроме отдельно зафиксированного отказа самого оконного запуска. |
| GATE-046 | Закрытый парктроник не удерживает уведомления | При включённой парковочной системе открыть и закрыть её графику минимум десять раз, оставляя `PrkgDstCtrlSts=2/3` после закрытия. Сопоставить видео, снимки окон ECARX и журнал паузы: уведомление и очередь возобновляются после подтверждённого исчезновения последнего парковочного окна без ручного выключения датчиков. Проверить отдельную камеру 360, перекрытие двух удержаний, выбранное блокирующее приложение, два парковочных окна, окно на другом дисплее, потерю callback/канала, холодный старт и повторную инициализацию. Неизвестный снимок не считается достоверным закрытием; никакой TTL, watchdog или максимальное время ожидания не снимают паузу вместо фактического закрытия. Автотесты проверяют эти состояния; аппаратная приёмка остаётся отдельной. |
| GATE-047 | Актуальный дизайн приборки | Сопоставить новый макет с реальным водительским дисплеем KX11, неизменяемыми элементами и последним конкретным изображением `CLUSTER-021`. Подтверждение пользователя относится к конкретному макету; разрешение собрать APK с исправлением парковки не закрывает этот критерий. |
| GATE-048 | Полнота цифрового справочника Geely | Реестр доступных дампов/логов имеет отметку разбора, каждый вывод связан с проверяемым источником, неизвестные механизмы перечислены с конкретными вопросами и способом проверки; корневой документ опубликован рядом с требованиями без закрытых исходных материалов. По `KNOW-004` матрица охватывает также неизученные системы и неизвестное оснащение; каждый пробел связан с источником либо явно отмеченным отсутствием источника и имеет следующий шаг. Журнал различает завершённую проверку, отрицательный результат в точной области и необходимый сбор новых данных. Полнота каталога интерфейсов, число карточек и прежние 14 вопросов не считаются доказательством полноты всех систем автомобиля. `ОТКРЫТО`: создание матрицы само по себе не закрывает этот критерий. |
| GATE-049 | Адресный сбор недостающих файлов на macOS | `ОТКРЫТО`: каждой цели соответствует точный путь/ограниченный перечень и конкретный пробел доказательств; полученные байты проверяются SHA-256 и дедуплицируются с сохранением всех исходных путей. Сборщик формирует проверяемый отчёт для полученных, отсутствующих, запрещённых к чтению и ошибочных целей; отсутствие файла не подменяет отказ доступа. Переносимый macOS launcher и инструкция проверены локальными mock-тестами, включая пути с пробелами, ошибки команд и частичный результат. Успех локальных проверок не означает успешный сбор с KX11 и не закрывает `GATE-046/GATE-048`; точный запуск и полученные источники подтверждаются отдельно. |
| GATE-050 | Курсор вне полилинии | Сравнить основной Navigator, HUD и приборку: обычное движение по маршруту, несколько последовательных позиций вне него при сохранённой старой RoutePosition, обратный выезд, пересчёт, временная потеря RoutePosition, неподвижный автомобиль, отсутствие маршрута. После ROUTE_LOST/NOT_ON_ROUTE старые координаты/heading и их hold-кэш не заменяют текущую Guidance location. При возвращении к подтверждённому ON_ROUTE восстанавливается map matching. Курсор и камера обеих независимых карт используют один выбранный кадр; полилиния не притягивает к себе курсор. |
| GATE-051 | Опции топлива HUD | На 15/10/64 л проверить границы цвета и скрытия, ручной объём (например 70 л), возврат в режим 64 л и правильный долив; switch «только P» проверить в P/D/R/N и при неизвестной передаче. Сопоставить редактор и HUD. Изменить все поля и нажать «Отмена» — исходный экспорт/вид не меняются; применить, перезапустить, экспортировать/импортировать — значения сохраняются. Без данных остатка показывается недоступность, а не выдуманный долив; скрытый в работе топливный элемент остаётся выбираемым в редакторе. |
| GATE-052 | Всплывающая громкость HUD | Сверить реальное деление системной громкости с цифрой/иконкой HUD при увеличении, уменьшении, достижении нуля и быстрых последовательных нажатиях. Без изменения виджета нет; после последнего изменения он скрывается через 2 с. Старт HUD/смена трека/повтор того же значения не вызывают показ и не продлевают его. Редактор показывает выбираемый образец без имитации изменения в живом HUD. |
| GATE-053 | Мягкие края карты | На HUD и приборке отдельно включить эффект, изменить степень и размер, установить ноль, проверить малую карту и перекрывающий её текстовый виджет. Меняется только плавность края, центр остаётся чётким, текст соседних элементов и прозрачность окна сохраняются; Surface/MapWindow не пересоздаются. После сохранения и перезапуска выбор восстанавливается. |
| GATE-054 | Медиа HUD при звонке | При музыке проверить входящий, исходящий, отклонённый и завершённый звонок через подключённый телефон: на переходе источника playback в паузу исчезают обложка/все тексты/таймер HUD. Возвращаются после подтверждённого PLAY, без автокоманды от Natro; отказ от продолжения сохраняет скрытие. Дополнительно обычная пауза/возобновление, исчезновение session, поздняя обложка, смена трека во время паузы, редактор и независимый краткий показ громкости. Если OEM оставляет playing=true во время звонка, фиксировать отдельный дефект источника, не объявлять этот gate пройденным. |
| GATE-055 | Обновление исходного artwork | На KX11 сравнить полную стрелку штатного Navigator и HUD/приборки на последовательных манёврах и во время анимации. Изменение изображения при прежнем Drawable обновляет HUD, переходный наконечник не застывает. Дистанции сравнивать только в синхронных кадрах. |
| GATE-056 | Единый модуль поворотников | Добавить один модуль, изменить его положение/ширину/высоту и сохранить; проверить левый, правый, аварийку, выключение и неизвестные данные. Обе стрелки выбираются одной рамкой, мигают синхронно при аварийке; редактор показывает обе. Старые раскладки загружаются без потери элементов. |
| GATE-057 | Принадлежность знаков маршруту | На развязке как IMG_8290.jpeg сравнить ALWAYS/ROUTE_ONLY/HIDDEN отдельно для HUD и приборки. В ROUTE_ONLY соседние дороги не показывают значки, а подтверждённые события выбранного маршрута сохраняются; после смены/завершения маршрута старые события очищаются. Проверить отдельно Yandex и HUD Speed и отсутствие побочных изменений другого профиля. |

| GATE-058 | Табличка по штатным командам | Сопоставить основной Navigator, HUD и приборку на прямом участке, левых/правых поворотах, развилке, круге, съезде, промежуточной точке, основных/следующих полосах и цветных указателях. Проверить смену режимов и владельца View, скрытие отдельных/всех областей, пересчёт и отмену маршрута, работу в фоне, reconnect и изменение размеров/шрифтов. Ресурсы совпадают с выбранными движком, исходное окно не меняется, старые данные не воскресают. Отдельно проверить несовместимый Navigator и 30-минутную стабильность. Автоматические source/replay проверки не закрывают аппаратную приёмку. |

Автоматический прогон выпуска 01.09.2026: `GATE-001…GATE-004` пройдены для CI run 167
и подписанной пары из дерева `7508e7bb81776321f0a4ac997cfaee6c4e61d90f`. `GATE-005` не
пройден без физического KX11 и остаётся обязательной пользовательской приёмкой. `GATE-006`
выполняется в отчёте выдачи: автоматические и аппаратные результаты не смешиваются.

Автоматический прогон исправляющего выпуска 2.6.1: `GATE-001…GATE-004` пройдены в CI run 173
для исходного commit `4abcf578aebf5e2c049ba0acd10fbe1bd794c00d` и дерева
`b0845312145e428fc69ef654fcd2329afb14dd5d`. Подписаны Natro 2.6.1 и Navigator 30.3.0;
сертификат обоих APK имеет SHA-256
`6e9855aedc008bbdd8a7fbf3f490be07f964b7ac658a837a1592647a08365c75`. `GATE-005`,
`GATE-007` и `GATE-008` остаются незакрытыми до проверки на физическом KX11.

Автоматический прогон исправляющего выпуска 2.6.2: CI run 181 полностью прошёл для commit
`36218be1d38c2f0c248ef4a21ba166af6884df48` и дерева
`78c0cf8da42465df4dd825f6c5382cfa8dc264ef`; `GATE-001…GATE-004` пройдены. Подписаны
Natro 2.6.2 (`59cb23f0593d30ba83e317f95411b9db4ade8914ebdd33210ed54266f7369167`),
Navigator 30.3.0 (`cd5831406335dc94e524f1ecfca39fa32a4994adc83439455511cd82812e40bf`)
и неизменённый по функциональности HUD Speed Bridge 76.0-L13
(`3468e987b2f25d569658fd39801bf0a9d1b69ba8d0ef99a614b3480e0b22bf20`). Все три APK
подписаны сертификатом `6e9855aedc008bbdd8a7fbf3f490be07f964b7ac658a837a1592647a08365c75`.
`GATE-005`, `GATE-007`, `GATE-008` и `GATE-009` остаются до проверки на физическом KX11.

Автоматический прогон исправляющего выпуска 2.6.4: CI run 204 полностью прошёл для commit
`f4249ed6ea0e95f8db34f0023b2230e871b59446` и дерева
`63bff38826e076951272e0bde7f0bb0bdff2e30f`. Из этого дерева собраны и стабильным сертификатом
подписаны Natro 2.6.4 (`ae76c55128ce971ddbb744caff012e8f5af567a4c0b4b43a5383441cca6c2735`),
Navigator 30.3.0 (`6f2fbc7853eda1ab090be0cd82f0a3940bacf8b12bd53b416c6d2c7efbd3fec8`)
и функционально неизменённый HUD Speed Bridge 76.0-L13
(`3468e987b2f25d569658fd39801bf0a9d1b69ba8d0ef99a614b3480e0b22bf20`). Статические gates
пройдены; `GATE-010` и `GATE-011` остаются открытыми до длительной проверки на физическом KX11.

Автоматический прогон выпуска 2.6.5: CI run 208 полностью прошёл для commit
`21956ddf1c5b98ad85c1cb901972a5746013e183` и дерева
`91b066ce75d4b1e7905c2b02c7852afa22569071`. Из этого дерева стабильным сертификатом подписаны
Natro 2.6.5 (`08c61c220a7e114d8e2992c9e4632570aef1b1c4df74ceb012a65247003a787f`) и
Navigator 30.3.0 (`e6bb944a18d9f4d6659e61e3be50a863537f1a7c57b4cb4860070c5b6c67dcb5`).
`GATE-001…GATE-004` и статический KX11 compatibility gate пройдены; `GATE-010`, `GATE-011` и
новый визуальный `GATE-012` остаются открытыми до проверки на физическом KX11.

Автоматический прогон выпуска 2.6.6: CI run 212 полностью прошёл для commit
`5277b4b852f7f2b76b82fd4f68ace65b57e7ba4b` и дерева
`588cdbf268c729ead231766d43543fb666063018`. Из этого дерева стабильным сертификатом подписаны
Natro 2.6.6 (`6640d70f4aedea10324dcb003816b3f11f7adbb51b4ba43432b78ee6f5e6bf87`) и
Navigator 30.3.0 (`11ca3bd08dc80b7fa53cb646a6d221396a777e5847314a1a5c7c346192251bd7`).
Автоматические release-gates, общий сертификат и установка поверх подписанной пары 2.6.5
проверены; визуальный `GATE-013` остаётся открытым до проверки на физическом KX11.

Автоматический прогон выпуска 2.6.7: CI run 216 полностью прошёл для commit
`abed6c2353ad17100a811d7a51decca41c664e24` и дерева
`615134b6033fccd6eee9a04d05c5051122d61fb6`. Из этого дерева стабильным сертификатом подписаны
Natro 2.6.7 (`f4c7aa1bdf16a76ebe214f32c752e694da75f06ba89a42cc7e874f836c28a8e5`),
Navigator 30.3.0 (`3517d06b33bba74d9c9a9e108fcbf2b10c6c5b9245342c646535b7b97f0733cd`) и
функционально неизменённый HUD Speed Bridge 76.0-L13
(`3468e987b2f25d569658fd39801bf0a9d1b69ba8d0ef99a614b3480e0b22bf20`). Автоматические
release-gates, общий сертификат и обновление Natro поверх 2.6.6 проверены; визуальные
`GATE-013…GATE-015` остаются открытыми до проверки на физическом KX11.

## 12. Журнал изменений реестра

- **01.09.2026:** создан единый реестр; восстановлены требования за 25.08–01.09; зафиксирован
  обязательный разрыв между «есть в коде» и «подтверждено на KX11»; добавлен release gate для
  преобразования скорости камер и правило постоянного обновления файла.
- **01.09.2026:** CI run 167 полностью прошёл; подтверждены автоматические проверки нормализации
  скорости, состава Navigator DEX, границ baseline и общей подписи. Визуальная и аппаратная
  проверка перечисленных функций на KX11 намеренно оставлена незакрытой.
- **01.09.2026:** повторный аудит истории и документов выпусков выявил, что часть точных условий
  была спрятана в общих формулировках. Добавлены отдельные пункты для DIM-панели, пяти панелей
  11–15, второго курсора/скачков карты, фоновой работы карт, кнопки окна, конечной точки,
  свежести маршрута, порядка и размеров слоёв, обратимой линии маршрута, точных карточек,
  сигналов 360/парктроника, активной data-SIM, границ ответственности Bluetooth и MediaSession.
- **01.09.2026:** по журналу `status-widget-debug(20260901-144416).txt` добавлен P0-дефект
  `NAV-011`: три последовательных завершения Navigator через 13/10/9 секунд. Повторно запрещён
  automotive `NavigationLayer` на независимых картах; аппаратный gate увеличен до 30 минут в
  каждом режиме. По `IMG_8179.jpeg` добавлен `NATRO-014`: accessibility восстанавливается тихо,
  а полный и сокращённый форматы компонента считаются одной записью.
- **01.09.2026:** для исправлений `NAV-011` и `NATRO-014` собрана и подписана пара 2.6.1 из
  дерева `b0845312145e428fc69ef654fcd2329afb14dd5d`; CI run 173 пройден. Итог не помечен как
  аппаратно подтверждённый: обязательны `GATE-007` (по 30 минут в двух режимах) и `GATE-008`
  (холодная загрузка и два входа в настройки без повторного Toast).
- **01.09.2026:** аппаратная проверка 2.6.1 по `IMG_8187.jpeg`, `IMG_8185.jpeg`,
  `IMG_433214B6-4907-4D12-93FB-E8D24594356F.jpeg`, `IMG_8184.jpeg` и `IMG_8183.jpeg`
  повторно открыла `WIN-005`, `WIN-006`, `WIN-009`, `MAP-008` и `MAP-009`. Добавлен строгий
  `MAP-023`: самодельные знаки поворотов запрещены, нужен штатный стиль стрелок полилинии MapKit.
  По отдельному сообщению добавлены `CLUSTER-017` и `GATE-009`: карта панели обязана запускаться
  на холодном старте без входа в настройки.
- **01.09.2026:** в 2.6.2 самодельные знаки поворотов заменены на штатные стрелки MapKit с точной
  `PolylinePosition`, ось названий улиц исправлена, показ кнопки перенесён в глобальный touch hook,
  top inset повторно применяется к позднему дереву контролов, а приборная панель сама запускает
  Binder endpoint до первой surface. CI run 181 и статические release-gates пройдены; результат
  честно оставлен `В КОДЕ / НУЖЕН KX11` до аппаратной проверки.
- **01.09.2026:** по `IMG_8188.mp4` и `status-action-session(6).json` добавлены `SHADE-001…007`:
  отдельная редактируемая шторка Natro должна заменить окно `ecarx.notificationcenterui` без
  одновременного показа, не отключая весь SystemUI; отключение обязательно обратимо и требует
  точного component-level исследования и recovery через подтверждённый root ADB.
- **01.09.2026:** пользователь дал прямую команду начать реализацию `SHADE-001…007`; статусы
  переведены в `В РАБОТЕ`. Первоначальный каталог строится из уже поддержанных Natro элементов,
  а отключение ECARX остаётся заблокированным до точного определения безопасного компонента.
- **01.09.2026:** добавлена первая реализация системной шторки: отдельный main-display overlay,
  жест/анимация/Back, часы, дата, медиа, громкость, яркость, независимые действия, живой
  move/resize/style-редактор, boot-autostart и safety-сокрытие при внешних автомобильных окнах.
  Android CI компилирует обе конфигурации и завершает все 1760 unit-тестов; аппаратные пункты не
  переводятся в `ГОТОВО` до проверки KX11. Точный компонент `ecarx.notificationcenterui` всё ещё
  должен быть получен read-only аудитом до любого отключения штатной панели.
- **02.09.2026:** аппаратная проверка 2.6.3 открыла критический дефект `SHADE-008`: стандартный
  жест не открывает панель, а после попытки открытия невидимое полноэкранное overlay-окно
  перехватывает все касания Natro и не позволяет снять лог. `SHADE-001` и `SHADE-005` возвращены
  в статус `ОШИБКА`; до исправления пользователю предписано отключить шторку после перезагрузки.
- **02.09.2026:** пользователь сообщил о мерцании всей карты одновременно на HUD и панели
  приборов и подтвердил отсутствие видимых стрелок поворотов на полилинии. Добавлены `NAV-012`,
  `GATE-010`, `GATE-011`; `MAP-009` и `MAP-023` возвращены в статус аппаратной ошибки до
  доказанного видимого результата на KX11.
- **02.09.2026:** аудит нашёл две конкретные причины: обе независимые карты заменяли геометрию
  маршрута до 10 раз/с и могли пересоздавать `OffscreenMapWindow` при resize/временной
  невидимости, а стрелки копировали документированный штатный флаг `enabled=false`. Геометрия
  маршрута теперь неизменна, прогресс скрывается обратимым `Subpolyline`, lease surface не
  отзывается при кратком скрытии и не публикуется заново при resize; повороты добавляются
  `RouteHelper.addManeuvers` и получают явно включённый штатный стиль. Просроченный маршрут также
  отбрасывается перед повторным включением слоя, поэтому старые стрелки не могут вернуться после
  паузы. Автоматические контракты добавлены, но `GATE-010` и `GATE-011` остаются открытыми до
  проверки на KX11.
- **02.09.2026:** исправлен ошибочный вывод об отсутствии подписи: в постоянном хранилище найдены
  `status-widget-ha-update.jks` и приватный резервный архив с паролем. Сертификат и alias сверены,
  выпуск 2.6.4 подписан тем же ключом; добавлено постоянное правило `REL-015`, запрещающее
  повторно запрашивать комплект у пользователя без проверки обеих сохранённых копий.
- **02.09.2026:** пользователь заменил самодельные подписи улиц на требование брать их штатный
  вид непосредственно из дорожного слоя Яндекса по эталону `IMG_8189.jpeg`. `MAP-008` отмечен
  заменённым, добавлены `MAP-024` и `GATE-012`: bitmap/Canvas-надписи запрещены, прежний режим
  «только на маршруте» мигрирует на нативные дорожные подписи MapKit.
- **02.09.2026:** реализация `MAP-024` собрана в Natro 2.6.5 и Navigator 30.3.0 из commit
  `21956ddf1c5b98ad85c1cb901972a5746013e183`; CI run 208 и все статические release gates
  пройдены. Пункт переведён в `В КОДЕ / НУЖЕН KX11`, а визуальный `GATE-012` намеренно не
  закрыт до проверки штатного вида и изгиба подписей на HUD и панели приборов автомобиля.
- **02.09.2026:** добавлено `MAP-025`: пользователь потребовал отдельные настройки цвета,
  цвета и толщины обводки и длины штатных стрелок поворотов. Уточнено, что длина пропорционально
  меняет наконечник, но не ширину тела; ширина обязана следовать толщине маршрутной линии.
  Добавлен аппаратный критерий `GATE-013`.
- **02.09.2026:** пользователь дал прямое разрешение выпустить подписанную совместимую пару с
  реализацией `MAP-025`. Для следующего выпуска зафиксированы Natro `2.6.6` / `208021299` и
  неизменная идентичность Navigator `30.3.0` / `739564630`; HUD Speed функционально не меняется.
- **02.09.2026:** CI run 212 и окончательная проверка подписанной пары 2.6.6 прошли. Natro и
  Navigator имеют один сертификат `6e9855ae…65c75`, Natro увеличил `versionCode` ровно на один,
  а Navigator сохранил пакет и версию; исходное дерево и контрольные суммы зафиксированы выше.
- **02.09.2026:** по `IMG_8192.jpeg` и `IMG_8196.jpeg` добавлены `MAP-026…028`: общий размер
  сектора камеры заменён независимыми длиной и шириной только дальнего основания, добавлен цвет,
  знак камеры/скорости должен стать одним компактным штатноподобным кругом, а минимальная длина
  стрелок поворота снижена с 50% до 10%. Старое значение сектора мигрирует в обе новые настройки.
- **02.09.2026:** пользователь сообщил, что изменение размеров карты HUD растягивает старый кадр.
  Добавлены `NAV-013` и `GATE-015`; установлено, что прежний resize менял лишь метаданные уже
  созданного viewport. Теперь новая геометрия получает новую generation и отдельный
  `OffscreenMapWindow`, а последний готовый буфер остаётся на экране во время короткой пересборки.
  Это уточнение заменяет более раннее предположение, что surface нельзя повторно публиковать при
  resize: повторная публикация необходима, но выполняется без detach-only интервала.
- **02.09.2026:** пользователь дал прямую команду немедленно собрать APK. Для выпуска зафиксированы
  Natro `2.6.7` / `208021300` и совместимый Navigator `30.3.0` / `739564630`; оба должны быть
  подписаны стабильным сертификатом проекта. HUD Speed Bridge функционально не меняется.
- **02.09.2026:** первый CI run 214 был остановлен до выпуска из-за необъявленной локальной
  переменной только в новом source-contract тесте; ни один APK из него не выдавался. Тест исправлен
  отдельным опубликованным commit, после чего CI run 216 прошёл Android unit-тесты, сборку всех
  трёх DEX, baseline boundary и упаковку кандидата. Финальные Natro, Navigator и неизменённый HUD
  Speed Bridge подписаны сертификатом `6e9855ae…65c75`; версии, схемы подписи, `zipalign`,
  совместимость обновления 2.6.6→2.6.7 и контрольные суммы проверены.
- **02.09.2026:** пользователь уточнил визуальное требование после `IMG_8197.jpeg` и
  `IMG_8198.jpeg`: на знаке камеры с известной скоростью не должно быть даже маленького значка
  камеры. `MAP-027` заменён на `MAP-029`; отдельный значок сохраняется только при отсутствии
  числового ограничения, а `GATE-014` обновлён.
- **02.09.2026:** фото `IMG_8199.jpeg` и `IMG_8200.jpeg` повторно подтвердили три дефекта
  Navigator: большой верхний резерв в оконном режиме, кнопка режима справа сверху вместо штатного
  левого блока и отсутствие обратной кнопки в полном экране. `WIN-005`, `WIN-006`, `WIN-009`
  повторно открыты; добавлены строгие `WIN-010…WIN-012` и аппаратный сценарий `GATE-016`.
- **02.09.2026:** исходное дерево 2.6.8 `eb1b09892c4fb906cc0d6c2265bf5989c8a724e3`
  опубликовано commit `07e1e07aff9166e06fd37fcd010f00a9fd5d6b73`. CI run 218 успешно
  выполнил Android unit-тесты, сборку Natro, трёх изолированных DEX и baseline
  boundary. Natro `2.6.8` (`208021301`) и Navigator `30.3.0` (`739564630`) подписаны
  стабильным сертификатом `6e9855ae…65c75`; SHA-256 APK: Natro
  `debdc86a6823beae55ba66bdfab19a36b5fc313005a3d7d776c695d28c1e5ac7`, Navigator
  `3298bed59d644fc12903c6c64c409ab8b92657ffa425ff338a5ea23ca0837244`. `GATE-014` и
  `GATE-016` остаются открытыми до физической приёмки на KX11.
- **02.09.2026:** аппаратная проверка 2.6.8 провалила обе части `GATE-016`: в оконном режиме
  тап по карте вообще не показывает кнопку режима, а верхние элементы остались слишком низко.
  `WIN-005`, `WIN-006`, `WIN-009`, `WIN-010` и `WIN-012` возвращены в статус ошибки.
  Техническое требование внедрять кнопку дочерним элементом в изменяемый штатный `LinearLayout`
  заменено `WIN-013`: сохраняется точное визуальное место в левом блоке, но владельцем остаётся
  стабильный touch-прозрачный слой Natro. Добавлено `WIN-014`: для окна нужно переопределять
  listener каждого поздно созданного `PaddingtonView`, потому что штатная реализация повторно
  читает глобальный root inset и отменяет разовую коррекцию padding.
- **02.09.2026:** пользователь сообщил о повторной ощутимой задержке кнопок переключения музыки
  на руле в 2.6.8. `MUSIC-006` повторно подтверждён как аппаратная ошибка; добавлены строгие
  `MUSIC-010` и `GATE-017`, запрещающие общей очереди и тяжёлым подсистемам задерживать передачу
  каждого события в активную MediaSession.
- **02.09.2026:** пользователь повторно подтвердил провал `CLUSTER-017/GATE-009` в 2.6.8:
  включённая карта панели приборов не стартует после холодной загрузки и появляется немедленно
  только после открытия настроек Natro. В 2.6.9 boot-путь должен вызывать тот же запуск
  endpoint/lease, который сейчас неявно активирует экран настроек.
- **02.09.2026:** в дереве 2.6.9 реализован единый пакет исправлений по повторной аппаратной
  обратной связи 2.6.8. Кнопка режима остаётся в стабильном слое Natro и переустанавливается из
  глобального touch-hook; вложенные `PaddingtonView` больше не могут поздно вернуть верхний inset.
  Для руля добавлен прямой cached-MediaSession путь без синхронного поиска и общей очереди.
  Холодный старт приборной карты защищён с двух сторон: повтором публикации живой `TextureView`
  поверхности и немедленным восстановлением Binder/HELLO после внешнего DIM-запуска. Автоматические
  контракты проверяют реализацию; `GATE-009`, `GATE-016` и `GATE-017` остаются открыты до KX11.
- **02.09.2026:** по `IMG_8208.jpeg`/`IMG_8209.jpeg` пользователь подтвердил потерю второго типа
  у составной камеры, потребовал отдельный размер наконечника стрелок, а по
  `IMG_8210.png`/`IMG_8207.jpeg`/`IMG_8206.jpeg` — общую раскладку карточек вокруг маршрута и
  штатные хвостики. Добавлены `MAP-030…032`, обновлены `MAP-006`, `MAP-025`, `MAP-029` и
  `GATE-013/014`, создан `GATE-018`. Записи камер теперь объединяют типы и оба направления, а
  светофоры получают восемь штатных `LegPlacement` вместо принудительного `NONE`.
- **02.09.2026:** отдельным уточнением пользователь потребовал хвостик и у таблички движения по
  полосам. Добавлен `MAP-033`: слой больше не вырезает один `View` вручную, а использует полную
  штатную `BalloonTexture` Яндекса с `BalloonAnchor` и точным `imageAnchor`; проверка включена в
  `GATE-018` и остаётся открытой до установки 2.6.9 на KX11.
- **02.09.2026:** перед финальной сборкой пользователь потребовал заново сверить приоритеты всего
  набора слоёв со штатным Navigator после добавления новых слоёв. Добавлены `MAP-034` и
  `GATE-019`; финальная сборка 2.6.9 заблокирована до аудита исходного порядка, текущих
  `zIndex`/collection-приоритетов, безопасного ручного диапазона и повторной проверки на KX11.
- **02.09.2026:** статический аудит `MAP-034` выполнен по исходному APK Navigator 30.3.0
  `SHA-256 663018fb66074e001eed7caba8e33bee1bcf78f6798bc84949d253dcb348f27f` и текущему
  дереву `5fa67eacc6f3`. Выявлены блокирующие расхождения: custom-ID не включены в штатную
  feature-type-aware схему, ручной алгоритм переносит только первый sublayer каждого ID и ставит
  выбранные слои выше всех непереставленных слоёв, камера смешивает ground-сектор и placemark-знак,
  штатные стрелки являются частью маршрутной полилинии, поэтому отдельный priority-slider сейчас
  ничего не меняет. Исправление в рамках аудита не вносилось; `GATE-019` остаётся открытым.
- **02.09.2026:** после разрешения пользователя исправление `MAP-034` внесено для обеих
  независимых карт. Камерный root разделён на ground-сектор (`IGNORE`) и placemark-знак (`EQUAL`),
  все sublayer находятся с явным feature type, а пользовательские ID ставятся относительно
  штатных якорей без перемещения слоёв Яндекса в конец. Стрелки MapKit остаются внутри маршрутной
  полилинии и наследуют её приоритет; ложный отдельный ползунок удалён, точный пресет 2.6.8
  мигрирует на порядок `30/40/50/60/70/80/90`. Статический контракт проходит локально;
  фактическая композиция HUD/панели остаётся на `GATE-019` для KX11.
- **02.09.2026:** по замечанию пользователя проверено качество значков при уменьшении. Штатные
  пластины камер исходно векторные, но MapKit получает уже bitmap; число скорости раньше
  растрировалось прямо в минимальный экранный диаметр 26–40 px. Добавлен `MAP-035`: камера теперь
  создаётся минимум в 80 px и уменьшается `IconStyle.scale` при неизменных видимом размере и
  collision-рамке. Чёткость 50/75/100% добавлена в аппаратный `GATE-014`.
- **02.09.2026:** исходное дерево `bbe3ae6214ed610f2215e528c31672e7869e8902` опубликовано как
  commit `bc6500506fdfc0cd086ff75c17ca51471d369023`; push CI run 228 прошёл полностью. Подписаны
  Natro 2.6.9 (`208021302`, SHA-256 `fa71ecf9…d2527`) и Navigator 30.3.0
  (SHA-256 `9ec2b265…8c347`) стабильным сертификатом `6e9855ae…65c75`. Статическая совместимость
  KX11 пройдена; аппаратные `GATE-007`, `GATE-009…019`, включая верхние элементы оконного
  Navigator, чёткость камеры и фактический порядок слоёв, не закрыты без установки на ГУ.
- **02.09.2026:** пользователь распространил требование чёткого уменьшения камеры на все значки
  карты. Добавлены `MAP-036` и `GATE-020`: каждый размер камеры, светофора/секунд, полос,
  дорожного события, конечной точки и курсора должен формироваться из исходного vector/drawable
  или штатного рендера, а не из уже уменьшенного bitmap. Требование пока не помечено выполненным.
- **02.09.2026:** повторно проверены известные причины падений Navigator и Natro. Automotive
  `NavigationLayer`, дополнительная `GuidanceCamera`, ранний type 2038 и повторная оконная
  транзакция в исполняемый путь не вернулись. Добавлены `NAV-014`, `NATRO-015`, `GATE-021` и
  отдельный отчёт аудита; DEX-запрет расширен, отложенные poll/inset callbacks изолированы от
  главного looper, а полный лимит connector-listener больше не бросает исключение в процесс.
  Аппаратная стабильность остаётся открытой до `GATE-007` на KX11.
- **02.09.2026:** исходный commit `078591241a580067291c0eff33594212a1ab1dc0`, дерево
  `16c01838204a3371771186bf9b21b2b19d67c93f`, прошёл CI run 231: Android unit-тесты, сборку
  Natro, изолированный Navigator `classes19.dex`, HUD Speed bridge и baseline-verifier self-tests.
  Подписанная финальная APK-пара из этого пост-релизного аудита не выпускалась; `GATE-007`,
  `GATE-020` и аппаратная часть `GATE-021` остаются открытыми до KX11.
- **03.09.2026:** аппаратная проверка по `IMG_8214.jpeg`, `IMG_8219.jpeg`, `IMG_8215.jpeg`,
  `IMG_8216.jpeg`, `IMG_8220.jpeg`–`IMG_8223.jpeg` выявила три регрессии: все типы камер
  смещались с маршрута восьмипозиционным anchor, отдельный Guidance `PaddingtonView` возвращал
  верхний inset только при активном ведении, а кнопка оконного режима выбирала разные позиции по
  глобальным Alice/ghost-контролам и моменту layout. Добавлены `MAP-037`, `WIN-015`, `WIN-016` и
  `GATE-022`; `MAP-032`, `WIN-013`, `WIN-014`, `GATE-016` и `GATE-018` уточнены без удаления
  прежней истории. В коде камера закреплена центральным anchor и первой резервирует footprint,
  Guidance-root ищется заново и получает отдельный inset-guard, а третий левый слот блокируется
  до показа только по точной штатной паре. Исходный commit
  `836da17ab359a8d2da3d1d646c84586b31ca541b` (дерево
  `e64dfa5e9597dd0777d5b06ee6150deef06efba5`) прошёл CI run 232: Android unit-тесты, сборку
  Natro, изолированный Navigator `classes19.dex`, HUD Speed bridge, baseline-verifier self-tests
  и упаковку неподписанной пары. Задание подписи пропущено; итог остаётся `НУЖЕН KX11`.
- **03.09.2026:** пользователь дал прямое разрешение на подписанную сборку после исправлений
  `MAP-037`, `WIN-015` и `WIN-016`. Промежуточная подпись с прежней идентичностью Natro 2.6.9
  остановлена до выдачи: после уже выпущенной 2.6.9 в Natro добавлен production safety-fix
  `NATRO-015`, поэтому повторное использование `208021302` нарушило бы `REL-003`. Для финального
  выпуска зафиксированы Natro `2.7.0` / `208021303` и неизменная идентичность Navigator
  `30.3.0` / `739564630`; подписывать разрешено только заново проверенный опубликованный source
  commit, а аппаратные `GATE-007`, `GATE-009…022` остаются открытыми до KX11.
- **03.09.2026:** подписанная пара собрана только из опубликованного commit
  `6395f98e38f5042cdbd747823b651c14dc37e286` (дерево
  `e456c7db089086016d73b3474243d952760d4387`) после успешного push CI run 234
  (`33717713006`); SHA-256 архива кандидата `a4d864cd035d0f678cbe83ea779d83b487e7367569cdd5c50424ff9ef74ed4d5`.
  Итоговые APK: Natro 2.7.0 / `208021303`, SHA-256
  `a8172394dc7cc795dc4ac4eff961e90ebc99384ea80f29d4df8dbc0fe8229d88`; Navigator 30.3.0 /
  `739564630`, SHA-256 `7c598f4e60723ac789aed74b791a62cc7140a6726fb3262a7fc0a8d85f50a22a`.
  Оба подписаны стабильным сертификатом
  `6e9855aedc008bbdd8a7fbf3f490be07f964b7ac658a837a1592647a08365c75`; Natro проверен как
  допустимое обновление поверх выпущенной 2.6.9 (`208021302`, SHA-256
  `fa71ecf9a3a31ebf6bb477afaf7ed7a548885751e0b0dc70bc9f39de2e0d2527`): package и сертификат
  совпадают, новый `versionCode` строго больше. Статический KX11-verifier и контрольные суммы
  пройдены; у Navigator относительно точного baseline изменены только `AndroidManifest.xml`,
  `classes4.dex`, `classes12.dex`, добавлен `classes19.dex`. Аппаратные `GATE-007` и
  `GATE-009…022` остаются открытыми до установки и проверки на KX11.
- **03.09.2026:** аппаратная проверка подписанного Navigator 30.3.0 из пары Natro 2.7.0 по
  `IMG_8227.jpeg` повторно отклонила `WIN-015`: карта начинается у верхней границы окна, но
  карточка манёвра и вся группа скорости сохраняют одинаковый большой отступ. Проверка baseline
  30.3.0 показала, что оба элемента являются прямыми детьми одного безымянного
  `CarGuidanceController` со штатным верхним margin 12 dp; следовательно, смещён не отдельный
  составной знак, а внешний экран активного ведения. Добавлены `WIN-017` и `GATE-023`. В коде
  оконного режима отдельно контролируются общий `activity_container_controller`, динамический
  визуальный корень с `contextmaneuverview` + `speed_group` и все поздние `PaddingtonView` внутри
  Conductor-дерева. Отрицательный `translationY` не используется, полноэкранное состояние
  сохраняется для восстановления; новая APK без отдельного разрешения пользователя не собиралась.
- **03.09.2026:** пользователь потребовал до следующих точечных правок полностью разобрать
  исполняемую UI-цепочку карты Navigator 30.3.0 и отдельно добраться до настоящего контейнера
  левых кнопок. Добавлен `NAV-015`. По `IMG_8221.jpeg` существующая координатная overlay-кнопка
  отклонена не только по положению, но и по подложке и поведению появления. `WIN-013` заменён,
  `WIN-016` уточнён новым `WIN-018`: кнопка должна стать третьим ребёнком точного штатного
  контейнера, использовать его прямоугольный стиль и наследовать его полный цикл видимости и
  анимации без собственного таймера. Добавлен аппаратный `GATE-024`; APK не собиралась.
- **03.09.2026:** статический аудит `NAV-015` завершён и сохранён в
  `docs/NAVIGATOR_30_3_UI_CONTAINER_AUDIT_RU.md`. Он выявил различие landscape Guidance:
  `guidance_add_road_event` вложен в `MapControlsFrameLayoutRect`, поэтому валидируется общий
  вертикальный rail и предыдущая штатная обёртка, а не одинаковый непосредственный parent двух
  ID. Реализован `WIN-018`: координатный overlay, блокировка экранной позиции и собственный
  5-секундный fade удалены; один элемент создаётся в штатной прямоугольной оболочке, получает
  векторную иконку конечного размера 48 dp и вставляется третьим после voice-control в
  free-drive/Guidance. Во время Conductor transition он хранится только скрытым в слое Natro.
  Неактуальные ползунки размера/opacity убраны из двух экранов настроек, wire-поля сохранены для
  совместимости. Пройдено 20 статических тестов (1 platform-skip), baseline boundary self-check
  прошёл; запрещённые аварийные NavigationLayer/GuidanceCamera пути не возвращены. APK не
  собиралась и в git remote ничего не отправлялось до аппаратного `GATE-023/024`.
- **03.09.2026:** пользователь дал прямую команду собрать подписанный APK после `WIN-017/018`.
  Поскольку Natro 2.7.0 / `208021303` уже выпущена, новая пара получает Natro 2.7.1 /
  `208021304`; Navigator сохраняет 30.3.0 / `739564630`. Выдача разрешена только после публикации
  точного source commit, успешной CI-сборки этого commit, локальной подписи стабильным сертификатом
  `6e9855ae…65c75` и всех статических release-gates. `GATE-023/024` остаются аппаратными и не могут
  считаться закрытыми до проверки пользователем на KX11.
- **03.09.2026:** первый CI-прогон 236 для 2.7.1 корректно остановил выпуск на двух устаревших
  Java contract-assertions: они всё ещё требовали удалённый собственный opacity-slider и прежнюю
  текстовую форму условия видимости кнопки. Исполняемый код скомпилировался; тесты приведены к
  `WIN-018` и теперь явно запрещают собственные size/opacity sliders в обоих экранах настроек.
  Кандидат из проваленного run не подписывается и не выдаётся.
- **03.09.2026:** подписанная пара 2.7.1 собрана только из опубликованного source commit
  `ac32d74895623ceb8864649d1fc4c286bf227521` (дерево
  `8179bd30b5b88e884144d59dd5c5faa354ccb1d7`) после полностью успешного push CI run 238
  (`33728895500`); SHA-256 точного CI-архива кандидата
  `78e1f0b2f54e704c17d945b51a4ef69b4670266cfea86d3adbc8cb73f01b593e`. Итоговые APK:
  Natro 2.7.1 / `208021304`, SHA-256
  `e42e00dd900d74bd045a42250fa06b14356be9a74d215b33a3c2caed68fc3cf4`; Navigator 30.3.0 /
  `739564630`, SHA-256 `89f2779bd17da515ee60dacb29f6bec9a56abfc58f1ce6856b92d1fbc83ede25`.
  Оба имеют одного подписанта со стабильным сертификатом `6e9855ae…65c75`; Natro проверен как
  обновление поверх 2.7.0 (`208021303`) с v2+v3, Navigator — v3. Boundary gate сохранил 17 223
  защищённые записи: изменены только `AndroidManifest.xml`, `classes4.dex`, `classes12.dex`,
  добавлен `classes19.dex`. Статические release-gates пройдены; `GATE-023/024` и остальные
  аппаратные проверки остаются открытыми до установки и приёмки на KX11.
- **03.09.2026:** аппаратная проверка подписанной пары 2.7.1 повторно провалила
  `CLUSTER-017/GATE-009`: после обновления HUD-карта запускается автоматически, а карта панели
  приборов появляется только после открытия настроек Natro. Повторная публикация `TextureView`
  и HELLO-reconnect из 2.6.9 не закрыли потерю первого lifecycle-запуска; настройки не могут
  оставаться неявным восстановителем.
- **03.09.2026:** по `IMG_8228.jpeg` пользователь повторно отклонил `WIN-017/GATE-023`: при
  активном маршруте карточка манёвра и группа скорости всё ещё вместе расположены слишком низко.
  Одновременно выявлена регрессия `WIN-019`: после реализации штатного rail по `WIN-018` тап по
  карте перестал скрывать/показывать штатные элементы. Добавлен `GATE-025`; требуется проверить
  синхронное изменение rail внутри `ACTION_DOWN` и реальный touch pass-through `controlLayer`, не
  возвращая отдельный overlay, отрицательный `translationY` или аварийные map-layer механизмы.
- **03.09.2026:** повторный исполняемый аудит локализовал три независимые причины. Для
  `WIN-017` точный Guidance `PaddingtonView` больше не зависит от того, оказался ли он потомком
  общего Conductor-host, его известный нулевой XML-padding не снимается с уже изменённого
  runtime-значения, а late-attach запись перехватывается последним pre-draw guard. При временных
  дубликатах ID выбирается верхний attached/visible controller. Для `WIN-019` удалена синхронная
  перестройка rail из `ACTION_DOWN` и
  полноэкранного `controlLayer`: полный жест только наблюдается, а rebind ставится после
  завершённого одиночного тапа; во время pan/pinch poller иерархию не меняет. Для `CLUSTER-017`
  boot/package-replaced путь сам стартует Binder endpoint и ждёт готовности аутентифицированного
  Navigator до 30 секунд в рамках одного pending-запуска, не полагаясь на вход в настройки.
  Все три изменения остаются аппаратно открытыми до `GATE-009`, `GATE-023` и `GATE-025`.
- **03.09.2026:** по `IMG_8233.jpeg` пользователь уточнил, что размещение карточек обязано
  учитывать изгиб и направление маршрута, а по `IMG_8229.jpeg`/`IMG_8230.jpeg` повторно указал
  на отсутствующую ножку светофора. Добавлены и реализованы `MAP-038/MAP-039`, аппаратный
  `GATE-026`. События сохраняют точную позицию на полилинии; общий screen-space проход оценивает
  все восемь штатных положений по реальным размерам/anchor, текущему viewport, курсору, камерам,
  подходу к событию, участку после него и ближайшему повороту. Светофор остаётся в исходной
  координате и одним `CompositeIcon` получает штатную плашку плюс гарантированную screen-facing
  векторную ножку; обновление named parts не очищает композицию между кадрами. Заодно завершена
  часть `MAP-036`: курсор и конечная точка растрируются из vector-path сразу в целевой размер,
  а расчёт штатного светофора использует фактический масштаб его текстуры. Локальные статические
  и геометрические тесты проходят; фактические кадры и 30-минутная стабильность остаются открыты
  до `GATE-020/GATE-026` на KX11. Следующий кандидат имеет новую идентичность Natro 2.7.2 /
  `208021305`; публикация source commit, CI и подпись выполняются только после полного
  автоматического release-прогона.
- **03.09.2026:** подписанная пара Natro 2.7.2 / Navigator HUD v2 выпущена из source commit
  `075f4a8ec8b879205e6fb66a65b38b45712b7815`, tree
  `25a008144d1f3fa540bd05064d14578ce49a7f5c`. GitHub Actions run `240`
  (`33756426074`) успешно выполнил unit/static tests, сборку Natro, Helper 70, Navigator
  `classes19.dex`, baseline-verifier и упаковку кандидата; SHA-256 архива кандидата
  `1be47cc9ce86d6c0717778a8da164c42d8c217cc33d67171f52eedf1d35bdeb0`. Итоговые APK:
  Natro 2.7.2 / `208021305`, SHA-256
  `b7b453c8e579cac9278e9154a2d4cb91b2f6090cecae3553f435b7cdb9d099cd`; Navigator 30.3.0 /
  `739564630`, SHA-256 `bb100ccabd30c2adce252f168625817956666309a3b995401b59869440231917`.
  Оба APK имеют одного подписанта со стабильным сертификатом `6e9855ae…65c75`; Natro
  автоматически подтверждён как обновление поверх 2.7.1 с тем же package/certificate и
  возрастающим versionCode. Boundary gate сохранил 17 223 защищённые записи: изменены только
  `AndroidManifest.xml`, `classes4.dex`, `classes12.dex`, добавлен `classes19.dex`.
  Автоматические release-gates пройдены; `GATE-007/009/020/023/024/025/026` остаются открытыми
  до установки, маршрутных снимков и 30-минутной приёмки на KX11.
- **03.09.2026:** аппаратная проверка 2.7.2 по `IMG_8236.jpeg` повторно отклонила верх активного
  Guidance: между верхом видимого окна карты и карточкой манёвра/`speed_group` остаётся большой
  общий пустой пояс. `WIN-017/GATE-023` не закрыты и заменены уточнённым `WIN-020/GATE-027`:
  требуется найти реального runtime-владельца вертикальной координаты, а не повторять guard
  вложенного `PaddingtonView`. Пользователь также сообщил, что отображаются не все светофоры.
  Добавлены `MAP-040/GATE-028`: размещение и collision могут выбирать ножку, но не сокращать
  полный snapshot валидных маршрутных событий; жизненный цикл каждого светофора проверяется по
  устойчивой route identity и точной позиции на полилинии. Исправления и новая APK ещё не
  выпускались.
- **03.09.2026:** повторный аудит локализовал обе причины отказа после 2.7.2. Верхняя Guidance-
  группа теперь выравнивается по фактической экранной координате `activity_search_map_view`:
  компенсируется margin единого `MATCH_PARENT`-корня, а не padding предполагаемого контейнера;
  два прямых верхних ребёнка сохраняют штатные 12 dp, низ/ETA не переводится. Коррекция
  пропускает transient-анимацию, не использует `translationY`, восстанавливает исходные margin и
  не накапливает сдвиг, если vendor-parent его игнорирует. Неполный набор светофоров был вызван
  нашим фильтром 30 м с выбором MAIN/ближайшего и режимом конфликтов `EQUAL`. Оба механизма
  удаления исключены: весь опубликованный snapshot рендерится, а screen-space проход меняет
  только сторону ножки. Publisher запрашивает 16 событий, изолирует ошибку одного native-wrapper
  и формирует ключ из route epoch, точной позиции, секции и стрелки. Автоматические контракты
  обновлены. Следующий кандидат имеет новую identity Natro 2.7.3 / `208021306`; аппаратные
  `GATE-027/GATE-028` остаются открытыми.
- **03.09.2026:** по `IMG_8242.jpeg` пользователь добавил два опциональных маршрутных типа для
  обеих независимых карт: штатные синие знаки искусственной неровности и маленькие локальные
  ограничения скорости. Добавлены `MAP-041/MAP-042/GATE-029`. Для неровностей подтверждён
  безопасный публичный источник `DrivingRoute.getSpeedBumps()` с точной `PolylinePosition` и
  штатный drawable Navigator; подключать Automotive `NavigationLayer` запрещено. Для локальных
  ограничений до реализации сначала фиксируется точный route API/metadata-контракт, чтобы не
  перепутать их с текущим лимитом или камерами. HUD и панель получают независимые переключатели
  и масштаб каждого типа; реализация ещё не объявлена готовой и требует KX11.
- **03.09.2026:** пользователь уточнил объём `IMG_8242.jpeg`: дополнительные маленькие
  ограничения скорости не нужны. `MAP-042/GATE-029` заменены `MAP-043/GATE-030`; новый слой не
  читает `DrivingRoute.getSpeedLimits()` и не дублирует текущий лимит либо камерные знаки.
  Текущая реализация ограничена искусственными неровностями из `getSpeedBumps()`; плохие и
  грунтовые участки `getRuggedRoads()` остаются вне задачи до отдельного подтверждения.
- **03.09.2026:** после аудита точного API Navigator 30.3.0 пользователь подтвердил неровности
  только активного маршрута. Режим «везде» исключён: глобальный `RoadEventsLayer` не экспортирует
  `EventTag.SPEED_BUMP`, поэтому соседние `DANGER`/POI не подменяют физические неровности.
  Добавлен отдельный `SpeedBumpMapLayer`: точка берётся из `getSpeedBumps()` через штатный
  `pointByPolylinePosition()`, хвост векторного знака остаётся на маршруте, прошедшие точки
  скрываются, а HUD и панель имеют независимые переключатель, масштаб и приоритет. Дополнительный
  слой ограничений скорости не добавлен. Изменение входит в тот же ещё не выпущенный кандидат
  Natro 2.7.3 / `208021306`; аппаратный результат остаётся за `GATE-030`.
- **03.09.2026:** видео `IMG_8239.MOV` и `IMG_8241.MOV` подтвердили провал `WIN-009/WIN-018`
  и `GATE-024`: после штатного автоскрытия дорожного события и Алисы/голоса третья кнопка режима
  Natro остаётся на экране. Исполняемый аудит локализовал причину в
  `ensureModeButtonAttachedToStockRail()`: принадлежность общему вертикальному `LinearLayout`
  ошибочно считалась достаточной для наследования lifecycle, хотя Navigator скрывает/анимирует
  отдельные штатные слоты. Метод безусловно вызывает `setVisibility(VISIBLE)`, а poller повторяет
  это каждые 1 секунду в окне или 5 секунд в полном экране; скрытый, но attached rail при этом
  остаётся валидным кандидатом. Исполняемый код в рамках диагностики ещё не исправлялся.
- **03.09.2026:** для кандидата 2.7.3 исправлен провал автоскрытия `WIN-009/WIN-018` без
  собственного таймера. Rail теперь хранит точные road-event control, его landscape-wrapper и
  voice control. Покадровый observer только читает их effective visibility до общего
  `LinearLayout`, объединяет состояние пары и копирует alpha/scale/translation с видимого
  штатного слота; при нажатии сохраняется собственный pressed-state штатной оболочки. Поиск и
  reparent остаются вне pre-draw и вне текущего жеста карты, poller больше не вызывает
  безусловный `VISIBLE`, callback изолирован `Throwable` и удаляется в `destroy()`. Статические
  регрессии обновлены; аппаратные `GATE-024/GATE-025` остаются открытыми.
- **03.09.2026:** первая удалённая сборка 2.7.3, GitHub Actions run `33790260857` / №243,
  остановилась на одном устаревшем source-contract assertion в
  `NavigationHudV2ContractTest`: тест продолжал требовать удалённый helper `containsViewId()` и
  глобально запрещал `setTranslationY`, хотя новое зеркалирование кнопки обязано копировать
  translation штатного voice-control. Исполняемый Android-код на этой стадии не был причиной
  отказа, но последующие DEX/release-gates корректно не запускались. Java-контракт приведён к тем
  же exact-pair/pre-draw требованиям, что Python-регрессия; требуется новый полный CI run.
- **03.09.2026:** подписанная пара Natro 2.7.3 / Navigator HUD v2 выпущена только из
  опубликованного source commit `dfc6c050d04d1d4e173fd718256a0591bed03fdb`, tree
  `ebf7867adabbee298d451cfa6f2611cd6a19ec27`. Push GitHub Actions run №244
  (`33790723679`) полностью прошёл Android unit/static tests, сборку Natro, Helper 70,
  изолированную компиляцию Navigator `classes19.dex`, HUD Speed bridge, baseline-verifier и
  упаковку кандидата; SHA-256 точного CI-архива
  `0954d62eafc3c73c106bb772d461a8ccc5d6969853d6530af81c02d26e9bb554`. Итоговые APK:
  Natro 2.7.3 / `208021306`, SHA-256
  `1cd32b39bfc21f42c534ff472cd5b053240f177c49dbe3062c024e8c6a74307c`; Navigator 30.3.0 /
  `739564630`, SHA-256 `20f86afd12349b407054117c913400c56ca3419e4ab11ff151e832896df4fad7`.
  Оба APK имеют одного подписанта со стабильным сертификатом
  `6e9855aedc008bbdd8a7fbf3f490be07f964b7ac658a837a1592647a08365c75`; Natro подтверждён как
  обновление поверх 2.7.2: package/certificate совпадают, `versionCode` повышен с `208021305` до
  `208021306`. Проверены внутренние суммы, zipalign, схемы подписи (Natro v2+v3, Navigator v3) и
  точное совпадение упакованного `classes19.dex` с CI-кандидатом. Boundary gate сохранил 17 223
  защищённые записи Navigator: изменены только `AndroidManifest.xml`, `classes4.dex`,
  `classes12.dex`, добавлен `classes19.dex`. Автоматические и статические release-gates пройдены;
  `GATE-007/009/020/023/024/025/026/027/028/030` остаются открытыми до установки, маршрутных
  кадров и 30-минутной приёмки на KX11.
- **04.09.2026:** пользователь запросил отдельный, не связанный со слоем карты модуль строки
  `Пробка на 10 мин (1,2 км)` для HUD и панели, его автоматическое появление/скрытие, выбор
  цвета тела и хвостика наших светофоров отдельно для двух карт и полную повторную ревизию
  чёткости при уменьшении элементов. Добавлены `MAP-044…MAP-046` и `GATE-031/GATE-032`.
  Источник пробки зафиксирован как nullable `Guidance.leftInTrafficJam()` с
  `JamForecast.getDuration()/getMeters()`, без OCR и вычисления из ETA. APK без отдельной
  команды пользователя не собирается; аппаратная приёмка остаётся открытой до KX11.
- **04.09.2026:** после явной команды на сборку изменения `MAP-044…MAP-046` выделены в новый
  релиз Natro 2.7.4 / `208021307`; опубликованная 2.7.3 / `208021306` остаётся неизменной.
  Navigator сохраняет штатную identity 30.3.0 / `739564630`. Требуются полный CI, общая
  стабильная подпись пары и статические release-gates до выдачи APK.
- **04.09.2026:** подписанная пара Natro 2.7.4 / Navigator HUD v2 выпущена из source commit
  `7f119d8390ee086a9371cdbc98b9689bd9d5b813`, tree
  `62371c055a9b94aab5a84b58255d53a2ed5fc8d8`. GitHub Actions push run №246
  (`33861742269`) полностью прошёл Android unit/static tests, сборку Natro, Helper 70,
  изолированную компиляцию Navigator `classes19.dex`, HUD Speed bridge, baseline-verifier и
  упаковку кандидата; SHA-256 CI-архива
  `79c047d75a4d0947a6214952c4c02bae28c16d1b09a724a58c3875e6311818b0`, `classes19.dex` —
  `a575b75e8c66a4f6546399ca0903ac059cddf39ca9348197ae88d74a00ce0520`. Итоговые APK:
  Natro 2.7.4 / `208021307`, SHA-256
  `2d76cc6ac80018588ba0643ba8027fba5ec496756b982c3f6a8f82a3e6e4977f`; Navigator 30.3.0 /
  `739564630`, SHA-256 `6c4e9be783c19780c30b2abd2c6573690874712329a16bb39a43ce8364fe8324`.
  Оба APK имеют одного подписанта со стабильным сертификатом
  `6e9855aedc008bbdd8a7fbf3f490be07f964b7ac658a837a1592647a08365c75`; Natro подтверждён как
  обновление поверх 2.7.3 с возрастающим `versionCode`, схемами v2+v3, Navigator — v3.
  `zipalign`, внутренние суммы, identity DEX и KX11 static verifier пройдены. Boundary gate
  сохранил 17 223 защищённые записи Navigator: изменены только `AndroidManifest.xml`,
  `classes4.dex`, `classes12.dex`, добавлен `classes19.dex`. `GATE-031/GATE-032` остаются
  открытыми до установки и аппаратной приёмки на KX11.
- **04.09.2026:** аппаратная проверка подписанной пары выявила четыре связанных рассинхрона.
  Модуль пробки сохранялся при уже скрытой штатной плашке (`IMG_8248.jpeg`/`IMG_8249.jpeg`),
  сектор направления камеры мог оставаться без знака (`IMG_8250.jpeg`), а legacy bitmap
  манёвра переживал смену semantic action и показывал старое направление
  (`IMG_8255.jpeg`/`IMG_8261.jpeg`). Кроме того, наша единая синяя плашка на `IMG_8253.jpeg`
  теряет структурированные области штатной карточки на `IMG_8251.jpeg`: цветной указатель
  «Подольск» и отдельный нижний блок «2-й съезд». Добавлены `MAP-047/MAP-048`,
  `CARD-011/CARD-012` и `GATE-033…GATE-036`; `MAP-044` заменено уточнённым правилом.
  Исполняемый аудит локализовал причины: отсутствующий stock StatusPanel visibility-gate,
  разные conflict modes знака/сектора, неключованный трёхминутный bitmap-кэш и уплощённый
  bridge-контракт без `DirectionSignItem`/`ManeuverAuxiliaryContent`. В рамках диагностики
  исполняемый код и APK не изменялись.
- **04.09.2026:** пользователь предположил, что обычные светофоры без отсчёта отсутствуют, и
  указал на отсутствие отдельной настройки. Проверка исполняемого пути подтвердила дефект:
  Natro публикует и рисует только `Windshield.getTrafficLightsWithSignal()`, тогда как полный
  маршрутный набор находится отдельно в `DrivingRoute.getTrafficLights()`. Уточняющая проверка
  показала, что в основном Navigator он отрисовывается не самостоятельным пользовательским
  слоем, а feature внутри route pins `NaviGuidanceLayer` через `provideTrafficLightStyle()`;
  `RoadEventsLayer` соответствующего `EventTag` не имеет. На независимых картах Natro штатный
  Guidance-владелец намеренно отсутствует из-за постоянных запретов `NAV-011/NAV-014`, поэтому
  вместе с текущей ручной полилинией эти pins не появляются. Добавлены `MAP-049/GATE-037`:
  отдельные настройки для HUD и панели, безопасная собственная коллекция, подавление обычного
  знака живой плашкой той же точки, точный lifecycle маршрута и векторная перерисовка без
  размытия. Исполняемый код и APK на этом этапе ещё не изменялись.
- **04.09.2026:** пользователь явно разрешил реализовать весь пакет `MAP-047…MAP-049` и
  `CARD-011/CARD-012` и собрать новую подписанную пару. Рабочая версия повышена до Natro
  2.7.5 / `208021308`; Navigator сохраняет штатную identity 30.3.0 / `739564630`. До
  завершения исходных тестов, release-gates и общей стабильной подписи требования отмечены
  «в работе», аппаратные критерии `GATE-033…GATE-037` остаются открытыми для KX11.
- **04.09.2026:** пользователь добавил в тот же релиз исправление меню экрана водителя: оно
  должно работать поверх запущенной полной панели приборов Natro. `DIM-004` уточнено —
  собственная instrument-панель больше не является конфликтом, тогда как mNavi, штатный
  звонок/медиаменю, другая DIM-вкладка и выключенный дисплей по-прежнему имеют приоритет.
- **04.09.2026:** пользователь потребовал сделать понятными состояния устройств умного дома в
  меню экрана водителя. Добавлены `DIM-005/GATE-039`: live-значение берётся из общей явной
  `stateBinding`, а отсутствие подтверждённых данных отображается отдельным состоянием, без
  сохранения визуально правдоподобного старого значения.
- **04.09.2026:** по прямому требованию пользователя оформление дополнительного меню сверено с
  исполняемой реализацией mNavi 2.0 и закреплено как точный пресет `DIM-006/GATE-040`. Renderer
  использует неизменяемые в этом режиме цвета `#00FFFFFF/#FF6C7984/#FF197BC5`, белый выбранный
  текст, геометрию заголовка и строк 14/24 sp, отступ 8 dp и радиус 6 dp; старые конфигурации
  мигрируют на пресет, а редактор явно отделяет его от пользовательского оформления.
- **04.09.2026:** пользователь отклонил синюю карточку без пиктограммы и с самостоятельно
  добавленной строкой «проспект Ленина · направо» (`IMG_8266.jpeg`/`IMG_8267.jpeg`) и отдельно
  запретил рисовать знаки манёвра по semantic action. `CARD-003/CARD-011` уточнены новым
  `CARD-013/GATE-041`: принимается только точное изображение видимого штатного
  `image_maneuverballoon_maneuver`, ключованное текущими identity/sequence; synthesized-текст
  удаляется. Одновременно по `IMG_8268.jpeg` добавлены `CARD-014/GATE-042`: в обоих каталогах
  требуется отдельная сводка маршрута с тремя значениями и полосой пробок, без штатных кнопок.
- **04.09.2026:** пользователь передал `IMG_8269.zip` и диагностический журнал: избранные кнопки
  лаунчера периодически исчезают, их подписи не видны, а запуск точки разворачивает Navigator на
  весь экран. Добавлены `NATRO-016/WIN-021/GATE-044/GATE-045`; одинаковый набор маршрутов больше
  не пересоздаёт дерево, подписи имеют стабильную геометрию, а exact `MapActivity` после deep link
  дважды получает подтверждение оконного режима через bridge.
- **04.09.2026:** пользователь повторно подтвердил, что после перезагрузки HUD-карта запускается,
  а карта панели приборов — нет. Анализ журнала разделил две причины: Android 9 мог оставить
  `TextureView.isAvailable()==true` без первого callback, а общий со Settings внешний handoff
  останавливал Natro и позволял системе отложить фоновый старт Navigator. Добавлены
  `CLUSTER-018/GATE-043`: boot-путь запускает DIM напрямую, панель восстанавливает `Surface` из
  уже доступного `SurfaceTexture`, а отклонённый холодный attach повторяется ограниченно с новой
  generation. Код 2.7.6 и CI-проверки не заменяют обязательную приёмку на физическом KX11.

- **04.09.2026, NOTIF-009 / GATE-046:** пользователь сообщил о зависании уведомлений после
  исчезновения парктроников и немедленном продолжении при принудительном выключении системы.
  Пользователь отдельно отверг ограниченный срок удержания: пауза должна зависеть от изображения
  на экране. Старое предположение NOTIF-008 (`PrkgDstCtrlSts=2/3` означает видимость) заменено:
  28995 остаётся в диагностике, но не владеет паузой. Общий ECARX observer читает окно
  `com.ecarx.parking`, его display/type/viewVisibility/frame; положительная геометрия только
  на основном дисплее удерживает телефонные карточки и системную шторку. Закрытие требует
  двух свежих отсутствующих/скрытых снимков, разделённых 180 мс; отложенный callback только
  запрашивает новый снимок и не снимает паузу. UNKNOWN сохраняет последнее подтверждённое
  состояние без TTL, перезапускает канал и повторное чтение. Периодический запрос раз в секунду
  восстанавливает события появления/закрытия, если OEM не прислал callback. Независимое
  удержание камеры 360 сохраняется; штатная очередь и оставшееся время карточки не меняются.
  Анализ предоставленного `ecarx.adaptapi.jar` показал: WindowManagerImpl после RemoteException
  возвращает прежний массив, а успешное чтение создаёт новый; повтор массива отклоняется,
  в том числе после повторной инициализации. В журнал попадают переходы parking state и
  метаданные только парковочного окна, без заголовков и содержимого чужих приложений.
  Локально прошли 64 JUnit-проверки политики парковки, камеры, очереди, паузы и общего observer;
  observer скомпилирован против Android 9 API со stub только для двух application helpers.
  Полный Android CI выделен в проверку исходников без assemble/sign APK. Реальное совпадение
  метаданных окна с видимой графикой и восстановление после закрытия пока не проверены
  на KX11; `GATE-046` остаётся открытым.

- **04.09.2026, проверка NOTIF-009:** исходники опубликованы в
  `fix/parking-window-visibility`, commit `c20a86c2bed4a2f4fe408a37307112515af30813`,
  tree `9e552b1df43e488301e837e6dba4092fa35a59a8`; удалённое дерево совпало с локальным.
  GitHub Actions `33917088264` / `Verify parking window visibility source` успешно выполнил
  `testGeelyDebugUnitTest` с компиляцией всего Android-приложения. APK не собирался и
  не подписывался. Документирование результата не меняет проверенный код. `GATE-046`
  остаётся открытым до наблюдения реального открытия/закрытия графики на KX11.

- **04.09.2026, аудит перед Natro 2.7.7:** пользователь прямо разрешил сборку APK с
  парковочным исправлением, повторно потребовал публикацию исходников и проверку актуальности
  всего реестра. Сверены все 250 существовавших строк требований/критериев с доступной историей,
  опубликованным кодом и результатами CI. Устаревшие статусы подготовки 2.7.5/2.7.6 заменены
  отметкой включения в подписанную 2.7.6 без закрытия аппаратных критериев. Уточнены
  MAP-035/MAP-036 относительно MAP-046, NOTIF-007 относительно NOTIF-009, текущий механизм
  SHADE-006/SHADE-008 и устаревшее будущее время MUSIC-006. GATE-004 больше не ссылается
  на неопределённый «вчерашний Navigator», GATE-005 согласован со штатными названиями улиц,
  GATE-031 — со штатной видимостью пробки, GATE-034 — с камерами без известного направления.
  Добавлены последние требования CLUSTER-019…CLUSTER-021, KNOW-001…KNOW-003 и GATE-047/048.
  Технически проверяемые факты версии/сертификата отделены от пользовательской приёмки.
  Старые решения и их история не удалены. Полный результат и границы аудита —
  `docs/REQUIREMENTS_AUDIT_2026_09_04.md`. Новая сборка: 2.7.7 / 208021310; объём
  исполняемых изменений — NOTIF-009 и общее удержание шторки, без нового дизайна приборки
  и без замены Navigator/Helper. GATE-046/047/048 остаются открытыми.


- **04.09.2026:** по прямому запросу пользователя цифровая инструкция Geely KX11 / ECARX
  размещена рядом с реестром требований; добавлено `REL-016`. Опубликованы аналитические
  материалы, справочники и открытые вопросы. Исходные дампы, APK, декомпилированные чужие
  исходники и приватные данные не публикуются согласно `REL-008`. Автомобильные команды,
  исходники приложений и версии APK не изменены; сборка не запускалась, аппаратные проверки
  и открытые дефекты не закрыты.

- **04.09.2026, выпуск Natro 2.7.7 и итоговая сверка:** Android CI `33918568644`
  успешно выполнил `testGeelyDebugUnitTest` и `assembleGeelyRelease` для исходников
  `c84feb33e9d2d2b90a2d12ca30e6ca466edac7a3`, tree
  `ea2a0f793101b1fb3b438551393eb1edd97b84f5`. APK подписан стабильным ключом; проверены
  один подписант, v2/v3, zipalign, Natro / `ru.natro.statuswidget`, Android API 28,
  отсутствие debug-флага и увеличение versionCode с 208021309 до 208021310 относительно
  сохранённого подписанного 2.7.6. SHA-256 нового APK:
  `feb2a2aa258ad681475507d989d99e9f89bf20271a45edb97da3f1871e5a001e`.
  Отчёт: `release-manifests/NATRO-2.7.7.md`, машинные результаты:
  `release-manifests/NATRO-2.7.7-report.json`. Физическая установка и GATE-046 ещё не выполнены.
  В выпускной сценарий добавлены runtime-библиотеки Android build-tools; исправлено повторное
  чтение однострочного файла пароля. Эти изменения сценариев не меняют содержимое приложения.
  При финальной синхронизации найден и сохранён параллельный коммит `a2174b7` с цифровой
  инструкцией и REL-016. После объединения реестр содержит **259 строк: 211 требований
  и 48 критериев**; KNOW-001/002 и аудит исправлены с учётом уже опубликованной базы.
  В базу знаний добавлена новая политика NOTIF-009 с точным источником и открытой аппаратной
  проверкой; прежняя политика 28995 отмечена исторической. Navigator/Helper и дизайн не изменены.

- **05.09.2026, продолжение исследования Geely:** по уточнению пользователя прежние 14
  открытых вопросов признаны приоритетной очередью, а не исчерпывающим перечнем всех систем
  автомобиля. Добавлено `KNOW-004`, расширен `GATE-048` без закрытия аппаратных проверок.
  Подготовлены [матрица охвата](docs/geely-kx11/research-2026-09-05/COVERAGE_RU.md) из
  **94 карточек**: 86 новых (25 — комфорт, 24 — силовая установка/шасси, 25 — архитектура,
  12 — Android/мультимедиа/связь) и 8 продолжений прежнего исследования — камера, PAS,
  HUD, DIM, навигация, медиакнопки, ANCS и голос; [журнал исследования](docs/geely-kx11/research-2026-09-05/RESEARCH_LOG_RU.md)
  и [план недостающих данных](docs/geely-kx11/research-2026-09-05/DATA_PLAN_RU.md).
  Расширена инвентаризация уже полученного корпуса; первичный каталог содержит **4440
  деклараций**, проведён отдельный аудит нулевых значений. Декларация API, живой сервис,
  подтверждённое оснащение и независимый физический эффект остаются разными уровнями
  доказательств; число деклараций не является числом функций или гарантией полного охвата.
  Для дальнейшего разбора сначала используются сохранённые материалы, новый сбор привязан
  к конкретному пробелу. Реестр теперь содержит **260 строк: 212 требований и 48 критериев**.
  Текущий выпуск Natro 2.7.7, политика `NOTIF-009` без TTL, открытые `GATE-046/047/048`,
  ограничения `DRIVE-002` и `REL-008` сохранены; версии, исходники приложений и автомобильные
  настройки в этом исследовании не менялись.

- **05.09.2026, новые аппаратные дефекты после обновления:** получены
  `Natro-ANCS-2026-09-05_14-30-12.log` и `status-widget-debug(20260905-113110).txt`,
  затем два фото основного Navigator и HUD в 14:33. История 2.7.5 отделена от 2.7.7:
  до обновления ANCS достиг READY в 14:25:50, после обновления в предоставленном интервале
  READY отсутствует. Подтверждены запоздалая публикация карты приборки после Settings,
  неучтённый advertisement reassert в ANCS и несовместимость artwork-возможностей Navigator.
  CLUSTER-018 и CARD-013 отмечены аппаратными ошибками, добавлены CLUSTER-022, ANCS-013
  и выпускной критерий REL-017. Ранее открытые проверки не закрываются; изменения
  касаются восстановления и совместимости, оформление приборки не переутверждается.

- **05.09.2026, дополнение NATRO-016 / GATE-044:** после нажатия системной кнопки «Домой» избранные маршруты всё ещё периодически исчезают. Предыдущий статус 2.7.6 не является приёмкой. После layout дочерних view повторно проверяется видимость проекций; отсутствующая исходная геометрия повторно связывается. Поведение при активном маршруте не меняется. В приложенном журнале нет отдельного события исчезновения плиток: привязка этого дефекта к конкретной секунде не установлена.

- **05.09.2026, уточнение пользователя об исчерпании корпуса и досборе:** добавлены
  `KNOW-005/GATE-049`. Пользователь требует продолжать исследование после промежуточной
  публикации до исчерпания относящегося к задаче доступного содержимого. Итоговый инструмент —
  адресный macOS-сборщик недостающих файлов с инструкцией
  `tools/geely-macos-collector/README_RU.md`, точными целями, SHA-256, дедупликацией и
  отчётом отсутствия/отказа доступа; проценты понимания автомобиля не выводятся из счётчиков
  извлечённых записей. Исходники сборщика и49 локальных проверок (включая Bash-launcher,
  synthetic ADB/qconn и проверку архива) готовы; нужен реальный запуск Mac/KX11, `GATE-049` открыт. Текущий выпуск Natro 2.7.7, политика `NOTIF-009`
  без TTL, открытые `GATE-046/GATE-048`, `DRIVE-002` и `REL-008` сохраняются; это исследование
  и подготовка инструмента сбора, без сборки APK или исполнения автомобильных команд.

- **05.09.2026, готовый адресный Mac-сборщик:** опубликован комплект1.0.0 с22 Android-путями,5 QNX-путями и13 проверками пакетов; каждая цель связана с доказательством. База19069 SHA построена из реальных доступных байтов158 зарегистрированных источников;925 SDK-контейнеров/DEX сопоставлены независимо. Повторный разбор включает все6 Bluetooth архивов,621 DEX,25 контрактов комфорта,42 выбранных cruise DEX,32 PCAP и LV initramfs. Найденный ts-dm-service исключён из отсутствующих; подтверждены9 отсутствующих climate/BT файлов. Прошли49 офлайн-проверок, включая запуск через Bash из папки с пробелами и целостность полученного архива. Настоящий запуск на Mac/KX11 ещё не выполнен, GATE-049 открыт. Новые отчёты: `docs/geely-kx11/research-2026-09-05-next/README_RU.md`. Исходные260 строк требований/критериев сохранены, добавлены KNOW-005 и GATE-049: всего262. Natro2.7.7, NOTIF-009 без TTL и остальные аппаратные gates не изменены.

- **05.09.2026, подписанная пара 2.7.8:** исходники выпуска
  `b0c64a5624158887c3f81a6b344f37e0a679c840`, дерево
  `6a476c61a9bec97fe478a569dab8f40082e2ce1e`; CI `33964848193` успешен:
  1796 тестов приложения и 18 проверок инструментов Navigator. Проверены единый прежний
  сертификат, Natro versionCode +1, равная install-over identity Navigator, Android 9,
  artwork IPC/hello 0x7fe7 и неизменность 17223 защищённых записей исходного Navigator.
  Обязательны оба APK: Natro 2.7.8 и Navigator-30.3.0-Natro-2.7.8. Helper 70 не меняется.
  [SHA-256 и протокол проверок](docs/releases/2.7.8/release-verification.json).
  Новые исходники и тесты не закрывают CLUSTER/CARD/NATRO/ANCS на автомобиле;
  GATE-041/043/044 остаются открытыми. Объединены новые записи исследования KNOW-005/GATE-049,
  все прежние ID сохранены: **265 строк, 216 требований и 49 критериев**.

- **05.09.2026, результат проверки 2.7.8:** пользователь передал IMG_8278.jpeg/IMG_8279.jpeg: исходный знак появился, но название «Еринское ш.» и компоновка карточки различаются. Также курсор HUD/приборки застывает на прежнем маршруте при уходе с него, хотя штатный Navigator продолжает движение. Добавлены CARD-015, MAP-050, GATE-050; GATE-041 уточнён. Наличие artwork capability не считается приёмкой всей карточки.

- **05.09.2026, уточнение слоёв перед выдачей 2.7.9:** пользователь запросил низкий приоритет обычных светофоров сразу после маршрутной линии и названий улиц. В интерфейсе это «Обычные светофоры активного маршрута» / RouteTrafficLightMapLayer, отдельный от live-сигнала и секунд TrafficLightMapLayer. Добавлено MAP-051; GATE-019 уточнён; всего 269 строк, 219 требований и 50 критериев. Предварительная сборка карточки/координат прошла CI, но окончательный выпуск должен включать и это уточнение.

- **05.09.2026, опции топлива HUD:** по IMG_8276.jpeg добавлены NATRO-017/GATE-051: пороги в литрах, режим бака 64 л/ручной, скрытие остатка и ограничение «Долить» парковкой, транзакционное применение/отмена. В исходнике найдено, что automaticCapacity ранее не учитывался при расчёте, а числовой Derived.refill_fuel не вычислялся общей numericValue; требуется связать UI и рабочую политику. Всего 271 строка: 220 требований и 51 критерий.

- **05.09.2026, дополнения громкости и мягких краёв карты:** MUSIC-011/GATE-052 уточняют реальные деления, иконку и краткий показ только при изменении (IMG_8277.MOV); MAP-052/GATE-053 добавляют степень и размер мягких краёв карты. Текущая сборка 2.7.9 расширена до этих требований, прежняя промежуточная CI-пара ещё не выдана. Всего 275 строк: 222 требования и 53 критерия.

- **05.09.2026, аудит переданного mHUD 6.1:** добавлены NATRO-018/MUSIC-012/GATE-054. MediaInfoReceiver принимает is_playing; HUD Presentation очищает обложку и медиа-тексты при false, прямой проверки звонка в этом пути нет. Каталог сверяется с реальными пунктами меню и режимами, не только именами enum. Всего 278 строк: 224 требования и 54 критерия.

- **05.09.2026, выпуск 2.7.9 / 208021312:** исходник `9f2b7bd52e847ed3424ac6c7a2fe2149f20e7c9b`,
  дерево `9d8145ab4728a7bd9075665eaf1bd3fdcacdee49`, CI `33971716682`: 1831 тест приложения
  и 32 проверки инструментов пройдены без ошибок/пропусков. Стабильным сертификатом подписаны
  Natro (`62d3324abe9d218ff0c76605a2c28f8ff5868b1b60e1083bb2f7b7c5f480cf74`) и Navigator
  (`e359c7878a7caa7e7495130926a66e39ddd7361d8f42fbf7d64fcba3ead30940`). Проверены реальные
  APK, совместимость с 2.7.8, exact classes19/capabilities и 17223 неизменённых записи baseline.
  Включены карточка/позиция/светофоры, топливо, громкость, мягкие края и медиа-пауза;
  [матрица выпуска](docs/releases/2.7.9/README_RU.md). GATE-019/041/050…054 остаются открытыми.
  Аудит mHUD сохраняет явный список недостающих составных элементов и режимов; он не объявлен
  полным переносом всех функций. Реестр: 224 требования + 54 критерия, все прежние ID сохранены.

- **05.09.2026, уточнения пользователя после 2.7.9:** CARD-016/GATE-055 фиксируют неполную стрелку; фотографии сделаны в разное время, разница расстояний не является дефектом. NATRO-019/GATE-056 требуют единого модуля поворотников вместо двух пунктов добавления. 226 требований + 56 критериев.

- **05.09.2026, IMG_8290.jpeg:** MAP-053/GATE-057 фиксируют события вне маршрута при выбранном ROUTE_ONLY на приборке. 227 требований + 57 критериев.

- **05.09.2026, уточнение по MAP-053:** дефект наблюдается только на приборке, HUD пользователь не считает неисправным. Сохранение/чтение clusterMap и hudMap в коде разделено; точные настройки обоих профилей на момент фото отсутствуют, причина различия проявлений не доказана.

- **05.09.2026, масштаб HUD:** пользователь допускает, что значки были не видны на HUD из-за другого масштаба. Это согласуется с общей реализацией, но не доказано фотографиями. MAP-053 применяется независимо к обоим профилям, отсутствие видимых значков не закрывает проверку HUD. Следующая версия по REL-004 — 2.8.0 / 208021313.

- **05.09.2026, выдача 2.8.0:** исходники 4fc148925e66d0c8e019e29fc8cf06bff4c80762, CI 33978715779 успешен: 1836 тестов приложения + 42 проверки инструментов. Подписаны оба APK стабильным сертификатом, проверены versionCode +1/совместимость/17223 защищённые записи и новые пути в DEX. CARD-016/NATRO-019/MAP-053 включены в подписанную пару; GATE-041/055/056/057 и прежние аппаратные критерии остаются открытыми. [Протокол](docs/releases/2.8.0/README_RU.md).

- **05.09.2026, штатные команды таблички:** пользователь разрешил переход на команды ManeuverView и отдельно исправил трактовку прежнего требования: нужны оригинальные знаки, а не обязательный снимок ImageView. Добавлены CARD-017/CARD-018/GATE-058; прежние технические ограничения способа передачи отмечены заменёнными без отмены оригинальности графики. Разрабатываются исходники и проверки, выпуск 2.8.0 и открытая аппаратная приёмка сохраняются.

- **05.09.2026, IMG_8283.jpeg / полосы на маршруте:** пользователь уточнил, что проверяет знак на карте. CARD-019 фиксирует найденный исходный путь LaneSign → LaneSignBalloon → штатная LaneSignBalloonTextureFactory → map placemark; это не screenshot и не собственные стрелки. Нативный выбор ресурсов не приравнивается к восстановлению полного NavigationLayer.

- **05.09.2026, проверки механизма таблички:** 39 локальных проверок прошли; exact-baseline smali после удаления 14 обёрток восстановлен побайтово, classes8.dex собран и закреплён; все исходники Navigator bridge прошли javac/D8 с Android 36, новые классы загрузки/рисования Natro компилируются. Полный Gradle-прогон остановился на загрузке AGP/Kotlin (Network is unreachable), запуск APK и KX11 не выполнялись. Auto-review дважды остановил source push, не приняв восстановленное прежнее разрешение; дальнейшие попытки публикации прекращены, код и workflow сохранены локально. Отчёт: docs/NATIVE_MANEUVER_COMMANDS_2026_09_05_RU.md.

- **05.09.2026, прямое разрешение публикации:** после запроса на отправку подготовленной ветки fix/maneuver-commands-20260905 с нашими исходниками и тестами в публичный AGKulikov/Status пользователь ответил «Разрешаю». Разрешение применяется к source push и проверкам workflow; сборка и публикация APK этим сообщением не запрошены.

- **05.09.2026, публикация и полный CI таблички:** после прямого «Разрешаю» опубликована fix/maneuver-commands-20260905, первый публичный HEAD ff5b56bd96816296103ed92658c8c3488ec63602, дерево cd91987a2ecc20582da207eea1fcea6655a7e554 совпадает с подготовленным локальным. GitHub CI 33987122595 успешно выполнил компиляцию Navigator bridge, полный testGeelyDebugUnitTest и проверки инструментов без сборки APK. При заключительном просмотре исправлено отображение штатной подписи «Далее» перед следующими полосами; добавлена проверка совместного очищения подписи и полос. Повторный CI проверяет эту правку; GATE-058 остаётся открытым.

- **05.09.2026, итог повторного CI таблички:** commit d5f68456fa8639dc0c2e10c001d7d4cc8c488038, дерево 5d1ff438a18efc18ba7b1402a9199944d3a1bc4b; публикация совпадает с локальным исходником. [CI 33987507476](https://github.com/AGKulikov/Status/actions/runs/33987507476) завершился успешно: компиляция Navigator bridge, полный testGeelyDebugUnitTest и 50 проверок инструментов. Исправление подписи следующих полос включено. APK не собирались; версия выпуска остаётся 2.8.0, GATE-058 требует проверки на KX11.
