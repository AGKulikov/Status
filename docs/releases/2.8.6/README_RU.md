# Подписанная пара Natro 2.8.6

11.09.2026 по прямому поручению пользователя выпущены Natro
**2.8.6 / 208021319** и совместимый Navigator **30.3.0 / 739564630**.
Рабочая ветка — `feature/natro-2.8.6-trip-ui-media`. Исполняемый подписанный commit —
`a4e8086a08f1146286c086f5810cb006f302d1fa`, дерево —
`b8a4e057bead0d81aac3011f3daea67d4b383ae3`; реализация внесена commit
`1c70ca3cb73db221eb853523ae0ffc0598a98738`, дерево
`ea6136a400d11e4cf11f64d9110b4d7fc3adc8f6`. Обычные CI-прогоны
[`34612692293`](https://github.com/AGKulikov/Status/actions/runs/34612692293)
(специализированный 2.8.6) и
[`34612692371`](https://github.com/AGKulikov/Status/actions/runs/34612692371) /
run 268 (общий Navigator HUD v2), а затем защищённый
[`34613313248`](https://github.com/AGKulikov/Status/actions/runs/34613313248) /
run 269 завершены успешно. Аппаратные критерии KX11 остаются открытыми.

## Что исправлено

- `NATRO-024`: ошибочные PA-поля 2.8.5 полностью исключены. Пробег Trip 2 читается
  из `DstTrvld2`, средняя скорость и её единица — из штатных приборных CarSignal.
  Время выводится из той же свежей пары завершёнными минутами; эталон пользователя
  `17,4 км / 24 км/ч` даёт `00:43`. Стабильные ID элементов сохранены.
- `CARD-025`: кэш автоширины/автовысоты зависит от фактического размерообразующего
  содержимого, поэтому карточка HUD и приборки не только уменьшается, но и снова растёт.
  Изменение одной скорости не вызывает лишний layout.
- `MAP-059`: полилиния каждой валидной альтернативы создаётся независимо от выноски.
  Неполные fork/name/delta данные больше не скрывают линию; выноска использует доступную
  точку и резервный текст `Вариант N`. Ошибка регионального `hide(Subpolyline)` сохраняет
  полную линию.
- `MUSIC-014`: новый key press не становится за Binder проигрывателя, зависшим дольше
  750 мс, а возвращается штатному Android media dispatcher; stale route обновляется.
  Active sessions дополнительно сверяются раз в 2 минуты на resolver-потоке.
- `NATRO-023`: без снижения разрешения, FPS, сглаживания, частоты данных или состава
  слоёв убраны snapshot-identity layout churn, синхронный boot preference fsync и
  PackageManager/icon work из `AppsAdapter.getView`.

## Матрица доказательств

| Требование | Проверка исходника | KX11 |
|---|---|---|
| NATRO-024 | typed getters/IDs, конвертация, freshness и запрет PA/ITripData | GATE-076 открыт |
| CARD-025 | content fingerprint в HUD и приборке, двусторонние последовательности | GATE-077 открыт |
| MAP-059 | независимые line/callout paths и безопасные fallback | GATE-078 открыт |
| MUSIC-014 | bounded queue, Binder-stall fallback, periodic refresh и main-hot-path audit | GATE-079 открыт |
| NATRO-023 | параметры качества не снижены | GATE-075 открыт |

## Сборка и подпись

Оба APK подписаны прежним общим сертификатом SHA-256
`6e9855aedc008bbdd8a7fbf3f490be07f964b7ac658a837a1592647a08365c75`.
Защищённый CI `34613313248` восстановил точный Navigator baseline SHA-256
`663018fb66074e001eed7caba8e33bee1bcf78f6798bc84949d253dcb348f27f`,
собрал пару из commit `a4e8086`, проверил 1 876 JVM-тестов без пропусков/ошибок,
девять наборов проверок инструментов и загрузил ровно два APK. HUD Speed Bridge в
выдачу не включён.

| APK | Package / version | SHA-256 |
|---|---|---|
| `Natro-2.8.6-signed.apk` | `ru.natro.statuswidget` / `2.8.6 (208021319)` | `de04b15fca9963f8f795534af1d759bd257543a8bb0be4d1330c846576eb630d` |
| `YN_30.3.0_Natro-HUD-v2-signed.apk` | `ru.yandex.yandexnavi` / `30.3.0 (739564630)` | `da4f7983f2c34e6db1ff0dbbaa2f1f8baf98de02eb8a4d477832b727e284cb9c` |

Повторная проверка скачанного артефакта подтвердила `SHA256SUMS`, CRC,
четырёхбайтовый `zipalign`, одного подписанта и схемы v2+v3 для обоих APK. У
Navigator сохранены 17 222 защищённые записи baseline; изменены только
`AndroidManifest.xml`, `classes4.dex`, `classes8.dex`, `classes12.dex`, добавлен
`classes19.dex`; `resources.arsc` и `res/` совпадают побайтно.

## Установка

Устанавливать поверх текущей подписанной пары без удаления приложений и без очистки
данных: сначала `YN_30.3.0_Natro-HUD-v2-signed.apk`, затем
`Natro-2.8.6-signed.apk`. Общий прежний сертификат обеспечивает криптографическую
совместимость обновления; фактический результат PackageManager на конкретной KX11
фиксируется аппаратной приёмкой.

Исходниковая/CI-проверка не доказывает совпадение Trip 2, визуальный рост карточки,
полноту альтернатив или задержку кнопок на физическом автомобиле. Эти результаты
закрываются только пользовательской проверкой `GATE-075…079` на KX11.
