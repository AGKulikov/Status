KX11 ANCS Helper v47 — transport-v2 candidate
Bundle ID: ru.natro.kx11ancshelper
Build: 47
Marketing version: 47.0
Paired runtime: matching Android transport-v2 source checkpoint
Status: source/replay accepted; hosted Xcode build and physical-device matrix remain release gates

Implemented:

- clean Route A (iPhone Peripheral / Android Central) and Route B
  (iPhone Central / Android Peripheral) coordinators with one top-level owner;
- UUID-only F201/F202 discovery, mutual installation-ID H proof, encrypted write+indicate CONTROL,
  fixed 20-byte H/C/A and fixed 8-byte CRC telemetry;
- durable BRS2 confirmed-quiescence switching: persisted freeze, exact C/A commitment, local
  terminal, explicit owner-zero, drain and fresh target generation;
- same-role loss recovery, stale callback fencing, bounded fail-closed deadlines, corrupt/CLOSED
  dual-namespace drain and v46 selected-role migration;
- production battery, power, network and lock telemetry routed only to exact ACTIVE generation;
- no protocol local name/address fallback, no automatic pair deletion and no cache-clear workflow.

Verified in the portable suite:

- all shared wire fixtures and strict negative frames;
- canonical switch vectors, 40 bidirectional switches and 20 same-role recoveries;
- timeout, stale, conflict, retry, duplicate C, client-first close, radio-loss and restoration
  callback inversions;
- v47 Xcode file membership, plist/background modes, workflow artifact contract and package file
  allowlist.

Not yet claimed:

- physical-device reconnect stability;
- a completed macOS/Xcode build in this Linux workspace;
- immunity from iOS user force-quit, Bluetooth/pairing policy, background scheduling or another
  system UI's historical display cache.

Install over v46 with the same bundle ID and existing pair. Do not combine v47 with a legacy
Android wire implementation. Do not label this candidate stable until the README field matrix and
hosted unsigned xcodebuild have passed.
