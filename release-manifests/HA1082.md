# HA1082 release record

HA1082 continues the published HA1081 / exact HA1068-derived launcher baseline.
It does not contain the abandoned embedded-navigator experiment.

## Android update identity

- Package: `ru.natro.statuswidget`
- Version name: `v2.8.2-ha1082`
- Version code: `208021082`
- Minimum / target SDK: `28` / `28`
- Required signing certificate SHA-256:
  `6e9855aedc008bbdd8a7fbf3f490be07f964b7ac658a837a1592647a08365c75`
- Required signing public key SHA-256:
  `2e65689cb623b9fe6ccf332857bd22a363fa4932f33b9c10e86c9209d15b6518`

The package and release signing configuration are unchanged from HA1068 through HA1081.
The version code is higher than HA1074 and HA1081, so a correctly signed HA1082 APK installs
as an in-place update.

## Included changes

- Added the configurable old-style Monjaro driver rail as a continuous full-height overlay.
- Added up to ten independently sized buttons, ordering, labels, vector presets, colors and panel
  appearance controls with a live settings preview.
- Added Home, Back, all-apps and a movable stock-climate proxy button. The proxy temporarily makes
  the rail input-transparent and taps the fixed OEM climate coordinate under it.
- Added a resolution-independent live climate icon. It displays driver temperature with one
  decimal, a nine-segment manual fan scale or a five-segment AUTO intensity scale. When climate is
  off it displays only the centered muted temperature.
- Reused the confirmed `climate.fan` routing and freshness policy from the existing climate panel,
  including the vehicle-specific two- and three-profile AUTO families. No independent mode guess
  or stale boot value is used.
- Added a complete installed-app catalog including system, disabled and non-launcher packages.
  Non-launchable packages remain visible and are explicitly identified.
- Kept the all-apps drawer on the same successful ECARX system layer as the rail so it appears
  above application and floating-navigation windows.
- Added boot restoration, ECARX navigation-bar lifecycle handling and direct settings navigation.

## Local verification before Git publication

- AAPT2 compiled all application resources.
- Focused geometry, touch-proxy, app-catalog, temperature and fan-indicator suites passed `9/9`.
- The driver rail controller, live climate view, settings activity and foreground services passed
  direct Java 21 semantic compilation against Android API 36.
- `git diff --check` passed.

The signed APK is produced only by the existing GitHub release workflow, which verifies the exact
package name, version, 16 KiB zip alignment and stable certificate fingerprint before publication.
