# Status Widget HA1172

Date: 2026-08-05
Version name: `v2.8.2-ha1172`
Version code: `208021172`

## Apple continuous notification corners

- Replaces Android circular `cornerRadius` geometry with one exponent-five continuous-corner
  path for the complete phone-notification card and its application icon.
- The card background, final alpha silhouette and optional border all use the same normalized
  path. No opaque rectangle, black corner cover or mismatched `GradientDrawable` remains.
- The application icon uses that same path after the `ImageView` has applied its saved aspect-ratio
  mode. Its exact-size bitmap buffer remains independent from the parent card buffer for the KX11
  Android 9 translucent-overlay compositor.
- Both the settings preview and live WindowManager notification continue to instantiate
  `PhoneNotificationCardView`, so their geometry cannot diverge.
- Existing saved corner values remain readable. The editor now labels them as Apple continuous
  card bend and Apple icon shape instead of ordinary rounding.

## Included previous fixes

- Actual one/two-line top-or-centre text alignment remains independent for every text field.
- Previous-track artwork stays rejected for the whole current track.
- Physical climate-button centring and Helper/BLE transport are unchanged.

## Verification gates

- Full Java unit and release-contract suite.
- Exact HA1172 unsigned release-candidate build in GitHub Actions.
- Stable-certificate APK package/version, v3 signature and zip-alignment verification.
