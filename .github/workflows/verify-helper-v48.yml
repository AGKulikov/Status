name: Verify iPhone Helper v48 · ANCS v2 clean-room

on:
  push:
    paths:
      - 'ios/KX11-iPhone-ANCS-Helper-v48/**'
      - 'docs/ancs-v2-wire-vectors.json'
      - 'app/src/main/java/dezz/status/widget/phone/transport/switching/ble-role-switch-transition-vectors.json'
      - '.github/workflows/verify-helper-v48.yml'
  pull_request:
    paths:
      - 'ios/KX11-iPhone-ANCS-Helper-v48/**'
      - 'docs/ancs-v2-wire-vectors.json'
      - 'app/src/main/java/dezz/status/widget/phone/transport/switching/ble-role-switch-transition-vectors.json'
      - '.github/workflows/verify-helper-v48.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-helper-v48:
    name: Compile and replay Helper v48 without signing
    runs-on: macos-15
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Record exact Helper source identity
        id: source
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          test -z "$(git status --short)"
          HELPER_TREE=$(git rev-parse HEAD:ios/KX11-iPhone-ANCS-Helper-v48)
          echo "helper_tree=$HELPER_TREE" >> "$GITHUB_OUTPUT"

      - name: Replay shared wire/switch/restoration contracts
        shell: bash
        run: ios/KX11-iPhone-ANCS-Helper-v48/verify-v48-contract.sh

      - name: Validate project settings
        shell: bash
        run: |
          set -euo pipefail
          xcodebuild \
            -project ios/KX11-iPhone-ANCS-Helper-v48/KX11ANCSHelper.xcodeproj \
            -target KX11ANCSHelper \
            -configuration Release \
            -sdk iphonesimulator \
            CODE_SIGNING_ALLOWED=NO \
            -showBuildSettings >"$RUNNER_TEMP/helper-v48-build-settings.txt"
          grep -q 'PRODUCT_BUNDLE_IDENTIFIER = ru.natro.kx11ancshelper' \
            "$RUNNER_TEMP/helper-v48-build-settings.txt"
          grep -q 'MARKETING_VERSION = 48.0' "$RUNNER_TEMP/helper-v48-build-settings.txt"
          grep -q 'CURRENT_PROJECT_VERSION = 48' "$RUNNER_TEMP/helper-v48-build-settings.txt"

      - name: Compile Helper v48 Release simulator
        shell: bash
        run: |
          set -euo pipefail
          xcodebuild \
            -project ios/KX11-iPhone-ANCS-Helper-v48/KX11ANCSHelper.xcodeproj \
            -target KX11ANCSHelper \
            -configuration Release \
            -sdk iphonesimulator \
            SYMROOT="$RUNNER_TEMP/KX11ANCSHelperV48Products" \
            OBJROOT="$RUNNER_TEMP/KX11ANCSHelperV48Intermediates" \
            CODE_SIGNING_ALLOWED=NO \
            clean build

      - name: Verify unsigned product
        shell: bash
        run: |
          set -euo pipefail
          app="$RUNNER_TEMP/KX11ANCSHelperV48Products/Release-iphonesimulator/KX11ANCSHelper.app"
          test -d "$app"
          test -x "$app/KX11ANCSHelper"
          test ! -d "$app/_CodeSignature"
          test -f ios/KX11-iPhone-ANCS-Helper-v48/README.md
          test -f ios/KX11-iPhone-ANCS-Helper-v48/RELEASE.txt
          test -x ios/KX11-iPhone-ANCS-Helper-v48/verify-v48-contract.sh

      - name: Package exact unsigned app and source
        id: package
        shell: bash
        env:
          HELPER_TREE: ${{ steps.source.outputs.helper_tree }}
        run: |
          set -euo pipefail
          products="$RUNNER_TEMP/KX11ANCSHelperV48Products/Release-iphonesimulator"
          app_archive="$RUNNER_TEMP/KX11-ANCS-Helper-v48-unsigned-simulator.tar.gz"
          source_archive="$RUNNER_TEMP/KX11-ANCS-Helper-v48-source.zip"
          tar -czf "$app_archive" -C "$products" KX11ANCSHelper.app
          staging="$RUNNER_TEMP/KX11-iPhone-ANCS-Helper-v48"
          mkdir -p "$staging/shared"
          git archive "$GITHUB_SHA:ios/KX11-iPhone-ANCS-Helper-v48" \
            | tar -x -C "$staging"
          git show "$GITHUB_SHA:docs/ancs-v2-wire-vectors.json" \
            > "$staging/shared/ancs-v2-wire-vectors.json"
          git show "$GITHUB_SHA:app/src/main/java/dezz/status/widget/phone/transport/switching/ble-role-switch-transition-vectors.json" \
            > "$staging/shared/ble-role-switch-transition-vectors.json"
          ditto -c -k --sequesterRsrc --keepParent "$staging" "$source_archive"
          APP_SHA=$(shasum -a 256 "$app_archive" | awk '{print $1}')
          SOURCE_SHA=$(shasum -a 256 "$source_archive" | awk '{print $1}')
          XCODE_VERSION=$(xcodebuild -version | tr '\n' ' ' | sed 's/ $//')
          SDK_VERSION=$(xcrun --sdk iphonesimulator --show-sdk-version)
          BUILD_ARCH=$(uname -m)
          jq -n \
            --arg sourceSha "$GITHUB_SHA" \
            --arg helperTree "$HELPER_TREE" \
            --arg appArchiveSha256 "$APP_SHA" \
            --arg sourceArchiveSha256 "$SOURCE_SHA" \
            --arg xcodeVersion "$XCODE_VERSION" \
            --arg sdkVersion "$SDK_VERSION" \
            --arg buildArch "$BUILD_ARCH" \
            '{sourceSha: $sourceSha, helperTree: $helperTree,
              bundleId: "ru.natro.kx11ancshelper", marketingVersion: "48.0",
              build: 48, platform: "iphonesimulator", configuration: "Release",
              deviceInstallable: false, signed: false,
              xcodeVersion: $xcodeVersion, sdkVersion: $sdkVersion, buildArch: $buildArch,
              appArchiveSha256: $appArchiveSha256,
              sourceArchiveSha256: $sourceArchiveSha256}' \
            > "$RUNNER_TEMP/helper-v48-candidate.json"
          (cd "$RUNNER_TEMP" && shasum -a 256 \
            KX11-ANCS-Helper-v48-unsigned-simulator.tar.gz \
            KX11-ANCS-Helper-v48-source.zip > helper-v48.sha256)

      - name: Upload unsigned app and exact clean-room source
        uses: actions/upload-artifact@v4
        with:
          name: KX11-ANCS-Helper-v48-unsigned-simulator
          if-no-files-found: error
          retention-days: 14
          path: |
            ${{ runner.temp }}/KX11-ANCS-Helper-v48-unsigned-simulator.tar.gz
            ${{ runner.temp }}/KX11-ANCS-Helper-v48-source.zip
            ${{ runner.temp }}/helper-v48-candidate.json
            ${{ runner.temp }}/helper-v48.sha256
