name: Build signed Natro 2.6.3 system shade

on:
  push:
    branches: ['feature/navigation-hud-v2']
    paths:
      - 'app/**'
      - 'build.gradle'
      - 'settings.gradle'
      - 'gradle/**'
      - 'gradlew'
      - 'tools/kx11-shade-*.sh'
      - '.github/workflows/build-natro-2.6.3-shade.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: natro-2.6.3-shade-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-sign-verify:
    runs-on: ubuntu-22.04
    timeout-minutes: 35
    env:
      VERSION_NAME: '2.6.3'
      VERSION_CODE: '208021296'
      ALLOW_UNSIGNED_RELEASE: 'true'
      BUILD_TOOLS_VERSION: '36.0.0'
      EXPECTED_CERT_SHA256: '6e9855aedc008bbdd8a7fbf3f490be07f964b7ac658a837a1592647a08365c75'
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle
      - name: Install Android SDK
        shell: bash
        run: |
          set -euo pipefail
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" \
            'platforms;android-36' "build-tools;${BUILD_TOOLS_VERSION}"
      - name: Test and assemble
        shell: bash
        run: |
          set -euo pipefail
          bash -n tools/kx11-shade-audit.sh tools/kx11-shade-component.sh
          python3 -m unittest discover -s tools/tests -p 'test_*.py' -v
          ./gradlew testGeelyDebugUnitTest assembleGeelyRelease --no-daemon
      - name: Sign with stable Natro key and verify
        id: artifact
        shell: bash
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
        run: |
          set -euo pipefail
          test -n "$KEYSTORE_BASE64"
          test -n "$KEY_PASSWORD"
          test -n "$KEY_ALIAS"
          TOOLS="$ANDROID_HOME/build-tools/${BUILD_TOOLS_VERSION}"
          INPUT=$(find app/build/outputs/apk/geely/release -name '*.apk' -print -quit)
          test -s "$INPUT"
          OUT="$RUNNER_TEMP/natro-2.6.3"
          mkdir -p "$OUT"
          KEYSTORE="$RUNNER_TEMP/natro-update.jks"
          printf '%s' "$KEYSTORE_BASE64" | base64 --decode > "$KEYSTORE"
          ALIGNED="$RUNNER_TEMP/Natro-2.6.3-aligned.apk"
          "$TOOLS/zipalign" -f -P 16 4 "$INPUT" "$ALIGNED"
          export KEY_PASSWORD
          "$TOOLS/apksigner" sign \
            --ks "$KEYSTORE" --ks-key-alias "$KEY_ALIAS" \
            --ks-pass env:KEY_PASSWORD --key-pass env:KEY_PASSWORD \
            --v1-signing-enabled true --v2-signing-enabled true \
            --v3-signing-enabled true \
            --out "$OUT/Natro-2.6.3-signed.apk" "$ALIGNED"
          CERTS=$("$TOOLS/apksigner" verify --verbose --print-certs \
            "$OUT/Natro-2.6.3-signed.apk")
          printf '%s\n' "$CERTS"
          printf '%s\n' "$CERTS" | grep -qi "$EXPECTED_CERT_SHA256"
          BADGING=$("$TOOLS/aapt" dump badging "$OUT/Natro-2.6.3-signed.apk")
          printf '%s\n' "$BADGING" | grep -F \
            "package: name='ru.natro.statuswidget' versionCode='208021296' versionName='2.6.3'"
          "$TOOLS/zipalign" -c -P 16 4 "$OUT/Natro-2.6.3-signed.apk"
          sha256sum "$OUT/Natro-2.6.3-signed.apk" | tee "$OUT/Natro-2.6.3-signed.apk.sha256"
          rm -f "$KEYSTORE" "$ALIGNED"
          echo "path=$OUT" >> "$GITHUB_OUTPUT"
      - name: Upload signed APK
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: Natro-2.6.3-system-shade-signed-${{ github.run_number }}
          path: ${{ steps.artifact.outputs.path }}/*
          if-no-files-found: error
          retention-days: 30
