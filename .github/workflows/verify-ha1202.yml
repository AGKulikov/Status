name: Verify HA1202 cold-start ANCS publication and authorization

on:
  push:
    branches:
      - 'work/ha1202-cold-start-authorization'
    paths:
      - 'app/**'
      - 'build.gradle'
      - 'gradle/**'
      - 'gradlew'
      - 'settings.gradle'
      - '.github/workflows/verify-ha1202.yml'
  pull_request:
    paths:
      - 'app/**'
      - 'build.gradle'
      - 'gradle/**'
      - 'gradlew'
      - 'settings.gradle'
      - '.github/workflows/verify-ha1202.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: F04 publication, ANCS authorization and unsigned geelyRelease
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v2

      - name: Install Android SDK 36
        shell: bash
        run: |
          set -euo pipefail
          set +o pipefail
          yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" --licenses >/dev/null
          set -o pipefail
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" \
            "platforms;android-36" "build-tools;36.0.0"

      - name: Preserved HA1197-HA1201 and HA1202 cold-start contracts
        shell: bash
        run: |
          set -euo pipefail
          chmod +x gradlew
          ./gradlew --no-daemon --no-configuration-cache --max-workers=1 \
            testGeelyDebugUnitTest \
            --tests dezz.status.widget.Ha1197TrunkAndReconnectContractTest \
            --tests dezz.status.widget.Ha1198AutomaticBleRecoveryContractTest \
            --tests dezz.status.widget.Ha1199ReferenceAncsConnectionContractTest \
            --tests dezz.status.widget.Ha1200CurrentLinkSecurityContractTest \
            --tests dezz.status.widget.Ha1201DirectAttachContractTest \
            --tests dezz.status.widget.Ha1202ColdStartAuthorizationContractTest

      - name: Compile complete unsigned geelyRelease APK
        shell: bash
        env:
          ALLOW_UNSIGNED_RELEASE: 'true'
          VERSION_NAME: 'v2.8.2-ha1202'
          VERSION_CODE: '208021202'
        run: |
          set -euo pipefail
          ./gradlew --no-daemon --no-configuration-cache --max-workers=1 \
            assembleGeelyRelease

      - name: Upload exact unsigned release candidate
        uses: actions/upload-artifact@v4
        with:
          name: ha1202-unsigned-release
          path: app/build/outputs/apk/geely/release/*.apk
          if-no-files-found: error
          retention-days: 7
